Title: Classical Adversarial Fault-Tolerance and PCPs

URL Source: https://arxiv.org/pdf/2608.16860v1

Published Time: Tue, 18 Aug 2026 02:26:28 GMT

Number of Pages: 74

Markdown Content:
# Classical Adversarial Fault-Tolerance and PCPs ∗

Anurag Anshu Harvard University 

anuraganshu@fas.harvard.edu 

Nikolas P. Breuckmann University of Bristol 

niko.breuckmann@bristol.ac.uk 

Louis Golowich UC Berkeley 

lgolowich@berkeley.edu 

Quynh T. Nguyen Harvard University 

qnguyen@g.harvard.edu 

Umesh Vazirani UC Berkeley 

vazirani@cs.berkeley.edu 

Abstract 

We show how to compile an arbitrary classical circuit into a fault-tolerant circuit, which performs the desired computation even when an almost-linear number of bits are adversarially chosen and corrupted in each timestep. Using a variant of this fault-tolerance scheme that only detects (rather than corrects) corruptions, we give a new construction of probabilistically checkable proofs (PCPs) for NP with polylogarithmic query complexity. This PCP construction from fault-tolerance presents a promising candidate for quantization by the work of Anshu, Breuckmann, and Nguyen (STOC’24), who provided a roadmap for constructing quantum PCPs via fault-tolerance. 

> ∗

To appear in 67th IEEE Symposium On Foundations Of Computer Science (FOCS) 2026. 

1arXiv:2608.16860v1 [cs.CC] 17 Aug 2026 Contents 

1 Introduction 3

1.1 Background on Circuits and Fault-Tolerance/Fault-Detection . . . . . . . . . . . . . 41.2 Main Result on Fault-Tolerance/Fault-Detection . . . . . . . . . . . . . . . . . . . . 51.3 Scheme via Tensor Code Switching . . . . . . . . . . . . . . . . . . . . . . . . . . . . 51.4 Comparison to Prior Fault-Tolerance Schemes . . . . . . . . . . . . . . . . . . . . . . 81.5 Main Result on PCPs . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 91.6 PCP Construction . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 10 1.7 Comparison to Prior PCP Constructions . . . . . . . . . . . . . . . . . . . . . . . . . 11 

2 Preliminaries 12 

2.1 Notation . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 12 2.2 Error-Correcting Codes . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 12 2.2.1 Tensor Product Codes . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 13 2.2.2 Reed-Solomon Codes . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 14 2.3 Gates and Circuits . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 15 2.4 Fault-Tolerance and Fault-Detection . . . . . . . . . . . . . . . . . . . . . . . . . . . 17 2.5 Gadget Composition . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 20 2.5.1 Sequential Composition . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 20 2.5.2 Parallel Composition . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 21 

3 Fault-Tolerant and Fault-Detecting Gadgets For Tensor Codes 23 

3.1 Error-Detection Gadget . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 23 3.2 Error-Correction Gadget . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 25 3.3 Code Switching Gadgets . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 28 3.4 Transversal Gate Gadgets . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 34 

4 Fault-Detecting and Fault-Tolerance Schemes 36 

4.1 Logical Circuit Compilation . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 38 4.2 Code Family . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 41 4.3 Fault-Detecting and Fault-Tolerant Gadgets . . . . . . . . . . . . . . . . . . . . . . . 43 

5 Instantiation and Alphabet Reduction 52 

5.1 Increasing and Reducing Alphabet Size . . . . . . . . . . . . . . . . . . . . . . . . . 54 5.2 Mending Fault-Detecting Gadget . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 55 5.3 Fault-Tolerant Gadget . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 62 

6 Probabilistically Checkable Proofs 66 

21 Introduction 

The celebrated PCP theorem [AS98, ALM +98] proves that large computations can be verified extremely quickly, with queries to just a few random bits of a suitably constructed proof. This verification is inherently robust , in that it will succeed even if a constant fraction of the proof’s bits are corrupted, because the sparse queries are unlikely to land on corrupted bits. It is then natural to ask whether we can perform , rather than simply verify, a computation in a manner that is similarly fault-tolerant, i.e. robust to large errors. In this work, we give an affirmative answer: we show how to compile any classical circuit into a fault-tolerant version, which simulates the original circuit even when an almost-linear number of bits are corrupted in every timestep. Our fault-tolerance holds even when the set of bits, as well as the errors applied, are chosen adversarially. We apply this fault-tolerance scheme to give a new proof of a “polylog-weak” PCP theorem, which has polylogarithmic query complexity instead of the (optimal) constant query complexity of the PCP theorem in [AS98, ALM +98]. Multiple aspects of our construction of PCPs via fault-tolerance may be of independent in-terest. Our construction is elementary in nature, as it primarily consists of manipulating tensor products of Reed-Solomon codes. The main prior result we rely on is that tensor product codes are locally testable, meaning that the weight of a corruption on a codeword can be estimated using a small number of random queries. This testability result itself has a clean combinatorial proof given by [Vid15]. We construct our PCPs from such locally testable codes without relying on stronger properties present in many prior PCP constructions, such as local decodability or lo-cal correctability. While some other PCP constructions share many of these aspects, we present these ingredients through the lens of fault-tolerance; see Section 1.7 for a more detailed comparison to prior PCPs. Furthermore, and perhaps most notably, our PCP construction is an appealing candidate for quantization, as we discuss below. The connection between fault-tolerance and PCPs was previously explored by [GS95]. They show that if arbitrary logical 1 circuits can be turned into fault-tolerant circuits with polynomial size overhead and depth O(log s), where s is the size of the original circuit, then a polylog-weak PCP theorem follows. They also demonstrate the existence of such a scheme. However, there are a couple of important caveats: (a) their construction is already based on the (stronger) PCP theorem itself, so it does not yield a new proof, and (b) they assume the input is provided in an encoding that depends on the logical circuit, and hence can directly include information about the output value. Our work directly addresses both of these issues, which were left as open questions in [GS95]. Other classical fault-tolerance schemes have been also developed without using PCPs, e.g. [Neu56, DO77, Pip85, Spi96], see Section 1.4 for a more comprehensive overview. However, these schemes were only resilient against adversarial errors on at most a polynomially vanishing fraction of bits, and hence are too weak to construct PCPs with subpolynomial query complexity. In contrast, we emphasize the truly global nature of the corruptions that our fault-tolerance scheme protects against. Specifically, we map a logical circuit acting on ¯N logical bits to a fault-tolerant circuit acting on N ≤ ¯N 1+ o(1) physical bits, which can protect against adversarial errors acting on N −o(1) -fraction of the physical bits in each timestep. This protection provides a super-polynomial improvement over prior schemes, which could only handle an inverse polynomial N −Ω(1) -fraction errors, assuming polynomial space overhead N ≤ ¯N O(1) . Indeed, the traditional approach     

> 1We refer to a (likely non-fault-tolerant) circuit that we want to run as a logical circuit, and a fault-tolerant version of it as a physical circuit.

3of repeating each logical bit many times is always vulnerable to errors on ¯N −1-fraction of the physical bits, which can wipe out all copies of a given logical bit [Neu56, DO77, Pip85]. We avoid this limitation by performing the entire computation on a joint encoding of all the logical bits, so that no logical bit is localized to a small set of physical bits that can easily be corrupted. Recently, [ABN24] suggested that fault-tolerance schemes could provide a route towards prov-ing the quantum PCP conjecture [AN02, AAV13], one of the major open questions in quantum complexity theory. A quantum analogue of the classical PCP theorem, the conjecture would imply that quantum computations can be efficiently verified by querying only a constant number of qubits in an appropriate proof. While there exist significant barriers to translating existing classical PCP constructions to the quantum setting [AAV13], the proposed approach of [ABN24] seems to avoid these barriers. Yet this approach requires fault-tolerance schemes protecting against an almost-linear number of errors with respect to the total number N of qudits. Achieving such strong error protection was previously an open problem; prior schemes could only protect against a smaller polynomial number N 1−Ω(1) 

of errors (see above). Our work resolves this question in the classical case. A related fault-tolerance scheme protecting against an almost-linear number of errors in the quantum case is given in the follow-up work [BGV26]. There remain open questions from [ABN24] regarding the mapping from fault-tolerant circuits to PCPs, whose resolution is needed to construct a quantum PCP with nontrivial (i.e. subpolynomial) query complexity. However, our classical PCPs provide strong evidence of the viability of this approach towards quantum PCPs. The remainder of this section provides more details on our results and techniques. In Section 1.1 below, we begin by informally defining fault-tolerance schemes, as well as a slightly weaker notion of fault-detecting schemes, which only detect corruptions instead of correcting them. The fully rigorous and general definitions can be found in Section 2. In Section 1.2, we state our main result providing fault-tolerance/detecting schemes against adversarial errors. We outline the construction and proof of this result in Section 1.3. We then present our main result on PCPs in Section 1.5, and in Section 1.6 we describe how we prove this result using our fault-detecting scheme. We compare our results and techniques to prior works in Sections 1.4 and 1.7. 

1.1 Background on Circuits and Fault-Tolerance/Fault-Detection 

In this paper, we consider circuits with gates of constant fan-in and fan-out. Each circuit acts on some set N of “dits,” i.e. symbols taking a value in some finite field Fq. The circuit runs over some number T of timesteps. Each dit is acted on by a gate (which may be the identity) at every timestep. We specifically use a gate set that can add or multiply any two dits (see Definition 2.10 for details). Defining fault-tolerance requires some care; we follow the general approach of [NP25, HNP25], which was developed for the (even more delicate) quantum setting. Specifically, our goal is to compile a logical circuit ¯R into a physical circuit R such that R simulates ¯R even in the presence of noise. However, ¯R may have input and output dits taking on arbitrary values not known during the compilation. It then seems that even a single corruption on the input or output of R may render the output incorrect. To resolve this issue, we specify encoding maps Enc in (resp. Enc out ), which map inputs (resp. outputs) of ¯R to inputs (resp. outputs) of R. The images of these encoding maps should be error-correcting codes , meaning that the encoding map can be inverted even after some dits have been corrupted. Then loosely speaking, we define a fault-tolerance scheme with error thresholds λin , λ run , λ out 

4to be a compiler from logical circuits ¯R to physical circuits R satisfying the following: for every input x to ¯R, if we run R on some input y that differs from Enc in (x) on an arbitrary set of < λ in 

dits, such that an arbitrary set of < λ run dits are corrupted after each timestep, then the resulting output differs from Enc out ◦ ¯R(x) on < λ out dits. We will want to construct schemes with λin ≥ λout ,so that we can sequentially compose fault-tolerant circuits while maintaining low error weight. In our applications to PCPs, it will in fact be sufficient to construct a fault-detecting scheme, which weakens the notion of a fault-tolerance scheme to only detect errors, rather than correct them. Specifically, we introduce an additional “detection threshold” parameter λdet , and we specify a set of “detector dits” in each timestep of our circuit. A fault-detecting scheme is then like a fault-tolerance scheme, except that the output is allowed to be wrong if ≥ λdet detector dits were nonzero in some timestep. That is, a fault-detecting scheme is a compiler from logical circuits ¯R

to physical circuits R satisfying the following: for every input x to ¯R, if we run R on some input y

that differs from Enc in (x) in an arbitrary set of < λ in dits, such that an arbitrary set of < λ run 

dits are corrupted after each timestep, then either the resulting output differs from Enc out ◦ ¯R(x)in < λ out dits, or else ≥ λdet detector dits were nonzero in some timestep. 

1.2 Main Result on Fault-Tolerance/Fault-Detection 

Our main result constructing fault-tolerance and fault-detecting schemes is stated below. 

Theorem 1.1 (Informal statement of Theorem 5.1) . The following statements hold over every constant alphabet Fr:1. For every set ¯N of logical dits and every ϵ > 0, there exists a fault-detecting scheme that maps an arbitrary logical circuit ¯R acting on dits ¯N using time ¯T to a fault-detecting physical circuit R acting on |N | ≤ | ¯N |1+ ϵ dits using time T ≤ ¯T · (log |N |)O(1 /ϵ ), with error thresholds 

λin ≥ λout ≥ λrun = λdet ≥ |N |

(log |N |)O(1 /ϵ ) .

2. For every set ¯N of logical dits, there exists a fault-tolerance scheme that maps an arbitrary logical circuit ¯R acting on dits ¯N using time ¯T to a fault-tolerant physical circuit R acting on |N | ≤ | ¯N | · 2O(log |N |)2/3

dits using time T ≤ ¯T · 2O(log |N |)2/3

, with error thresholds 

λin ≥ λout ≥ λrun ≥ |N |

2O(log |N |)2/3 .

Because fault-detection is a weaker requirement than fault-tolerance, in Theorem 1.1 we are able to construct fault-detecting schemes with error thresholds |N |/ poly log |N |, whereas our fault-tolerance schemes have slightly lower error thresholds of |N |/2O(log |N |)2/3

= |N |1−o(1) . As a result, the PCPs we obtain from our fault-detecting scheme have query complexity poly log |N |, whereas our fault-tolerance scheme would only give PCPs with query complexity |N |o(1) .

1.3 Scheme via Tensor Code Switching 

We now outline our fault-tolerance and fault-detecting schemes in Theorem 1.1. The two schemes use similar codes and protocols; the main difference is that our fault-tolerance scheme runs a 5decoder to correct errors, whereas our fault-detecting scheme simply applies local testability to detect errors. For both schemes, we encode the logical dits in ¯N into a tensor product of Reed-Solomon codes, as defined below. As described below, the key insight is that we can efficiently switch between different such tensor codes in a fault-tolerant (or fault-detecting) manner. This code switching gives us the necessary flexibility to perform a general computation with good fault-tolerance/detection. Recall that a classical (linear error-correcting) code of length n, dimension k, distance d, and alphabet size q is a k-dimensional linear subspace C ⊆ Fnq such that every nonzero codeword 

c ∈ C \ { 0} has Hamming weight |c| ≥ d. We summarize these parameters by saying C is a [ n, k, d ]q

code. 

Definition 1.2. For a subset E ⊆ Fq of size |E| = n, the the Reed-Solomon code RS( q, k, E ) ⊆

FEq = Fnq is the [n, k, n − k + 1] q code whose codewords are evaluations (f (x)) x∈E of polynomials 

f ∈ Fq[X]<k of degree < k .

Reed-Solomon codes are useful for fault-tolerant computation because they support “transversal multiplication” (also called a “multiplication property”): for codewords c, c ′ ∈ RS( q, k, E ), then the component-wise (i.e. transversal ) product c∗c′ := ( cxc′

> x

)x∈E is a codeword in the higher-dimensional Reed-Solomon code RS( q, 2k −1, E ). All linear codes also support component-wise (i.e. transversal) addition, as for c, c ′ ∈ C, then c + c′ ∈ C. Because addition and multiplication form a universal gate set over a finite field, we can perform universal computation with transversal operations on Reed-Solomon codes. Because each transversal operation performs a single gate to each dit in a codeword, it cannot propagate errors between dits in a codeword, and hence is naturally well-suited for achieving fault-tolerance. We specifically use systematic encoding maps for all our codes, meaning that the message equals the restriction of the codeword to an appropriate ku dits. Hence transversal addition and multiplication on codewords induces addition and multiplication on the underlying message dits. There are three main issues with trying to achieve a full fault-tolerance scheme (in the sense of Theorem 1.1) using Reed-Solomon codes: 1. After too many multiplications, the code dimension k will surpass the block length n, at which point the distance vanishes. 2. While transversal gates limit error propagation, we ultimately want to reduce the size of a corruption by correcting, or at least detecting, the errors. 3. It is unclear how to perform gates between different message dits encoded in the same code-word. We resolve all of these issues by performing our fault-tolerant circuit not on Reed-Solomon codes, but rather on tensor products of such codes, as defined below. 

Definition 1.3. The tensor product C1 ⊗ C2 of codes C1, C 2 of respective length n1, n 2 consists of all matrices c ∈ Fn1×n2 

> q

such that every column lies in C1 and every row lies in C2.More generally for u ∈ N, for i ∈ [u] let Ci be a [ni, k i, d i]q code for i ∈ [u]. For i ∈ [u], we say a direction-i column in Q

> i∈[u]

[ni] is a subset [ni] × { j−i} ⊆ Q

> i′∈[u]

[ni′ ] (where we push [ni] to the 

ith factor) for some j−i ∈ Q

> i′∈[u]\{ i}

[ni′ ]. Then the [Q 

> i∈[u]

ni, Q 

> i∈[u]

ki, Q 

> i∈[u]

di]q tensor product code C = N 

> i∈[u]

Ci is the code containing every c ∈ F 

> Q
> i∈[u][ni]
> q

whose restriction to every direction-i

column is a codeword in Ci for every i ∈ [u].

6Our fault-detecting and fault-tolerance schemes in Theorem 1.1 use input and output codes RS( q, k, E )⊗u for an arbitrary size-n subset E ⊆ Fq, where n, k, u are appropriately chosen functions of | ¯N | to ensure that the code has large distance, as well as large enough dimension ku ≥ | ¯N | to store the logical dits. In each timestep of our physical circuit, the logical dits are also encoded in such a tensor product of Reed-Solomon codes. These tensor products preserve transversal multiplication (and of course, transversal addition) of the underlying Reed-Solomon codes. That is, for c, c ′ ∈ RS( q, k, E )⊗u, then c ∗ c′ ∈ RS( q, 2k −

1, E )⊗u. However, we now also have a way to reduce the dimension of the multiplied code from (2 k − 1) u back down to the original dimension ku: we sequentially loop through i = 1 , . . . , u , and for each i, we unencode the RS( q, 2k − 1, E ) code in each direction-i column, and then re-encode the resulting message into the code RS( q, k, E ). This procedure effectively switches the factors RS( q, 2k − 1, E ) in the tensor product back to RS( q, k, E ) one at a time. This code switching procedure has one major problem: it is not fault-tolerant, as a single error may propagate to cause many more. Fortunately, we can address this issue by repeatedly performing error-correction on N 

> i∈[u]

Ci with a similar technique: we can sequentially loop through 

i = 1 , . . . , u , and for each i we run a decoder for Ci to correct errors within every direction-i column in parallel. In Lemma 3.5, we show that this error-correction procedure is fault-tolerant, meaning that it corrects more errors than it introduces, even in the presence of an almost-linear number of new errors at every timestep, as long as u grows as an appropriate function of n. Our key observation to prove Lemma 3.5 is that the new errors occurring during error-correction can only propagate within some appropriately defined closure set, which is not too large by a bound of [KP25]. Outside of this closure set, the error-correction proceeds noiselessly as if no new errors had occurred, and hence will successfully correct any input error that is not too high-weight. Hence the error-correction procedure only leaves a small residual error bounded by the closure set. If we only need to detect errors rather than correct them (as in the fault-detection scheme in Theorem 1.1, we can instead simply apply the result of [Vid15] showing that all tensor product codes of sufficiently large distance are locally testable. Hence to detect errors on N 

> i∈[u]

Ci, [Vid15] shows it is sufficient to loop through i = 1 , . . . , u , and check if every direction-i column lies in Ci;if most checks pass, any corruption on our codeword must have low weight. This procedure can detect more errors than our error-correction procedure, leading to the higher error thresholds for fault-detection than fault-tolerance in Theorem 1.1. Related ideas involving code switching in one direction at a time has previously been used in the quantum fault-tolerance literature (e.g. [Bom16, JO19, THL +25, GCZ25, XZB +25]), and is also reminiscent of the classical sum-check protocol (see Section 1.7). We have now seen how to perform addition, multiplication, and error-correction on code words in a fault-tolerant or fault-detecting manner. However, these addition and multiplication opera-tions act transversally, meaning if we add or multiply codewords c, c ′ that (systematically) encode messages m, m ′, then we can only add or multiply mj with m′ 

> j′

if j = j′.To implement logical circuits with general connectivity, we again turn to code switching. Specifi-cally, given a codeword in N 

> i∈[u]

Ci, we may choose some i ∈ [u] and then unencode every direction-i

column to obtain k = dim( Ci) codewords of N 

> i′∈[u]\{ i}

Ci′

. We may then perform transversal gates between these k codewords, before re-encoding in every direction-i column to return to our orig-inal tensor code N 

> i∈[u]

Ci. By repeating this procedure in all u directions i ∈ [u], interspersed with rounds of error correction/detection as described above, we obtain fault-tolerant/detecting circuits with hypercubic connectivity. That is, our logical dits are arranged in a u-dimensional 7hypercube [ k]u, and we can perform gates between dits labeled j, j ′ ∈ [k]u if j and j′ only differ in a single component i (by unencoding in direction i and then performing transversal gates). To compile a general logical circuit to respect this connectivity, we apply the sorting networks of [Bat68] (see Lemma 4.4), which show how to arbitrarily rearrange dits in the hypercube [ k]u using a poly log( ku)-depth circuit, in which each timestep performs transversal gates in some direction i ∈

[u]. Hence for every gate that we want to perform in some timestep of the logical circuit, we permute the logical dits acted upon by the gate to be neighbors in the hypercube [ k]u, and then we implement the gate transversally. Recall that every transversal gate here in turn is performed by unencoding the code Ci in every direction-i column, performing transversal addition or multiplication, re-encoding in direction-i, and then performing direction-by-direction dimensional reduction (if we performed a multiplication) and error-correction or error-detection as appropriate. Combining all of the components described above, we obtain Theorem 1.1. Note that while Reed-Solomon codes, and hence their tensor products, have growing alphabet size Fq, we stated Theorem 1.1 over a constant-sized alphabet Fr. Therefore to prove Theorem 1.1, we construct our fault-tolerance/detecting scheme over an extension field Fq = Frκ ∼= Fκr , and then we replace each q-ary dit with a size-κ block of r-ary dits. This alphabet reduction slightly degrades the error thresholds relative to the number of dits, though the loss is insignificant in our parameter regimes. 

1.4 Comparison to Prior Fault-Tolerance Schemes 

The study of fault-tolerant computation was initiated by von Neumann [Neu56], who proposed repeating each bit many times (i.e. encoding in a repetition code), and correcting errors by com-paring the different copies of a bit. Follow-up works later made this scheme rigorous and explicit [DO77, Pip85]. This scheme can with high probability correct random errors using only logarith-mic redundancy. However, if an adversary is allowed to choose the error locations, they can flip all copies of a single bit to corrupt the output. Hence the computation remains vulnerable to corruptions on localized sets of bits. More explicitly, if there are | ¯N | logical bits and |N | physical bits, the scheme is robust to at most |N |/| ¯N | adversarial errors. The repetition code scheme can only achieve exponential error suppression against random errors by introducing polynomial space overhead |N |/| ¯N | = poly( | ¯N |). Spielman [Spi96] showed how to reduce this space overhead to poly log  | ¯N | using a scheme based on a tensor product of Reed-Solomon codes. Spielman’s scheme shares many characteristics with ours: it uses a degree reduction technique similar to our code switching following transversal multiplications, and it also transforms the logical circuit into one with hypercubic connectivity. However, [Spi96] only takes the product of u = 2 Reed-Solomon codes, and uses a different error-correction scheme, which leads to a tolerance of at most ˜O(|N |1/4) adversarial errors. In contrast, by taking the product of a growing number u of Reed-Solomon codes, and decoding in one direction at a time, we are able to protect against an almost-linear number |N |1−o(1) adversarial errors. [Spi96] also requires the Reed-Solomon evaluation points to respect a particular symmetry in order to obtain appropriate logical permutations. Our scheme instead works for arbitrary evaluation points, as we unencode out of a factor code before permuting along the associated axis in the hypercube. As described above, G´ al and Szegedy [GS95] constructed a sort of fault-tolerance scheme against an almost-linear number of adversarial errors. However, this scheme relied on PCPs, and also does not match our definition of a fault-tolerance scheme, as it allowed the input encoding to contain information about the logical output. Other lines of work have also studied different error models, such the short-circuit model, where 8every corrupted gate must output one of its input bits [KLM97, KLR12, BEGY19, EHK +22]. In particular, in this model [EHK +22] obtained fault-tolerance against adversarial errors acting on a constant fraction of gates along every path through the circuit. However, short-circuit errors are much more restrictive than the general errors that we permit. In particular, the works above obtain fault-tolerance against short-circuit errors without needing to encode the logical input, whereas fault-tolerance in our model is impossible if the input is unencoded (see Section 1.1). The study of robust multi-party computation (MPC), which dates back to [BOGW88], provides a form of fault-tolerance in which several parties communicate to collectively compute a function, even when some parties may become corrupted. Our fault-tolerance scheme uses certain related techniques, including degree reduction on Reed-Solomon codes and routing networks, as some such MPC protocols (e.g. [DIK10]). However, we emphasize that our notion of fault-tolerance requires robustness against faults that corrupt different sets of dits (i.e. parties) in different timesteps, whereas MPC protocols typically assume all parties outside of a “bad” set remain uncorrupted throughout the entire computation. Nevertheless, the MPC notion of fault-tolerance is sufficient for various applications, including for constructing certain proof systems (see e.g. [BISW18]). It is an interesting direction of future work to further develop the connection between our results and robust MPC protocols. Our fault-tolerance scheme also shares some characteristics with certain quantum fault-tolerance schemes. In particular, the idea of code switching in one direction at a time to obtain universal computation or to implement logical permutations has been used in various schemes (e.g. [Bom16, JO19, THL +25, GCZ25, XZB +25]). However, these schemes relied on quantum codes of distance at most O(p|N |), and hence could not protect against more than O(p|N |) adversarial errors. 

1.5 Main Result on PCPs 

We now state our main result on PCPs; we will subsequently describe how we prove it using our fault-detecting scheme in Theorem 1.1. We briefly review the necessary definitions in this section; see Section 6 for the details. We will describe PCPs in terms of constraint satisfaction problems (CSPs). For the purpose of this paper, a CSP instance P with n boolean variables and m constraints consists of a set of functions P = {P 1, . . . , Pm}, where each function Pj : FBj 

> 2

→ F2 maps some subset Bj ⊆ [n] of the variables to a boolean value. We restrict attention to constant-locality CSPs, meaning that every |Bj | = O(1), and evey variable lies in O(1) sets Bj . We say a constraint Pj is satisfied by an assignment x ∈ F[n] 

> q

of the variables if Pj (x|Bj ) = 0. We say P is satisfiable if there exists an assignment that satisfies every constraint. We now state our main result on PCPs. 

Theorem 1.4 (PCPs for circuit-satisfiability with inverse-polylogarithmic soundness; informal statement of Theorem 6.2) . For every ϵ > 0, there exists a polynomial-time algorithm that takes as input a circuit R of size s that has a single output bit, and outputs a constant-locality CSP instance 

P with n ≤ s1+ ϵ variables and m = Θ( n) constraints, which exhibits: 1. (Completeness) If there exists an input x such that R(x) = 0 , then P is satisfiable. 2. (Soundness) If every input x has R(x) = 1 , then every assignment for P has ≥ 1/(log m)O(1 /ϵ )

unsatisfied constraints. 

9To interpret Theorem 1.4 as providing a “probabilistically checkable proof” (PCP) that a given circuit R has a an input x with R(x) = 0, we view a satisfying assignment to the associated CSP instance P as the “proof.” Theorem 1.4 implies that if we sample and check a single random constraint from P, we will successfully detect if P is unsatisfiable with probability 1 /(log m)O(1 /ϵ )

while querying just a constant number of variables. If we repeat this test (log m)O(1 /ϵ ) many times, we boost the test’s success probability to 1 − 1/2(log m)O(1 /ϵ )

, while increasing the query complexity to polylogarithmic. Hence we can view Theorem 1.4 as providing polylog-query PCPs for the 

NP -complete problem of circuit-satisfiability. The PCP proof (i.e. list of variable assignments) in Theorem 1.4 has length n ≤ s1+ ϵ for arbitrarily small constant ϵ > 0. In contrast, state-of-the-art PCP constructions have length growing as s poly log s, while only requiring a constant number of queries to verify the proof with constant success probability [Din07]. 

1.6 PCP Construction 

We now describe how we construct PCPs to prove Theorem 1.4 from our fault-detecting scheme in Theorem 1.1. The main idea behind mapping a fault-tolerance scheme to a PCP was out-lined in [ABN24, Appendix A]. We adapt this technique to fault-detecting schemes to obtain the 

|N |/ poly log |N | error thresholds for fault-detection in Theorem 1.1. We begin by mapping the circuit R in Theorem 1.4 to a circuit ¯R using constant time (i.e. con-stant depth) such that R has an input x with R(x) = 0 iff ¯R has an input z with all output bits of ¯R(z) equal to 0. We will then apply Theorem 1.1 to the logical circuit ¯R to obtain a fault-detecting physical circuit R′. Our final CSP instance P in Theorem 1.4 will then have variables corresponding to bits at every point in space and time in a transcript of the execution of R′. The constraints of P will enforce that the transcript describes a valid execution of R′ with all-0s output. If a small number of constraints are violated, the transcript describes an execution of R′ with a small number of errors, which by the fault-detecting property still has the correct output (up to a low-weight corruption). An incorrect output can only be induced by a large number of errors in the execution of R′, which would result in many violated constraints of P. Hence completeness and soundness follow. We now provide a more detailed description of each of these steps. To construct ¯R, we first define the transcript tran( R; x) of R on input x to be a list of the values of every bit in R during every timestep of the execution of R on input x. Therefore tran( R; x) ∈ FS 

> 2

, where S is a list of all space-time locations in R.2 In particular, |S| = Θ( s) grows linearly in the size of R.We then let ¯R take as input some z ∈ FS 

> 2

. If R has g gates, then ¯R has g +1 outputs. Viewing z

as a transcript of the execution of R under the presence of noise, then the first output of ¯R equals the bit outputted by this execution of R (i.e. the unique bit in the final timestep of the transcript). The remaining g outputs of ¯R each equal 0 iff the g respective gates in the transcript were executed correctly, meaning that the gate’s outputs equal the gate’s image under its inputs. Therefore if z equals a transcript of a noiseless execution of R on some input x with output 

R(x) = 0, then all outputs of ¯R(z) are 0; otherwise, ¯R(z) has a nonzero output. Because every gate in R acts on a constant number of bits (as we assume constant fan-in and fan-out), each input to ¯R only affects a constant number of outputs, and each output only relies on a constant number     

> 2In Section 6, we actually define Sto omit idling locations where identity gates are applied, so that smeasures the number of non-identity gates. For simplicity in this informal presentation we ignore this detail.

10 of inputs. Thus we can implement ¯R using Θ( s) bits in constant time (i.e. constant depth). The key step in our PCP construction is now to apply our fault-detecting scheme in Theorem 1.1 to the logical circuit ¯R, in order to obtain a fault-detecting physical circuit R′. By Theorem 1.1, the set S′ of space-time locations in R′ will have size |R ′| = s1+ O(ϵ), and R′ will use time T ′ ≤

(log s)O(1 /ϵ ). Our final CSP instance P will have variables y ∈ FS′ 

> 2

labeled by the set S′. Viewing y

as the transcript of an execution of R′ under the presence of noise, then we define P to have constraints that check: 1. (Fault constraints) That every gate in R′ was executed correctly, 2. (Detector constraints) That every detector bit had value 0, and 3. (Output constraints) That the output of R′ was all-0s. Each fault constraint acts on a constant number of bits because we assume all gates have constant fan-in and fan-out. Meanwhile, each detector and output constraint just checks that a single bit is nonzero. Therefore P has constant locality. To see that the completeness in Theorem 1.4 holds, if there exists x with R(x) = 0, then we can set z = tran( R; x), and then we set y = tran( R′; Enc in (z)) to be the transcript of R′ on input given by the encoding (associated to the tensor Reed-Solomon codes from our fault-detecting scheme; see Section 1.1) of z. By construction ¯R(z) = 0, and hence all constraints in P will be satisfied by assignment y.We now turn to showing the soundness in Theorem 1.4. Suppose that y is an assignment for P

with few (i.e. < 1/(log m)O(1 /ϵ )-faction) unsatisfied constraints. Viewing y as the transcript for a noisy execution of R′, then it follows that most gates in R′ executed noiselessly, most detector bits did not detect an error, and most output bits were 0. Therefore if the restriction of y to the first timestep (corresponding to the input to R′) is close to some codeword Enc in (z) of the input code of our fault-detecting scheme, then the fault-detecting property implies that the restriction of y to the last timestep (corresponding to the output of R′) must be close to the encoding Enc out ◦ ¯R(z) of ¯R(z). Assuming our output code has high distance, then Enc out ◦ ¯R(z) has large Hamming weight unless ¯R(z) = 0. Hence by the assumption that most output constraints were satisfied, we must have ¯R(z) = 0, which by the definition of ¯R means that R(x) = 0 for some x, as desired for soundness. There is only one remaining case to consider, namely, if the restriction of y to the first timestep is far from all codewords in the image of Enc in . That is, y could be the transcript of an execution of R′ with a large input error, but then with few additional errors during the execution. To guard against such large input errors, we use the local testability of our tensor Reed-Solomon [Vid15] codes to test the weight of the input error, and add detector constraints to enforce a low-weight error. Note that we formalize this idea using the “mending” property in Definition 2.18 below. 

1.7 Comparison to Prior PCP Constructions 

Many PCP constructions are fundamentally based on similar ingredients as ours, namely, locally testable codes with sufficient algebraic structure to encode a computation. Indeed, multiple PCP constructions specifically rely on the local testability of tensor products of Reed-Solomon (or re-lated) codes, e.g. [BSS08, Mei12, BSKK +16]. Our fault-tolerance/detecting schemes that operate on tensor codes in one direction (i.e. one factor in the product) at a time are reminiscent of the sum-check protocol [LFKN92], which was generalized to operate on arbitrary tensor codes in [Mei13], 11 and is used in PCP constructions such as [AS98, BGH +06, BSKK +16, GOS24, GOS25]. Indeed, the sum-check protocol also operates on one factor of the tensor product at a time. However, whereas sum-check is only used to check the sum of logical dits, we use code switching to apply general logical circuits. This key idea was largely inspired by applications of code switching in the quantum fault-tolerance literature (see Section 1.4). A recent work [BMVY25] also constructed PCPs using fault-tolerance ideas. However, their objective and techniques are substantially different from ours. There, the authors constructed a fault-tolerant communication protocol on a high-dimensional expander that gives rise to small soundness and 2-query PCPs. 

# 2 Preliminaries 

This section presents preliminary notions and results. 

2.1 Notation 

In this section we describe basic notation that we will use throughout the paper. For n ∈ N, we write [ n] = {1, 2, . . . , n }. For a prime power q, we let Fq denote the finite field of order q. For a vector x ∈ Fn, we let supp( x) = {i ∈ [n] : xi̸ = 0 } denote the support, and we let |x| = | supp( x)|

denote the Hamming weight (or simply the “weight”) of x.For vectors x, y ∈ Fn, we let x · y = P 

> i∈[n]

xiyi denote the standard bilinear form, and we let 

x ∗ y = ( xiyi)i∈[n] ∈ Fn denote the component-wise product. We extend this notation to subspaces 

A, B ⊆ Fn by letting A ∗ B = span {a ∗ b : a ∈ A, b ∈ B}. We let In : Fnq → Fnq denote the identity map In(x) = x.For sets A, B , we let A ⊔ B denote the disjoint union, so that for instance A ⊔ A is isomorphic to A × [2]. We extend this notation to powers, so that A⊔n = A ⊔ · · · ⊔ A ∼= A × [n] denotes the disjoint union of n copies of A. We also use the notation ‘ ⊔’ for direct products of functions, so that if fi : Ai → Bi is a function for i ∈ [2], then f1 ⊔ f2 : A1 × A2 → B1 × B2 is the function (f1 ⊔ f2)( a1, a 2) = ( f1(a1), f 2(a2)). 

2.2 Error-Correcting Codes 

In this section we present definitions and basic results regarding error-correcting codes, which we use to construct our fault-tolerance scheme. 

Definition 2.1. A classical linear code (or simply code ) C over the alphabet Fq of length n and 

dimension k is a k-dimensional subspace C ⊆ Fnq . The distance of C is the minimum weight 

d = min c∈C\{ 0} |c| of a nonzero element of C. We summarize these parameters by saying that C is a [n, k ]q or a [n, k, d ]q code. An encoding map for C is a linear isomorphism Enc : Fkq

> ∼

−→ C. An encoding map is systematic 

if there exists a subset K ⊆ [n] of size |K| = k such that the restriction of the encoding to dits in 

K equals the message, i.e. for every x ∈ Fkq we have Enc( x)|K = x (under some fixed isomorphism 

K ∼= [ k]). 

The following fact follows from a basic Gaussian elimination argument. 12 Fact 2.2. For every [n, k ]q code C, there exists a set K ⊆ [n] such that there is a systematic encoding map Enc : Fkq ∼= FKq → Fnq for which Enc( x)|K = x. Furthermore, for every set K0 ⊆ [n]

such that C|K0 = FK0 

> q

, then K can be chosen to contain K0.

The primary codes we consider are tensor products of Reed-Solomon codes, defined below. 

2.2.1 Tensor Product Codes 

Here we define tensor product codes. 

Definition 2.3. Let q be a prime power and let u ∈ N. For i ∈ [u], let Ci be a [ni, k i, d i]q code with encoding map Enc i. The tensor product code (or simply tensor code ) C = N 

> i∈[u]

Ci is the 

[Q 

> i∈[u]

ni, Q 

> i∈[u]

ki, Q 

> i∈[u]

di]q code 

C = span 

O

> i∈[u]

ci : ci ∈ Ci ∀i ∈ [u]

 ,

with tensor product encoding map Enc = N 

> i∈[u]

Enc i given by 

Enc 

O

> i∈[u]

ci

 = O

> i∈[u]

Enc i(ci)

for ci ∈ Ci ∀i ∈ [u], and extended by linearity to all of F  

> Q
> i∈[u]ki
> q

= N 

> i∈[u]

Fki 

> q

.

Tensor codes can be equivalently defined as follows. For i ∈ [u] and j ∈ Q

> i′∈[u]\{ i}

[ni′ ], we say the associated direction-i column is the set of all j′ ∈ Q

> i′∈[u]

[ni′ ] such that j′ 

> i′

= ji′ for every 

i′ ∈ [u] \ { i}. Therefore every direction-i column has ni elements. Then the tensor product of 

C1, . . . , C u is simply the set of all vectors c ∈ F 

> Q
> i∈[u][ni]
> q

such that for every i ∈ [u], the restriction of c to every direction-i column is a codeword in Ci. In other words, the tensor code is the kernel of the parity-check matrix defined below. 

Definition 2.4. For u ∈ N, for i ∈ [u] let Hi ∈ Fmi×ni 

> q

be a matrix. Define sets 

N = [ n1] × · · · × [nu]

and S = F 

> i∈[u]

Si with each 

Si = [ n1] × · · · × [ni−1] × [mi] × [ni+1 ] × · · · × [nu].

The tensor-code parity-check matrix H ∈ FS×Nq associated to H1, . . . , H u is given for x ∈ FNq by 

Hx = (( I⊗i−1 ⊗ Hi ⊗ I⊗u−i)x)i∈[u] ∈ M

> i∈[u]

FSi 

> q

= FSq .

This matrix is by definition a parity-check matrix for the tensor product of the codes ker( Hi) for 

i ∈ [u], that is, 

ker( H) = O

> i∈[u]

ker( Hi).

13 We will use the fact shown by [Vid15] (which strengthened a result of [BSS04]) that tensor product codes are locally testable . Loosely speaking, this result says that for every y ∈ F 

> Q
> i∈[u][ni]
> q

,the number of direction-i columns in which the restriction of y is not a codeword of Ci grows with the distance min c∈C |y − c| from y to C. In other words, defining H as in Definition 2.4, then |Hy |

grows with min c∈C |y − c|. We provide the formal statement in Lemma 2.5 below. 

Lemma 2.5 (Local testability of tensor product codes [Vid15]) . For u, n, d ∈ N, for i ∈ [u] let 

Ci = ker( Hi) be an [n, k i, d i ≥ d]q code with full-rank parity-check matrix Hi ∈ F(n−ki)×nq . Let 

H ∈ FS×[N ] 

> q

be the associated tensor-code parity-check matrix, and let C = ker( H) be the tensor code. Then it holds for every y ∈ FNq that |Hy | ≥ ρ(u, n, d ) · min c∈C |y − c|, where 

ρ(u, n, d ) = 1(un · (n/d )u)η

for an absolute constant η > 1.

The quantity ρ(u, n, d ) in Lemma 2.5 is called the soundness of the tensor code. 

Remark 2.6 . We have stated Lemma 2.5 slightly differently from [Vid15, Theorem 3.1], but our statement still follows directly from [Vid15]. Specifically: 1. [Vid15, Theorem 3.1] is stated assuming all codes Ci are equal, though as noted in [Vid15, Remark 4.5], the techniques extend flawlessly to different Ci.2. The tester in [Vid15, Theorem 3.1] actually checks if any of the n − ki entires of Hy in a given direction-i column are nonzero, meaning that |Hy | in Lemma 2.5 would be replaced by the number of such direction-i columns with a nonzero entry. Yet this quantity is always within a factor of n of |Hy |, so we can absorb the factor of n into the nη = poly( n) term in μ(u, n, d ). 3. When u is not a power of 3, the local tester in [Vid15] samples different syndrome components with different probabilities, meaning that it may seem that we should add different weights for different components when computing the Hamming norm |Hy | in Lemma 2.5. However, the result of [Vid15] naturally holds under all different permutations of the codes C1, . . . , C u, and all permutations of the n columns and the n − ki rows in each Hi. Averaging over all of these permutations, we conclude that the result holds with uniform weights on the components of the syndrome Hy .

2.2.2 Reed-Solomon Codes 

Here we define Reed-Solomon codes. 

Definition 2.7. For a finite field Fq, we let Fq[X] denote the space of univariate polynomials over 

Fq. For k ∈ N, we let Fq[X]<k ⊆ Fq[X] denote the subspace of polynomials of degree < k . For 

E ⊆ Fq, We let ev E : Fq[X] → FEq denote the evaluation map, that is, ev E (f ) = ( f (x)) x∈E .The Reed-Solomon code RS( q, k, E ) ⊆ FEq is the code RS( q, k, E ) = ev E (Fq[X]<k ). This code has parameters [n = |E|, k, d = n − k + 1] q.

We will crucially leverage the multiplication property of Reed-Solomon codes, stated below, which simply reflects the fact that the product of two degree < k polynomials is a degree < 2k − 1polynomial. Here recall from Section 2.1 that we use ‘ ∗’ to denote component-wise multiplication of vectors. 

Fact 2.8. RS( q, k, E ) ∗ RS( q, k, E ) ⊆ RS( q, 2k − 1, E ).

14 2.3 Gates and Circuits 

In this section, we describe the definitions of circuits and fault-tolerance that we use in this paper. As we work over general prime power alphabets Fq instead of just binary alphabets, we use the term “ q-ary dit” to refer to a symbol over such an alphabet, or simply “dit” if the alphabet size q

is clear from context. 

Definition 2.9. Fix a prime power q. A gate acting on q-ary dits with min input dits and mout 

output dits is a function G : Fmin  

> q

→ Fmout  

> q

.

We now present the gate set that we will use. 

Definition 2.10. For a prime power q, we define the following q-ary gates: 

• The initialization gate Init : {0} → Fq given by 

Init(0) = 0 .

• The termination gate Term : Fq → { 0} given by 

Term( x) = 0 .

• For a ∈ Fq, the gate Xa : Fq → Fq given by 

Xa(x) = x + a. 

• For a ∈ Fq, the gate CX a : F2 

> q

→ F2 

> q

given by 

CX a(x1, x 2) = ( x1, ax 1 + x2).

• For a ∈ Fq, the gate CCX a : F3 

> q

→ F3 

> q

given by 

CCX a(x1, x 2, x 3) = ( x1, x 2, ax 1x2 + x3).

For the gates above, we sometimes replace the subscript with ‘ ∗’ to denote the set of all possible values. For instance, {Init , CCX ∗} = {Init } ∪ { CCX a : a ∈ Fq}.

Note that the identity gate is given by X 0. Also, the gate CX a can be implemented using the gates Init , X1, CCX a. We nevertheless include CX a in our gate set for convenience. We next define our notion of a circuit. Below, recall that a directed acyclic graph (dag) is a directed graph with no cycles. Also recall from Section 2.1 that we use ‘ ⊔’ to denote direct products of functions. 

Definition 2.11. A circuit R = ( R1, . . . , R T ) acting on a set N of q-ary dits is given by a sequence 

R1, . . . , R T of functions Rt : FNt−1 

> q

→ FNt 

> q

for some subsets N0, . . . , N T ⊆ N . We call Nt the active dits at time t, and we call N0 and NT the sets of input and output dits , respectively. We let 

R(·) : FN0 

> q

→ FNT 

> q

denote the function 

R(x) = RT ◦ · · · ◦ R1(x).

15 We define the transcript tran( R; x) ∈ FN0⊔···⊔ NT 

> q

of R on input x by 

tran( R; x) = ( x, R 1(x), R 2 ◦ R1(x), . . . , R T ◦ · · · ◦ R1(x)) .

We say R uses time T and space N (or sometimes, space |N |). Note that each q-ary dit implicitly requires log 2 q bits to represent; q will be clear from context when discussing space usage. We say R uses a gate set G if each Rt can be decomposed as a direct product of gates Gt,i ∈ G 

acting on disjoint sets of q-ary dits. More formally, R uses gate set G if for each t ∈ [T ], there exists mt ∈ N and gates Gt, 1, . . . , G t,m t ∈ G such that Rt equals the direct product F 

> i∈[mt]

Gt,i for some assignment of the dits in Nt−1 (resp. Nt) to the set of all input (resp. output) dits of Gt,i .Furthermore, letting min and mout denote the number of input and output dits of Gt,i , respectively, then we require that the dits in Nt−1 ⊆ N assigned to the first min {min , m out } inputs of Gt,i equal the dits in Nt ⊆ N assigned to the first min {min , m out } outputs of Gt,i .The size of a circuit R using gate set G is the number of input dits |N0| plus the total number of non-identity gates Gt,i in the circuit, across all t ∈ [T ] and i ∈ [mt].

We will typically consider circuits using gate set G = {Init , Term , X∗, CX ∗, CCX ∗}. This gate set is universal because it can perform addition and multiplication, and every function over Fq can be expressed as a sufficiently high-degree polynomial. Definition 2.11 captures the standard notion of a circuit with distinct timesteps, such that every dit is acted upon by some gate in every timestep. Dits can also be created and terminated, if the sets Nt are not all the same. Perhaps the least standard part of Definition 2.11 is the requirement that for each Gt,i , the first min {min , m out } input and output dits are equal. Intuitively, this condition says that we do not allow “free” permutations of the dits between timesteps; gates must output the same dits in N

they were fed as inputs, up to creation/termination of dits. Because a pair of dits labeled by 1 , 2can be swapped using an ancilla dit labeled 3 with the circuit (a, b ) Init 3

−−−→ (a, b, 0) CX +1 1,3

−−−−→ (a, b, a ) CX −13,1

−−−−→ (0 , b, a ) CX +1 2,1

−−−−→ (b, b, a )

> CX −11,2

−−−−→ (b, 0, a ) CX +1 3,2

−−−−→ (b, a, a ) CX −12,3

−−−−→ (b, a, 0) Term 3

−−−−→ (b, a ),

circuits that allow {Init , CX ∗, Term } gates can perform arbitrary permutations with constant space and time overhead. Therefore our prohibition of “free” permutations of dits only affects the time and space usage of the circuits we consider by a constant factor. We nevertheless choose to prohibit free permutations for consistency with a standard model of circuits common in the quantum literature, in which gates act on a fixed set N of (qu)dits, and each gate has the same input and output (qu)dits. The following basic method for applying linear transformations using CX ∗ gate will be useful. 

Definition 2.12. For a matrix A ∈ Fm×nq , we define an associated circuit RA as follows. RA acts on dits N = [ m] ⊔ [n], uses time T = mn , and uses gate set {CX ∗}. The input and output dits are all of N . We define RA to sequentially loop through every (i, j ) ∈ [m] × [n], and for each such pair apply CX Ai,j with control dit j ∈ [n] and target dit i ∈ [m]. Therefore on input (x, y ) ∈ F[n]⊔[m] 

> q

,then RA outputs RA(x, y ) = ( x, y + Ax ).

16 2.4 Fault-Tolerance and Fault-Detection 

In this section, we define the notions of fault-tolerance and fault-detection we will use in this paper. The definitions here are inspired by those for quantum circuits in [NP25, HNP25], though of course we only consider classical circuits. We begin by defining faults, which capture the errors against which we will want to protect. 

Definition 2.13. Let R = ( R1, . . . , R T ) be a circuit with each Rt : FNt−1 

> q

→ FNt 

> q

. A fault on the circuit R is a sequence F = ( F1, . . . , F T ) of functions Ft : FNt 

> q

→ FNt 

> q

. Letting supp( Ft) ⊆ Nt

denote the set of dits in Nt on which Ft does not act as the identity, we define supp( F) ⊆ N ⊔T by 

supp( F) = supp( F1) ⊔ · · · ⊔ supp( FT ).The F-corrupted circuit R[F] is defined by 

R[F] = ( R1, F 1, R 2, F 2, . . . , R T , F T ).

In a slight abuse of notation, we define the transcript tran( R[F]; x) ∈ FN0⊔···⊔ NT 

> q

of an F-corrupted circuit R[F] on input x to exclude every other timestep corresponding to a fault layer, so that 

tran( R[F]; x) = ( x, F 1 ◦ R1(x), F 2 ◦ R2 ◦ F1 ◦ R1(x), . . . , F T ◦ RT ◦ · · · ◦ F1 ◦ R1(x)) .

We next define the notion of a bad set, which is a set of dits that, if entirely corrupted, will overwhelm our fault-tolerance scheme. 

Definition 2.14. For a set N and a family of subsets E ⊆ 2N called bad sets , we say a set S ⊆ N

is E-avoiding if for every E ∈ E we have E̸ ⊆ S.We extend this definition to faults in the natural way. That is, given a family of bad sets 

E ⊆ 2N ⊔T

= 2 N ×T for a circuit using space N and time T , a fault F is E-avoiding if supp( F) is 

E-avoiding. 

We will often construct families of bad sets for a circuit using space N and time T by specifying a family E ⊆ N of bad sets for a single timestep, and then letting E⊔T ⊆ N ⊔T be our family of bad sets for the circuit. We in turn typically choose E to contain all sets of size above some given threshold, as defined below. 

Definition 2.15. For a set N , a family E ⊆ 2N , and a real number λ, we define 

E| ≥λ = {E ∈ E : |E| ≥ λ}.

In particular, 2N |≥λ denotes the set of all subsets of N of size ≥ λ.

It will also be helpful to associate families of bad sets to codes, to represent errors on codewords that we cannot detect and/or correct. 

Definition 2.16. A decorated code D = ( C, Enc , E) consists of a linear code C with encoding map 

Enc and a family E ⊆ 2[n] of bad sets, where n denotes the length of C. We let ∅ denote the trivial decorated code with n = 0 .For a set S ⊆ C of codewords in C (where S may not be a linear subspace), we say y ∈ Fnq is a 

E-deviation of S if there exists c ∈ S for which supp( y − c) is E-avoiding. 

We are now ready to define our notion of a fault-tolerant gadget. 17 Definition 2.17. Let R = ( R1, . . . , R T ) be a circuit using space N and time T , with input and output dits Nin and Nout respectively. Let Erun ⊆ 2N ⊔T

be a family of bad sets. For α ∈ { in , out },let Dα = ( Cα, Enc α, Eα) be a [nα, k α]q decorated code with codeword dits labeled by Nα, so that 

nα = |Nα| and Eα ⊆ 2Nα .We say the data 

(R, Erun , D in , D out )

forms a fault-tolerant gadget for a set ¯O of functions ¯O : Fkin  

> q

→ Fkout  

> q

if the following hold: 1. For every x ∈ Fkin  

> q

, it holds that R◦ Enc in (x) ∈ Enc out ◦ ¯O(x) (i.e. R◦ Enc in (x) = Enc out ◦ ¯O(x)

for some ¯O ∈ ¯O). 2. For every x ∈ Fkin  

> q

, every Ein -deviation y of Enc in (x), and every Erun -avoiding fault F for R,then the output R[F]( y) is a Eout -deviation of Enc out ◦ ¯O(x).

In Definition 2.17, Item 1 requires that a noiseless execution of R outputs a noisless encoding of the input message, while Item 2 requires that a noisy execution of R outputs a noisy encoding of the input message. The first condition (Item 1) is not necessary for fault-tolerance in general, but will be useful to construct PCPs with perfect completeness. Depending on the choice of Ein , Erun , Eout , we typically design a fault-tolerant gadget to either limit the propagation of errors, actively correct errors, or both. For our application to PCPs, the notion of fault-tolerance in Definition 2.17 is stronger than necessary. Specifically, in the PCP setting, it is ok to fail to output (a deviation of) the true output, if we can detect the failure. That is, it is sufficient to detect errors, which can be easier than correcting them. We therefore define the following notion of a fault-detecting gadget. 

Definition 2.18. Let R = ( R1, . . . , R T ) be a circuit using space N and time T , with active dits 

Nin = N0, N 1 . . . , N T = Nout . Let Erun , Edet ⊆ 2N ⊔T

be families of sets. For α ∈ { in , out },let Dα = ( Cα, Enc α, Eα) be a [nα, k α]q decorated code with codeword dits labeled by Nα, so that 

nα = |Nα| and Eα ⊆ 2Nα .We say the data 

(R, Erun , D in , D out , Edet )

forms a fault-detecting gadget for a set ¯O of functions ¯O : Fkin  

> q

→ Fkout  

> q

if the following hold: 1. For every x ∈ Fkin  

> q

and every E ∈ E det , it holds that R ◦ Enc in (x) ∈ Enc out ◦ ¯O(x) and 

E ∩ supp(tran( R; Enc in (x))) = ∅.2. For every x ∈ Fkin  

> q

, every Ein -deviation y of Enc in (x), and every Erun -avoiding fault F for R,then at least one of the following holds: (a) The output R[F]( y) is a Eout -deviation of Enc out ◦ ¯O(x), or (b) There exists a set E ∈ E det such that E ⊆ supp(tran( R[F]; y)) , where here we view 

E ⊆ N ⊔T ∼= N × { 1, . . . , T } as a subset of N ⊔T +1 ∼= N × { 0, 1, . . . , T }.We say the gadget is furthermore mending if for every y ∈ FNin  

> q

and every Erun -avoiding fault 

F for R, then at least one of the following holds: (c) There exists x′ ∈ Fkin  

> q

such that the output R[F]( y) is a Eout -deviation of Enc out ◦ ¯O(x′), or 

18 (d) There exists a set E ∈ E det such that E ⊆ supp(tran( R[F]; y)) .

The union S Edet of all sets in the family Edet ⊆ 2N ⊔T

in Definition 2.18 provides a set of dits in the space-time execution (or equivalently, in the transcript) of a circuit R that detect errors. Specifically, Item 1 in Definition 2.18 says that in the absence of errors, all of these dits must be 0. Meanwhile, Item 2 in Definition 2.18 says that in the presence of a Ein -avoiding input error and a 

Erun -avoiding fault, then either the output error is Eout -avoiding, or else the set of nonzero dits in S Edet is sufficiently large to contain some E ∈ E det .We will often fix some set Edet ⊆ N ⊔T and of “detector dits” and then choose Edet = 2 Edet |≥λdet 

to be the family of subsets of Edet of size at least some threshold λdet . Then the criterion for detecting an error (given in Item 2b in Definition 2.18 above) is simply that ≥ λdet of the detector dits in Edet are nonzero. By definition, a fault-tolerant gadget is equivalent to a fault-detecting gadget with Edet = ∅:

Fact 2.19. The data (R, Erun , D in , D out ) forms a fault-tolerant gadget for ¯O if and only if the data 

(R, Erun , D in , D out , ∅) forms a fault-detecting gadget for ¯O.

We nevertheless retain the language “fault-tolerant” to describe this special case for consistency with the prior literature. The mending property in Definition 2.18 requires that even when the input error is arbitrarily large, the gadget either outputs a Eout -deviation of an encoding of some output message in ¯O(x′)(for an arbitrary input x′), or else detects an error. An analogue of the mending property in the quantum setting is used in [HNP25, NP25] (where it is called the friendly property) to allow gadgets to be composed in a simulative manner. That is, the error rate of the (qu)dits in a fault-tolerance scheme can be reduced by recursively simulating each (qu)dit with another fault-tolerance scheme. If one of these recursive simulations experiences a high-weight error, a mending gadget can return the corrupted state to a valid code state, so that the recursive simulation of that (qu)dit can resume. While we will not need to perform such simulative composition, we will use the mending property for a different purpose in the PCP setting. Specifically, we assume a PCP proof is provided in encoded form, to which we apply a fault-detecting gadget. If the provided encoding has a high-weight error, the mending property ensures that either the error is detected, or else the gadget’s output is close to the true output for some valid input x′. In contrast, without the mending property, we would have no control over our gadget’s output under high-weight input errors. Below, we show that in order to prove (mending) fault-detection, without loss of generality we may restrict attention to additive faults, which simply add some fixed error into the circuit’s dits at every timestep. This basic result holds because we allow the fault to be chosen after the circuit’s input, so the outputs of an arbitrary fault can be simulated by an appropriately chosen additive fault. 

Definition 2.20. For a circuit R, a fault F = ( F1, . . . , F T ) is additive if every Ft : FNt 

> q

→ FNt 

> q

is of the form Ft(y) = y + ft for some ft ∈ FNt 

> q

.

Note that an additive fault F acts independently on each dit, i.e. each Ft is a direct product of the single-dit functions yi 7 → yi + ( ft)i for i ∈ Nt.

Lemma 2.21. Let (R, Erun , D in , D out , Edet ) and ¯O be some data that satisfy the conditions of Def-inition 2.18 upon restricting attention to only additive Erun -avoiding faults F. Then in fact this 

19 data satisfies Definition 2.18 for general Erun -avoiding faults F, that is, this data forms a (mending) fault-detecting gadget. Proof. We define the variables Cα, Enc α, Eα, n α, k α, N α as in Definition 2.18. We first prove the claim for non-mending fault-detecting gadgets. Because Item 1 in Definition 2.18 does not depend on a choice of fault, it holds immediately by assumption from the lemma statement. We next show that Item 2 holds for general faults F, assuming it holds for additive faults F′.Let x ∈ Fkin  

> q

, let y be a Ein -deviation of Enc in , and let F = ( F1, . . . , F t) be a general Erun -avoiding fault for R. We then construct an additive fault F′ = ( F ′

> 1

, . . . , F ′ 

> T

) for R by letting F ′ 

> t

(z) = z + f ′

> t

for 

f ′ 

> t

= tran( R[F]; y)t − Rt ◦ Ft−1 ◦ Rt−1 · · · ◦ F1 ◦ R1(y),

where tran( R[F]; y)t ∈ FNt 

> q

denotes the restriction of the transcript to the tth timestep. By definition, every dit j ∈ Nt \ supp( Ft) must have ( f ′ 

> t

)j = 0, so supp( F′) ⊆ supp( F), and hence F′

is Erun -avoiding. This construction also ensures that tran( R[F]; y) = tran( R[F′]; y). Thus because Item 2 in Definition 2.18 holds for F′, Item 2 must also hold for F, as Item 2 by definition only depends on values of x, y, and the transcript tran( R[F]; y) = tran( R[F′]; y) (because R[F]( y) = tran( R[F]; y)T ). Thus ( R, Erun , D in , D out , Edet ) is a fault-detecting gadget for ¯O, as desired. The proof for mending gadgets is analogous as above; we omit the details to avoid redundancy. While Lemma 2.21 shows that we can always restrict attention to additive faults in this paper, one may need to consider more general faults in other settings, such as when the fault-detecting circuit is allowed to be randomized. 

2.5 Gadget Composition 

This section describes how fault-detection (and as a special case, fault-tolerance) is preserved under sequential and parallel composition of gadgets, defined below. Similar results are shown in the quantum setting in [NP25, HNP25]; we simply adapt these results to our setting. 

2.5.1 Sequential Composition 

Here we consider sequential composition, in which two gadgets are run one after another on a shared set of dits. 

Lemma 2.22. For a set N , for i ∈ [2] let 

(Ri = ( Ri

> 1

, . . . , R iTi ), Ei

> run

, D i

> in

, D i

> out

, Ei

> det

)

be a fault-detecting gadget for ¯Oi using space Ni ⊆ N and time Ti. Assume that the set of output dits of R1 equals the set of input dits of R2, and that D1out = D2in .Define the sequential composition of R1, R2 to be the circuit acting on dits N given by 

R2 ◦ R 1 = ( R11, . . . , R 1 

> T1

, R 21, . . . , R 2 

> T2

).

Then  R = R2 ◦ R 1, Erun = E1run ⊔ E 2run , D 1in , D 2out , Edet = E1det ⊔ E 2det 

 (1) 20 is a fault-detecting gadget for ¯O = ¯O2 ◦ ¯O1 using space N (so |N | ≥ max {| N1|, |N2|} ) and time 

T = T1 + T2.Furthermore, if (R1, E1run , D 1in , D 1out , E1det ) is mending, then the sequential composition gadget in Equation (1) is mending. Proof. We begin with the claim regarding non-mending fault-detection. By definition R uses space 

N and time T = T1+T2, so it suffices to show that the sequential composition gadget in Equation (1) is fault-detecting for ¯O. For this purpose, let each Diα = ( Ciα, Enc iα, Eiα) be a [ niα, k iα]q code. The condition in Item 1 in Definition 2.18 for the sequential composition follows directly by applying the same condition for ( Ri, Ei

> run

, D i

> in

, D i

> out

, Ei

> det

) along with the fact that D1out = D2in , that is, for every x ∈ Fk1in  

> q

,

R2 ◦ R 1 ◦ Enc 1in (x) ∈ R 2 ◦ Enc 1out ◦ ¯O1(x) ⊆ Enc 2out ◦ ¯O2 ◦ ¯O1(x).

To show the condition in Item 2 in Definition 2.18 for the sequential composition, let x ∈ Fk1in  

> q

,let y be a E1in -deviation of Enc 1in (x), and let F = ( F1, F2) be a Erun -avoiding fault for R, so that each Fi is a Ei

> run

-avoiding fault for Ri. Applying the fault-detection of ( R1, E1run , D 1in , D 1out , E1det ), we have that either R1[F1]( y) is a E1out -deviation of Enc 1out ◦ ¯O1(x), or else there exists some E ∈E1det such that E ⊆ supp(tran( R1[F1]; y)). In the latter case, because E1det ⊆ E det , then Item 2b in Definition 2.18 holds for the sequential composition gadget, so we are done. In the former case, we can then apply the fault-detection of ( R2, E2run , D 2in , D 2out , E2det ) to conclude that either 

R2[F2]◦R 1[F1]( y) = R[F]( y) is a E2out -deviation of Enc 2out ◦ ¯O(x), or else there exists some E ∈ E 2det 

such that E ⊆ supp(tran( R2[F2]; R1[F1]( y))). Again in the latter case, we are done because 

E2det ⊆ E det . Now in the former case, then Item 2a in Definition 2.18 holds for the sequential composition gadget, so we are also done. The proof of the claim regarding mending fault-detection is similar to the non-mending case above. The difference is that now the input y may be an arbitrary element of Fn1in  

> q

, and then either R1[F1]( y) is a E1out -deviation of Enc 1in ◦ ¯O1(x′) for some x′ ∈ Fk1in  

> q

, or else there exists some E ∈ E1det such that E ⊆ supp(tran( R1[F1]; y)). Then applying the fault-detection of (R2, E2run , D 2in , D 2out , E2det ) as above, we conclude that the sequential composition gadget is mend-ing. 

2.5.2 Parallel Composition 

Here we consider parallel composition, in which two gadgets are run at the same time on disjoint sets of dits. We will use the following notation for (decorated) codes consisting of the disjoint union of smaller (decorated) codes. 

Definition 2.23. For i ∈ [2] , let codes Di = ( Ci, Enc i, Ei) be a [ni, k i, d i] decorated code. We define the disjoint union of C1, C 2 to be the [n1 + n2, k 1 + k2, min {d1, d 2}]q code 

C1 ⊔ C2 = {(c1, c 2) : c1 ∈ C1, c 2 ∈ C2},

and then we define the disjoint union of D1, D 2 by 

D1 ⊔ D2 = ( C1 ⊔ C2, Enc 1 ⊔ Enc 2, E1 ⊔ E 2).

21 Lemma 2.24. For T ∈ N and for i ∈ [2] let 

(Ri = ( Ri

> 1

, . . . , R iTi ), Ei

> run

, D i

> in

, D i

> out

, Ei

> det

)

be a fault-detecting gadget for ¯Oi using space Ni and time T .Define the parallel composition of R1, R2 to be the circuit acting on dits N = N1 ⊔ N2 given by 

R1 ⊔ R 2 = ( R11 ⊔ R21, . . . , R 1 

> T

⊔ R2 

> T

).

Then 

 R = R2 ⊔ R 1, Erun = E1run ⊔ E 2run , D in = D1in ⊔ D2in , D out = D1out ⊔ D2out , Edet = E1det ⊔ E 2det 

 (2) 

is a fault-detecting gadget for ¯O = ¯O1 ⊔ ¯O1 using space N (so |N | = |N1| + |N2|) and time T .Furthermore, if for every i ∈ [2] the gadget (Ri, Ei

> run

, D i

> in

, D i

> out

, Ei

> det

) is mending, then the parallel composition in Equation (2) is mending. Proof. We begin with the claim regarding non-mending fault-detection. By definition R uses space N = N1 ⊔ N2 and time T , so it suffices to show that the parallel composition gadget in Equation (2) is fault-detecting for ¯O. For this purpose, let each Diα be a [ niα, k iα]q code, so that Dα

is a [ nα = n1 

> α

+ n2

> α

, k α = k1 

> α

+ k2

> α

]q code. The condition in Item 1 in Definition 2.18 for the parallel composition follows directly by applying the same condition for ( Ri, Ei

> run

, D i

> in

, D i

> out

, Ei

> det

), that is, for every x = ( x1, x 2) ∈ Fkin  

> q

=

Fk1in +k2in  

> q

,(R1 ⊔ R 2) ◦ (Enc 1in ⊔ Enc 2in )( x1, x 2) ∈ (Enc 1out ⊔ Enc 2out ) ◦ ( ¯O1 ⊔ ¯O2)( x1, x 2).

To show the condition in Item 2 in Definition 2.18 for the parallel composition, let x = ( x1, x 2) ∈

Fkin  

> q

= Fk1in +k2in  

> q

, let y = ( y1, y 2) be a E1in ⊔ E 2in -deviation of of Enc in (x), and let F = ( F1, . . . , F T )be a Erun -avoiding additive fault for R. As F is additive, we can write F = F1 ⊔ F 2, meaning that each Ft = F 1 

> t

⊔ F tt , where each Fi is some Ei

> run

-avoiding fault for Ri. Applying the fault-detection of ( Ri, Ei

> run

, D i

> in

, D i

> out

, Ei

> det

), we have that either R[F]( y) = ( R1[F1]( y1), R2[F2]( y2)) is a 

Eout -deviation of Enc out ◦ ¯O(x), or there exists some E ∈ E 1det such that E ⊆ supp(tran( R1[F1]; y1)), or there exists some E ∈ E 2det such that E ⊆ supp(tran( R2[F2]; y2)). The latter two cases together are equivalent to the statement that there exists some E ∈ E det = E1det ⊔ E 2det such that E ⊆

supp(tran( R[F]; y)), so we conclude that either Item 2a or Item 2b in Definition 2.18 holds, as desired. Now assume that for both i ∈ [2] the gadget ( Ri, Ei

> run

, D i

> in

, D i

> out

, Ei

> det

) is mending. Let y =(y1, y 2) ∈ Fn1in +n2in  

> q

be an arbitrary vector, and let F be an additive Erun -avoiding fault. Because 

F is additive, we can decompose each Ft = F 1 

> t

⊔ F 2 

> t

where each F it is an additive function acting on the dits of Ri at timestep t. Then by definition Fi = ( Fi

> 1

, . . . , FiT ) is an additive Ei

> run

-avoiding fault for Ri. Thus for i ∈ [2], either Ri[Fi]( yi) is a Ei

> out

-deviation of ¯Oi(x′

> i

) for some x′ 

> i

∈ Fki 

> out
> q

,or else there exists E ∈ E i 

> det

with E ⊆ supp(tran( Ri[Fi]; yi)). By the definition of R, Eout , ¯O, Edet ,it follows that either R[F]( y) = ( R1[F1]( y1), R2[F2]( y2)) is a Eout -deviation of ¯O(x′

> 1

, x ′

> 2

), or else there exists E ∈ E det with E ⊆ supp(tran( R[F]; y)). Thus by Lemma 2.21, the parallel composition gadget in Equation (2) is mending, as desired. 22 3 Fault-Tolerant and Fault-Detecting Gadgets For Tensor Codes 

In this section, we present our fault-tolerant and fault-detecting gadgets for tensor codes. We will subsequently show how to compose these gadgets to compile an arbitrary logical circuit into a fault-tolerant or fault-detecting circuit. 

3.1 Error-Detection Gadget 

Here present an error-detection gadget for tensor codes. Our gadget simply applies the parity-check matrix to the corrupted codeword, and declares an error has been detected if the resulting syndrome has sufficiently high weight. Lemma 2.5 implies that this procedure successfully detects high-weight errors. In fact, such a procedure applies for arbitrary locally testable codes, though we only describe the tensor code setting that is relevant for our purposes. 

Lemma 3.1. For u, n, d ∈ N, for i ∈ [u] let Ci be a [ni = n, k i, d i ≥ d]q code with encoding map 

Enc i. Let ρ(u, n, d ) be the soundness parameter defined in Lemma 2.5. Let C = N 

> i∈[u]

Ci be the tensor code with encoding map Enc = N 

> i∈[u]

Enc i. Let N = Q

> i∈[u]

[ni] = [ n]u, K = Q

> i∈[u]

[ki], and 

D = du, so that C is a [|N |, |K|, ≥ D]q code. Let λin , λ run , λ out , λ det > 0 satisfy 

λin ≤ D/ 2

λout ≥ 8u2n4

ρ(u, n, d ) · λrun 

λdet = ρ(u, n, d )8 · λout .

(3) 

For α ∈ { in , out }, define the decorated code Dα = ( C, Enc , Eα = 2 N |≥λα ). Then there exists a circuit R using gate set {Init , Term , CX ∗}, space |N ′| ≤ (u + 1) nu, and time T ≤ un 2 + 2 along with a set Edet ⊆ N ′ such that 

(R, Erun = 2 N ′

|⊔T 

> ≥λrun

, D in , D out , Edet = 2 Edet ×{ T −1}|≥λdet ) (4) 

forms a mending fault-detecting gadget for the |K|-dit identity function ¯O = IK : FKq → FKq . Above, 

Edet × { T − 1} denotes the subset of N ′ × [T ] ∼= N ′⊔ T given by a single copy of the subset Edet ⊆ N ′

within the (T − 1) st copy of N ′.

When applying Lemma 3.1, it will often be useful to set λout equal to its minimum allowed value λout = (8 u2n4/ρ (u, n, d )) · λrun . The advantage of choosing a larger value of λout is that λdet 

becomes larger, meaning that more of the “detector dits” in Edet are nonzero when an error is detected. 

Proof of Lemma 3.1. We will first describe the circuit R. For this purpose, for i ∈ [u], fix a full-rank parity-check matrix Hi ∈ F(ni−ki)×ki 

> q

for Ci. Define the set S = F 

> i∈[u]

Si and the tensor-code parity-check matrix H ∈ FS×Nq as in Definition 2.4. On input y ∈ FNq , the desired circuit R first applies Init ⊔S to initialize a set of ancilla dits labeled by the set S. Next, R applies the linear map ( y, 0S ) 7 → (y, Hy ) by sequentially looping through i = 1 , . . . , u , and for each i using CX ∗ gates to apply the map (y, 0Si

) 7 → (y, (I⊗i−1 ⊗ Hi ⊗ I⊗u−i)y) (5) 23 to dits N ⊔ Si ⊆ N ⊔ S. Specifically, to apply this map in Equation (5), R simply applies the circuit RHi from Definition 2.12 in parallel to each direction-i column of Si and N , which takes time ( ni − ki)ni ≤ n2 

> i

= n2. Finally, R applies Term ⊔S to terminate all dits in S.Thus R uses 1 timestep for the Init gates, at most u · n2 timesteps to apply the linear map (y, 0S ) 7 → (y, Hy ), and one timestep for the Term gates, for a total of T ≤ un 2 + 2 timesteps. Meanwhile, R uses space N ′ = N ⊔ S, so |N ′| ≤ nu + un u ≤ (u + 1) nu.We define the set Edet ⊆ N ′⊔ T = N ′ × [T ] to contain all dits in S at timestep T − 1, that is, 

Edet = S × { T − 1}.We will now prove that the data in Equation (10) forms a mending error-detecting gadget for ¯O = IK . First, for every input y ∈ C so that Hy = 0, then in the absence of errors, following timestep T − 1 the dits in N ′ = N ⊔ S have value ( y, Hy ) = ( y, 0S ). Thus the restriction of tran( R; y) to Edet = S × { T − 1} equals 0 S , so Item 1 in Definition 2.18 is satisfied. To show Item 2 as well as the mending property in Definition 2.18, let x ∈ Fkin  

> q

, let y ∈ FNq ,and let F be an additive Erun -avoiding fault for R. Let x′ = arg min x′′ ∈FKq |y − Enc( x′′ )| and let 

f0 = y − Enc( x′). To prove Item 2 in Definition 2.18, we will assume y is a Ein -deviation of Enc( x), meaning that |y − Enc( x)| < λ in ≤ D/ 2 (see Equation (3)), and therefore x′ = x. To prove the mending property, we instead assume y is arbitrary, and we may have x′̸ = x.For t ∈ { 0, . . . , T }, let N ′ 

> t

⊆ N ′ be the active dits at time t. For t ∈ [T ], define ft ∈ FN ′

> t
> q

to be the vector such that Ft(z) = z + ft. By assumption, for t ∈ [T ] we have |ft| < λ run . By construction, a fault-free execution of R (where ft = 0 for every t ∈ [T ]) has tran( R; y)|Edet =

Hy = Hf 0. Meanwhile, in an execution of R with the fault F, because every dit is involved in P 

> i∈[u]

ni(ni − ki) < un 2 CX ∗ gates, every fault error (corresponding to a nonzero component of 

ft for some t ∈ [T ]) can propagate to at most un 2 − 1 other dits. That is, in timestep T − 1, the difference of the transcript tran( R[F]; y)|Edet and the fault-free transcript tran( R; y)|Edet = Hf 0

has weight 

| tran( R[F]; y)|Edet − Hf 0| ≤ X 

> t∈[T]

|ft| · un 2 < T · λrun · un 2 (6) dits. Also, because dits in N can only be changed by fault errors (as all CX ∗ gates we run use dits in N as controls, not targets), the difference of the output R[F]( y) and the fault-free output 

R(y) = y has weight 

|R [F]( y) − y| < T · λrun . (7) If R[F]( y) is a Eout -deviation of Enc( x′), then Item 2a (if y is a Ein -deviation of Enc( x) = Enc( x′)) or Item (c) (for general y ∈ FNq ) in Definition 2.18 holds. Otherwise, we have |R [F]( y) −

Enc( x′)| ≥ λout , so because y = Enc( x′) + f0, Equation (7) implies that 

|f0| ≥ λout − |R [F]( y) − y| > λ out − T · λrun . (8) By definition Enc( x′) is the closest codeword in C to y = Enc( x′) + f0, and therefore 0 is the closest 24 codeword in C to f0, i.e. |f0 − C| = |f0|. Therefore 

| tran( R[F]; y)|Edet | > |Hf 0| − T · λrun · un 2

≥ ρ(u, n, d ) · | f0| − T · λrun · un 2

≥ ρ(u, n, d ) · (λout − T · λrun ) − T · λrun · un 2

≥ ρ(u, n, d ) · (λout − (un 2 + 2) · λrun ) − un 2(un 2 + 2) · λrun 

≥ ρ(u, n, d )8 · λout 

= λdet .

where the first inequality above holds by Equation (6), the second inequality holds by Lemma 2.5 and because min c∈C |f0 −c| = |f0| by the definition of f0, the third inequality holds by Equation (8), the fourth inequality holds because T ≤ un 2 + 2, and the fifth and sixth inequalities hold by Equation (3). Thus some E ∈ E det = 2 Edet |≥λdet must lie inside supp(tran( R[F]; y)), so Item 2b (if 

y is a Ein -deviation of Enc( x)) and Item (c) (for general y ∈ FNq ) in Definition 2.18 are satisfied. Therefore by Lemma 2.21, our gadget in Equation (10) is indeed mending fault-detecting for ¯O =

IK .

3.2 Error-Correction Gadget 

In this section, we present a fault-tolerant gadget that corrects errors on tensor codes, rather than simply detecting them as in Section 3.1. Such error-correction is not necessary for the PCP setting, and we are not able to correct as many errors as we can detect in Lemma 3.1. However, the ability to perform error-correction enables us to obtain a complete fault-tolerance scheme for universal classical computation, as opposed to simply a fault-detecting scheme. Such a fault-tolerance scheme protecting against a large number of adversarial errors may be of independent interest. We begin with the following notion of a decoder, which captures the usual notion in the coding theory literature. 

Definition 3.2. A radius-λ decoder for a [n, k, d ]q code C with encoding map Enc is a fault-tolerant gadget 

(R, Erun = 2 N ′

, D in = ( C, Enc , 2[n]|≥λ), D out = ( C, Enc , 2[n])) 

for the k-dit identity function ¯O = Ik, acting on some set of dits N ′.

In words, a radius-λ decoder for a code C is a circuit that takes as input any y ∈ Fnq that differs from a codeword c ∈ C in < λ dits, and outputs c. Such a decoder can only exist when there exists a unique such codeword c ∈ C, meaning we must have λ − 1 < d/ 2. Because Erun = 2 N ′

in Definition 3.2, the decoder does not need to output a meaningful answer in the presence of a fault. Below, we state the well-known result that there exist polynomial-time decoders for Reed-Solomon codes (see Section 2.2.2) with decoding radius equal to half the distance. While there is a large literature showing stronger results, such as with more efficient algorithms or more robust decoding properties, we intentionally state the basic result that will be sufficient for our purposes. 

Lemma 3.3 (Welch-Berlekamp decoder; see e.g. Chapter 17 of [GRS22]) . For a prime power q, a subset E ⊆ Fq, and a positive integer k ≤ n, let C = RS( q, k, E ) be the [n = |E|, k, d = n − k + 1] q

Reed-Solomon code. Then there exists a radius-d/ 2 decoder for C using space |N ′| = O(n2 log q),time T = O(n2 log q), and gate set G = {Init , Term , X∗, CX ∗, CCX ∗} over q-ary dits. 

25 Remark 3.4 . The space and time bounds in Lemma 3.3 are not optimal, but are sufficient for our purposes. We now briefly justify them. The Welch-Berlekamp algorithm (see e.g. [GRS22, Chapter 17]) simply consists of running Gaussian elimination over Fq a constant number of times, where each system of equations has O(n) variables and O(n) linear constraints. Gaussian elimination on such a system can be performed using O(n2) q-ary dits and O(n2) row/column operations (i.e. additions or scalar multiplications of entire rows or columns of a O(n)×O(n) matrix). Each such row/column operation takes O(n) space and O(1) time in our circuit model. The O(log q) factors in the space and time usage in Lemma 3.3 arise from the algorithm’s control operations, which decide which row/column operations to perform. There are O(n2) such control operations, each of which either adds or multiplies two elements of Fq, or else must map an element a ∈ Fq to 0 if a = 0 and to 1 if 

a̸ = 0. In the latter case, we must simply compute aq−1, which can be implemented using O(log q)space and time via repeated squaring with the gate set G = {Init , Term , X∗, CX ∗, CCX ∗}. Hence the Welch-Berlekamp algorithm can be implemented using O(n2 log q) space and time with gate set 

G = {Init , Term , X∗, CX ∗, CCX ∗} over q-ary dits. We now present our error-correction gadget for tensor codes, assuming the factor codes have decoders in the sense of Definition 3.2. 

Lemma 3.5. For u, n, d ∈ N, for i ∈ [u] let Ci be a [ni = n, k i, d i ≥ d]q code with encoding map 

Enc 2. Assume that each Ci has a radius-λDec ≥ 1 decoder Ri 

> Dec

using space NDec , time TDec , and gate set GDec . Let C = N 

> i∈[u]

Ci be the tensor code with encoding map Enc = N 

> i∈[u]

Enc i. Let 

N = Q

> i∈[u]

[ni] = [ n]u and K = Q

> i∈[u]

[ki], so that C is a [|N |, |K|, ≥ du]q code. For λrun ≥ 0, let 

λin =

 λDec 

2

u

λout =

 2u + 1 

λDec /2n

u

· u2n · TDec · λrun 

(9) 

For α ∈ { in , out }, define the decorated code Dα = ( C, Enc , Eα = 2 N |≥λα ). Then there exists a circuit R using space |N ′| = nu−1 · | NDec |, time T = u · TDec , and gate set GDec such that 

(R, Erun = (2 N ′

|≥λrun )⊔T , D in , D out ) (10) 

forms a fault-tolerant gadget for the |K|-dit identity function ¯O = IK .

To prove Lemma 3.5, we will use the following basic combinatorial notion from [KP25] (though we slighly modify the notation). 

Definition 3.6. For u, n ∈ N and λ > 0, we say a set E ⊆ [n]u is λ-closed if for every i ∈ [u] and every direction-i column L ⊆ [n]u, then either |E ∩ L| < λ or E ∩ L = L (i.e. |E ∩ L| = n). For 

E ⊆ [n]u, we then define the λ-closure [ E]λ ⊆ [n]u to be the minimal λ-closed set that contains E.

Lemma 3.7 ([KP25]) . For every E ⊆ [n]u, it holds that 

|[E]λ| ≤ 

 2u + 1 

λ/n 

u

· | E|.

Proof of Lemma 3.5. The desired circuit R simply loops through i = 1 , . . . , u , and for each such 

i runs the decoder Ri 

> Dec

in parallel on every direction-i column in N . As there are nu−1 such direction-i columns, R uses space |N ′| = nu−1 · | NDec |, and uses time T = u · TDec .26 We now show that the gadget in Equation (10) forms a fault-tolerant gadget for ¯O = IK . For 

c ∈ C, then the restriction to every direction-i column lies in Ci, and Ri 

> Dec

preserves codewords of Ci. Therefore R(c) = c, so Item 1 in Definition 2.17 holds. It remains to prove Item 2 in Definition 2.17. For this purpose, we first introduce the following notation. For λ > 0, for E, F ⊆ N = [ n]u, and for i ∈ { 0, . . . , u }, we define Sλi [F ]( E) ∈ FNq inductively as follows. First, we let Sλ 

> 0

[F ]( E) = E.Then for i ∈ [u], we let Sλi [F ]( E) be the union of all direction-i columns L for which either 

|Sλi−1[F ]( E) ∩ L| ≥ λ or |F ∩ L| > 0. In particular, it follows that Sλi [F ]( E) contains all or none of each direction-i column. We let Sλi (E) = Sλi [∅]( E). Now let x ∈ FKq and let y be a Ein -deviation of Enc( x), so that we can write y = c + e where 

c = Enc( x) and e ∈ FNq has |e| < λ in . Let F = ( F1, . . . , F T ) be a Erun -avoiding fault for the circuit 

R defined above. Our goal is to show that |R [F]( y) − c| < λ out .For i ∈ { 0, . . . , u }, let R≤i·TDec [F]( y) ∈ FNq denote the state of dits in N after running the first 

i·TDec timesteps of R[F]( y), i.e. after running the decoders Ri′ 

> Dec

in the first i directions i′ = 1 , . . . , i 

on input y = c + e. Also define F = S  

> t∈[T]

supp( Ft) ⊆ N to be the set of all dits that lie in the support of the fault F, across all timesteps. Therefore 

|F | ≤ |F| < T · λrun = u · TDec · λrun . (11) In the following claim, we show that SλI [F ](supp( e)) contains the set of errors remaining after these first i rounds of decoding. 

Claim 3.8. For i ∈ { 0, . . . , u }, we have supp( R≤i·TDec [F]( y) − c) ⊆ SλDec  

> i

[F ](supp( e)) .Proof. We prove the claim by induction on i. For the base case, when i = 0 then by definition 

R≤0[F]( y) − c = e and SλDec  

> 0

[F ](supp( e)) = supp( e). For the inductive step, for i ∈ [u], assume the claim holds for i − 1. For a direction-i column L, if the decoder Ri

> Dec

[F] (with F restricted to dits L and timesteps ( i − 1) TDec + 1 , . . . , iT Dec ) applied to dits R≤(i−1) ·TDec [F]( y)|L fails to output 

c|L, then either |(R≤(i−1) ·TDec [F]( y) − c)|L| ≥ λDec , or else F has nonempty support inside L. If 

|(R≤(i−1) ·TDec [F]( y)−c)|L| ≥ λDec , then by the inductive hypothesis |SλDec  

> i−1

[F ](supp( e)) ∩L| ≥ λDec ,and then by the definition of SλDec  

> i

we must have L ⊆ SλDec  

> i

[F ](supp( e)). If F has nonempty support inside L, then |F ∩ L| > 0, so again by the definition of SλDec  

> i

we must have L ⊆ SλDec  

> i

[F ](supp( e)). Thus for every direction-i column L that has nonempty intersection with supp( R≤i·TDec [F]( y) − c), we have L ⊆ SλDec  

> i

[F ](supp( e)), completing the inductive step. Define F ′ ⊆ N = [ n]u to be the union over all i ∈ [u] of all direction-i columns that have a nonempty intersection with F . Because every point in F lies in u direction-i columns, each containing n elements, we have 

|F ′| ≤ un · | F |. (12) The following claim shows that any point lying in Sλi [F ]( E) but not in Sλ/ 2 

> i

(E) = Sλ/ 2 

> i

[∅]( E) must lie in the λ/ 2-closure [ F ′]λ/ 2 (see Definition 3.6) of F ′.

Claim 3.9. For λ > 0, E ⊆ [n]u, and i ∈ { 1, . . . , u }, we have Sλi [F ]( E) ⊆ Sλ/ 2 

> i

(E) ∪ [F ′]λ/ 2

Proof. We prove the claim by induction on i. For the base case, Sλ 

> 0

[F ]( E) = E = Sλ/ 2 

> i

(E). For the inductive step, for some i ∈ [u] assume the claim holds for i − 1. For every direction-i column 27 L ⊆ Sλi λ/ 2( E), then by definition either |Sλi−1[F ]( E) ∩ L| ≥ λ or |F ∩ L| > 0. If |F ∩ L| > 0, then by definition L ⊆ F ′ ⊆ [F ′]λ/ 2. Meanwhile, if |Sλi−1[F ]( E) ∩ L| ≥ λ, then by the inductive hypothesis either |Sλ/ 2 

> i−1

(E) ∩ L| ≥ λ/ 2 or |[F ′]λ/ 2 ∩ L| ≥ λ/ 2. If |Sλ/ 2 

> i−1

(E) ∩ L| ≥ λ/ 2 then by definition L ⊆ Sλ/ 2 

> i

(E), while if |[F ′]λ/ 2 ∩ L| ≥ λ/ 2 then L ⊆ [F ′]λ/ 2 by Definition 3.6. Thus in all cases, if the direction-i column L ⊆ Sλi λ/ 2( E), then L ⊆ Sλ/ 2 

> i

(E) ∪ [F ′]λ/ 2, completing the inductive step. Claim 3.8, Claim 3.9, Lemma 3.7, and Equation (12) together imply that 

|R [F]( y) − c| = |R ≤u·TDec [F]( y) − c|≤ | SλDec  

> u

[F ](supp( e)) |≤ | SλDec /2 

> u

(supp( e)) | + [ F ′]λDec /2

≤ | SλDec /2 

> u

(supp( e)) | +

 2u + 1 

λDec /2n

u

· un · | F |

< |SλDec /2 

> u

(supp( e)) | +

 2u + 1 

λDec /2n

u

· u2n · TDec · λrun 

= |SλDec /2 

> u

(supp( e)) | + λout .

The following claim shows that the |SλDec /2 

> u

(supp( e)) | term above vanishes as long as | supp( e)| =

|e| < (λDec /2) u, thereby completing the proof of fault-tolerance. 

Claim 3.10. For λ > 0 and for E ⊆ [n]u of size |E| < λ u, we have Sλu (E) = ∅.Proof. We show the result by induction on u. For the base case, when u = 1, if |E| = |Sλ 

> 0

(E)| < λ 

then by definition Sλu (E) = ∅. For the inductive step, for some u ∈ N, assume the claim holds when 

u is replaced by u−1. Now for E ⊆ [n]u, if |E| < λ u then there are < λ u−1 direction-1 columns L for which |E ∩L| = |Sλ 

> 0

(E)∩L| ≥ λ. Thus Sλ 

> 1

(E) contains < λ u−1 direction-1 columns, so for every j1 ∈

[n], we have |Sλ 

> 1

(E)∩({j1}× [n]u−1)| < λ u−1. Now by definition, the restriction of Sλ 

> 1

(E), . . . , S λu (E)to points in ( {j1} × [n]u−1) equals the exact sequence ¯Sλ 

> 0

( ¯E), . . . , ¯Sλu−1( ¯E) obtained by defining ¯S

like we defined S but with u replaced by u − 1, and setting ¯E = Sλ 

> 1

(E) ∩ ({j1} × [n]u−1). Thus by the inductive hypothesis we have Sλu (E) ∩ ({j1} × [n]u−1) = ¯Sλu−1( ¯E) = ∅ for every j1 ∈ [u], and hence Sλu (E) = ∅, completing the inductive step. 

3.3 Code Switching Gadgets 

In this section, we present code-switching gadgets for tensor codes. Specifically, for i ∈ [u], let Ci be a ki-dimensional code. In Lemma 3.11 below, given a codeword of the tensor code C[u] = N 

> i∈[u]

Ci

that encodes some message x, we show how to unencode one of the codes Ci to switch to the code 

C[u]\{ i}(N 

> i′∈[u]\{ i}

Ci′

)⊔ki while preserving the underlying message x. Similarly, given a codeword of 

C[u]\{ i}, we can encode Ci to switch back to C[u] while preserving the message. Then in Lemma 3.12, we show how to iteratively apply these gadgets to each i = 1 , . . . , u along with repeated applications of an error-detection (Lemma 3.1) or error-correction (Lemma 3.5) gadget in order to switch between two tensor codes in which all u factors Ci may differ. 28 Lemma 3.11. For u ∈ N, for i ∈ [u] let Ci be a [ni, k i]q code with encoding map Enc i. For I ⊆ [u],let N I = Q 

> i∈I

[ni], KI = Q 

> i∈I

[ki], and let CI = N 

> i∈I

Ci be the [|N I |, |KI |]q tensor product code with encoding map Enc I = N 

> i∈I

Enc i. Fix an arbitrary ¯i ∈ [u]. Then the following hold: 1. (Downwards switching) For λin , λ run ≥ 0, let 

λout = n¯i · λin + 3 n3¯i · λrun 

and define decorated codes 

Din =



C[u], Enc [u], Ein = 2 N [u]

|≥λin 



Dout =



C[u]\{ ¯i}, Enc [u]\{ ¯i}, Eout = 2 N [u]\{ ¯i}

|≥λout 



.

Then there exists a circuit R using gate set {Init , Term , CX ∗}, space |N ′| ≤ 2|N [u]|, and time 

T ≤ n2¯i + 2 such that (R, Erun = 2 N ′

|⊔T 

> ≥λrun

, D in , D ⊔k¯i 

> out

) forms a fault-tolerant gadget for the 

K[u]-dit identity channel ¯O = IK[u] : FK[u] 

> q

→ FK[u] 

> q

.2. (Upwards switching) For λin , λ run ≥ 0, let 

λout = n2¯i · λin + 3 n3¯i · λrun 

and define decorated codes 

Din =



C[u]\{ ¯i}, Enc [u]\{ ¯i}, Ein = 2 N [u]\{ ¯i}

|≥λin 



Dout =



C[u], Enc [u], Eout = 2 N [u]

|≥λout 



.

Then there exists a circuit R using gate set {Init , Term , CX ∗}, space |N ′| ≤ 2|N [u]|, and time 

T ≤ n2¯i + 2 such that (R, Erun = 2 N ′

|⊔T 

> ≥λrun

, D ⊔k¯i 

> in

, D ⊔k¯i 

> out

) forms a fault-tolerant gadget for the 

K[u]-dit identity channel ¯O = IK[u] : FK[u] 

> q

→ FK[u] 

> q

.Proof. 1. (Downwards switching) Fix some matrix U ∈ Fk¯i×n¯i 

> q

such that U ◦ Enc ¯i = Ik¯i .Our desired circuit R first uses Init gates to initialize an ancilla block of dits labeled by (N [u]\{ ¯i})⊔k¯i = N [u]\{ ¯i} × [k¯i]. Next, R applies the circuit RU from Definition 2.12 in parallel to each direction-¯ i column of the ancilla block N [u]\{ ¯i} × [k¯i] and the input block N [u]. Specif-ically, each such direction-¯ i column is specified by some j−¯i ∈ N [u]\{ ¯i}, and consists of the k¯i

dits labeled {j−¯i} × [k¯i] in the ancilla block, along with the n¯i dits labeled {j−¯i} × [n¯i] in the input block (where for the input block we push the [ n¯i] factor to the ith of the u components). Finally, R applies Term gates to the input block of dits N [u], and returns the ancilla block (N [u]\{ ¯i})⊔k¯i .Therefore R uses space |N ′| ≤ | N [u]|+k¯i|N [u]\{ ¯i}| ≤ 2|N [u]| and time T = 1+( n¯i −k¯i)n¯i +1 ≤

n2¯i + 2. 29 For x ∈ FK[u] 

> q

, in the absence of an input error and a fault, then 

R ◦ Enc [u](x) = ( I⊗¯i−1 ⊗ U ⊗ I⊗u−¯i) ◦

O

> i∈[u]

Enc i

 (x)=



> ¯i

O

> i=1

Enc i ⊗Ik¯i ⊗

> u

O

> i=¯ i+1

Enc i

 (x)= (Enc [u]\{ ¯i})⊔k¯i (x).

If instead y = Enc [u](x) + f0 with |f0| < λ in , and F is an additive Erun -avoiding fault for 

F, then upon running R[F]( y), at most |f0| + |F| < λ in + T · λrun direction-i columns will experience an error. Here an “error” means a dit at some point in time whose value differs from that of the noiseless execution of R(Enc [u](x)) described above; we crucually use that every gate in R acts on dits entirely contained inside some direction-i column. Thus 

|R [F]( y) − (Enc [u]\{ ¯i})⊔k¯i (x)| = |R [F]( y) − R (Enc [u](x)) |

< n ¯i · (λin + T · λrun )

≤ λout ,

where the final inequality above holds by the definition of λout becuase T ≤ n2¯i + 2 ≤ 3n2¯i .2. (Upwards switching) The proof is analogous as in the downwards switching case above, except now we apply the encoding map Enc ¯i instead of the unencoding map U within each direction-i

column. Specifically, R first uses Init gates to initialize an ancilla block of dits labeled by 

N [u]. Next, R applies the circuit REnc ¯i from Definition 2.12 in parallel to each direction-¯ i

column of the ancilla block N [u] and the input block ( N [u]\{ ¯i})⊔k¯i . Finally, R applies Term gates to the input block, and returns the ancilla block. Therefore R uses space |N ′| ≤ k¯i|N [u]\{ ¯i}|+|N [u]| ≤ 2|N [u]| and time T = 1+ n¯i(n¯i −k¯i)+1 ≤

n2¯i + 2. The proof of correctness and fault-tolerance is exactly analogous as in the downwards switch-ing case above, so we omit the details to avoid redundancy. In brief, in the absence of errors, then R simply applies Enc ¯i to each direction-i column, hence mapping (Enc [u]\{ ¯i})⊔k¯i (x)to Enc [u](x). In the presence of up to λin input errors on each of the k¯i ≤ n¯i input code blocks of Din , along with up to λrun fault errors in each of the T timesteps, then 

< n ¯i · λin + T · λrun direction-i columns will experience an error, so the output error has weight 

< n ¯i(n¯i · λin + T · λrun ) ≤ λout .In Lemma 3.12 below, we repeatedly apply Lemma 3.11 to swap out all u codes Ci in the tensor code N 

> i∈[u]

Ci.

Lemma 3.12. For u, n, d ∈ N, for i ∈ [u] and α ∈ { in , out } let Ciα be a [ni = n, k α,i , d α,i ≥ d]q code with encoding map Enc iα. Let N = Q

> i∈[u]

[ni] = [ n]u, Kα = Q

> i∈[u]

[kα,i ], and let Cα = N 

> i∈[u]

Ciα be the tensor code with encoding map Enc α = N 

> i∈[u]

Ciα. Then the following hold: 

30 1. (Fault-detecting switching) Let ρ(u, n, d ) be the soundness parameter defined in Lemma 2.5, and let λrun , λ in , λ out , λ det > 0 satisfy 

λin = du

2

λrun ≤ du · ρ(u, n, d )32 u2n7

λout = 8u2n4

ρ(u, n, d ) · λrun 

λdet = u2n4 · λrun .

(13) 

For α ∈ { in , out }, define the decorated code Dα = ( Cα, Enc α, Eα = 2 N |≥λα ). Then there exists a circuit R using gate set G = {Init , Term , CX ∗}, space |N ′| ≤ (u + 1) nu, and time 

T ≤ 16 u2n2 along with sets Edet ⊆ N ′, Tdet ⊆ [T ] such that 

(R, Erun = 2 N ′

|⊔T 

> ≥λrun

, D in , D out , Edet = 2 Edet |⊔Tdet  

> ≥λdet

) (14) 

forms a mending fault-detecting gadget for the function ¯O : FKin  

> q

→ FKout  

> q

given by 

¯O(x) = ( x|Kin ∩Kout , 0Kout \Kin ) (15) 

that preserves all input components in Kin ∩ Kout , and outputs 0 in all output components outside of Kin .2. (Fault-tolerant switching) Assume that each Ciα has a radius-λDec ≥ 1 decoder Ri 

> Dec

using space NDec , time TDec , and gate set GDec . Let λrun , λ in , λ out > 0 satisfy 

λin =

 λDec 

2

u

λrun ≤ 1

n5TDec 

·

 λ2Dec /n 

2u+8 

u

λout =

 2u+4 

λDec /n 

u

· nT Dec · λrun .

(16) 

For α ∈ { in , out }, define the decorated code Dα = ( Cα, Enc α, Eα = 2 N |≥λα ). Then there exists a circuit R using gate set G = {Init , Term , CX ∗} ∪ G Dec , space |N ′| ≤ max {nu−1 ·|NDec |, 2nu}, and time T ≤ 2u2TDec + 8 un 2 such that 

(R, Erun = 2 N ′

|⊔T 

> ≥λrun

, D in , D out ) (17) 

forms a fault-tolerant gadget for the function ¯O : FKin  

> q

→ FKout  

> q

in Equation (15) .Proof. 1. The desired circuit R loops through i = 1 , . . . , u , and for each u applies the following sequence of gadgets: (1) The error-detecting gadget in Lemma 3.1 for the code Ni−1 

> i′=1

Ci′ 

> out

⊗ Nui′=i Ci′

> in

, with 

λ(1) in = du

2

λ(1) out = 8u2n4

ρ(u, n, d ) · λrun 

λ(1) det = ρ(u, n, d )8 · λout = u2n4 · λrun .

31 (2) The downwards switching gadget in Item 1 in Lemma 3.11 to unencode from Ci 

> in

in direction i, with 

λ(2) in = λ(1) out 

λ(2) out = n · λ(2) in + 3 n3 · λrun ≤ 11 u2n5

ρ(u, n, d ) · λrun .

(3) A single timestep consisting either of Term gates on dits in [ n][u]\{ i} × ([ kin ,i ] \ [kout ,i ]) if kin ,i ≥ kout ,i , or else of Init gates on dits in [ n][u]\{ i} × ([ kout ,i ] \ [kin ,i ]) if kout ,i > k in ,i .(For these product sets [ n][u]\{ i} × A we implicitly push the factor A to the ith position.) (4) The upwards switching gadget in Item 2 in Lemma 3.11 to encode into Ci 

> out

in direction i,with 

λ(4) in = λ(2) out + λrun 

λ(4) out = n2 · λ(4) in + 3 n3 · λrun ≤ 15 u2n7

ρ(u, n, d ) · λrun .

(5) The error-detecting gadget in Lemma 3.1 for the code Nii′=1 Ci′ 

> out

⊗ Nui′=i+1 Ci′

> in

, with 

λ(5) in = λ(4) out < du

2

λ(5) out = 8u2n4

ρ(u, n, d ) · λrun 

λ(1) det = ρ(u, n, d )8 · λout = u2n4 · λrun .

The upper bound on λ(4) out above follows from the definition of λrun in Equation (13). Then because R is simply the sequential composition of fault-detecting gadgets (which include fault-tolerant gadgets as a special case; see Fact 2.19), and the first gadget in the sequence is mend-ing by Lemma 3.1, Lemma 2.22 implies that the gadget in Equation (14) is mending fault-detecting for the function ¯O given by the sequential composition of the functions implemented by the sequence of gadgets above, where λin = λ(1) in , λout = λ(5) out , and λdet = λ(1) det = λ(5) det . The set Edet ⊆ N ′ is simply the subset given in Lemma 3.1. Similarly, following Lemma 3.1, 

Tdet ⊆ [T ] is the set of all timesteps of R in which we execute the second-to-last timestep of a call to the gadget in Lemma 3.1. Item (3) is the only step above in which we do not apply a gadget that implements the logical identity function on all dits. Instead, across all i = 1 , . . . , u , Item (3) terminates all logical dits in Kin \ Kout , and initializes all logical dits in Kout \ Kin to 0. Thus the gadget in Equation (14) indeed implements the function ¯O in Equation (15). By definition, each gadget used above to define R uses gates in {Init , Term , CX ∗}. Further-more, the gadget in Lemma 3.1 uses space ≤ (u + 1) nu and time ≤ un 2 + 2, while the gadgets in Lemma 3.11 use space ≤ 2nu and time ≤ n2 + 2. Thus by Lemma 2.22, R uses space 

|N ′| ≤ max {(u + 1) nu, 2nu} = ( u + 1) nu and time T ≤ u(2( un 2 + 2) + 2( n2 + 2) + 1) ≤ 16 u2n2,as desired. 32 2. The proof is similar to that of the fault-detecting case above, except now we apply the error-correction gadget Lemma 3.5 instead of the error-detection gadget Lemma 3.1. Specifically, the desired circuit R loops through i = 1 , . . . , u , and for each u applies the following sequence of gadgets: (1) The error-correcting gadget in Lemma 3.5 for the code Ni−1 

> i′=1

Ci′ 

> out

⊗ Nui′=i Ci′

> in

, with 

λ(1) in =

 λDec 

2

u

λ(1) out =

 2u + 1 

λDec /2n

u

· u2n · TDec · λrun .

(2) The downwards switching gadget in Item 1 in Lemma 3.11 to unencode from Ci 

> in

in direction i, with 

λ(2) in = λ(1) out 

λ(2) out = n · λ(2) in + 3 n3 · λrun ≤ 2 ·

 2u + 1 

λDec /2n

u

· u2n3 · TDec · λrun .

(3) A single timestep consisting either of Term gates on dits in [ n][u]\{ i} × ([ kin ,i ] \ [kout ,i ]) if kin ,i ≥ kout ,i , or else of Init gates on dits in [ n][u]\{ i} × ([ kout ,i ] \ [kin ,i ]) if kout ,i > k in ,i .(For these product sets [ n][u]\{ i} × A we implicitly push the factor A to the ith position.) (4) The upwards switching gadget in Item 2 in Lemma 3.11 to encode into Ci 

> out

in direction i,with 

λ(4) in = λ(2) out + λrun 

λ(4) out = n2 · λ(4) in + 3 n3 · λrun ≤ 3 ·

 2u + 1 

λDec /2n

u

· u2n5 · TDec · λrun .

(5) The error-detecting gadget in Lemma 3.1 for the code Nii′=1 Ci′ 

> out

⊗ Nui′=i+1 Ci′

> in

, with 

λ(5) in = λ(4) out <

 λDec 

2

u

λ(5) out =

 2u + 1 

λDec /2n

u

· u2n · TDec · λrun .

The upper bound on λ(4) out above follows from the definition of λrun in Equation (16). Then because R is simply the sequential composition of fault-tolerant gadgets, Lemma 2.22 implies that the gadget in Equation (17) is fault-tolerant for the function ¯O given by the sequen-tial composition of the functions implemented by the sequence of gadgets above, where by Equation (16) we have λin = λ(1) in and λout ≤ λ(5) out .Item (3) is the only step above in which we do not apply a gadget that implements the logical identity function on all dits. Instead, across all i = 1 , . . . , u , Item (3) terminates all logical dits in Kin \ Kout , and initializes all logical dits in Kout \ Kin to 0. Thus the gadget in Equation (17) indeed implements the function ¯O in Equation (15). 33 By definition, each gadget used above to define R uses gates in {Init , Term , CX ∗} ∪ G Dec .Furthermore, the gadget in Lemma 3.5 uses space ≤ nu−1 · | NDec | and time ≤ u · TDec , while the gadgets in Lemma 3.11 use space ≤ 2nu and time ≤ n2 + 2. Thus by Lemma 2.22, R uses space |N ′| ≤ max {nu−1 ·| NDec |, 2nu} and time T ≤ u(2 uT Dec +2( n2 +2)+1) ≤ 2u2TDec +8 un 2,as desired. 

3.4 Transversal Gate Gadgets 

In this section, we present gadgets to fault-tolerantly perform the gates in Definition 2.10 on all dits in a codeword with constant space and time overhead. These gadgets will not rely on a tensor code structure, though our gadget for CCX ∗ gates will require a certain multiplication property, which for instance is satisfied by Reed-Solomon codes. We borrow the term “transversal gate” from the quantum error-correction literature to refer to the application of a gate uniformly across a block of dits: 

Definition 3.13. Let G : Fmin  

> q

→ Fmout  

> q

be a gate, and let N be a set. The transversal application 

G⊔N : FN ×[min ] 

> q

→ FN ×[mout ] 

> q

of G is the gate with input dits labeled by N ⊔min ∼= N × [min ] and output dits labeled by N ⊔mout ∼= N × [mout ], which simply applies G to each set of dits sharing the same label in N .We extend this definition in the natural way to sets of input dits N1 ∼= · · · ∼= Nmin ∼= N and output dits N ′ 

> 1

∼= · · · ∼= N ′

> mout

∼= N with fixed isomorphisms to N .

While the term “transversal gate” sometimes includes more general constant-depth circuits involving a specified gate, in this paper we use the more restrictive notion in Definition 3.13. Below, recall that we use ‘ ∗’ to denote component-wise multiplication of vectors. 

Lemma 3.14. Let C be a [n, k ]q code with encoding map Enc . For α ∈ { in , out }, we let Dα =(C, Enc , Eα = 2 [n]|≥λα ) for λα defined below. Then for every λin , λ run ≥ 0, the following hold: 1. Letting λout = λrun , there exists a fault-tolerant gadget 

(RInit , Erun = 2 [n]|≥λrun , ∅, D out )

for ¯OInit = Init ⊔kin using space N = [ n], time T = 1 , and gate set {Init }.2. There exists a fault-tolerant gadget 

(RTerm , Erun = ∅, D in , ∅)

for ¯OTerm = Term ⊔kin using space N = [ n], time T = 1 , and gate set {Term }.3. Letting λout = λin + λrun , for every a ∈ Fkq , there exists a fault-tolerant gadget 

(RXa , Erun = 2 [n]|≥λrun , D in , D out )

for ¯OXa = X a := F 

> i∈[k]

Xai using space N = [ n], time T = 1 , and gate set {X∗}.

34 4. Letting λout = 2 λin + λrun , for every a ∈ Fq, there exists a fault-tolerant gadget 

(RCX a , Erun = 2 [n]⊔2

|≥λrun , D ⊔2in , D ⊔2out )

for ¯OCX a = (CX a)⊔k using space N = [ n]⊔2, time T = 1 , and gate set {CX a}.5. Let λout = 3 λin + λrun . Let C′ ⊆ Fnq be a [n, k ′ ≥ k]q code with encoding map Enc ′ such that 

C ∗ C ⊆ C′, and such that for every x1, x 2 ∈ Fkq , there exists x′ ∈ Fk′−kq such that 

Enc( x1) ∗ Enc( x2) = Enc ′(x1 ∗ x2, x ′). (18) 

For α ∈ { in , out }, let D′ 

> α

= ( C′, Enc ′, E′ 

> α

= 2 [n]|≥λα ). Then for every a ∈ Fq, there exists a fault-tolerant gadget 

(RCCX a , Erun = 2 [n]⊔3

|≥λrun , D in ⊔ Din ⊔ D′

> in

, D out ⊔ Dout ⊔ D′

> out

)

for the set ¯OCCX a of functions ¯O : Fk+k+k′ 

> q

→ Fk+k+k′ 

> q

that first apply (CCX a)⊔k to the input, and then add (0 k, 0k, 0k, x ′) to the result for some x′ ∈ Fk′−kq . That is, ¯O(x1, x 2, x 3) = (x1, x 2, x 3 + ax 1x2 + x′), where every x′ ∈ Fk′−kq yields a distinct ¯O ∈ ¯O. This gadget uses space N = [ n]⊔3, time T = 1 , and gate set {CCX a}.Proof. 1. The result follows immediately by letting RInit = Init ⊔n.2. The result follows immediately by letting RTerm = Term ⊔n.3. We let RXa = X Enc( a). On input x ∈ Fkq , a noiseless execution outputs 

RXa (Enc( x)) = Enc( x) + Enc( a) = Enc(X a(x)) .

As RXa has a single timestep consisting of single-dit gates, if there are < λ in input errors and < λ run fault errors, then there are < λ out = λin + λrun output errors, so the desired fault-tolerance holds. 4. We let RCX a = (CX a)⊔n. On input ( x1, x 2) ∈ Fk+kq , a noiseless execution outputs 

RCX a (Enc ⊔2(x1, x 2)) = (Enc( x1), Enc( x2) + a Enc( x1)) = Enc ⊔2(x1, x 2 + ax 1)= Enc ⊔2(CX a(x1, x 2)) .

As RCX a has a single timestep consisting of 2-dit gates, any input error on some dit can only propagate to one other dit. Therefore if each of Enc( x1), Enc( x2) has < λ in input errors and there are < λ run fault errors, then there are < λ out = 2 λin + λrun output errors, so the desired fault-tolerance holds. 5. We let RCCX a = (CCX a)⊔n. On input ( x1, x 2, x 3) ∈ Fk+k+k′ 

> q

, defining x′ ∈ Fk′−kq as in Equation (18), then a noiseless execution outputs 

RCCX a (Enc( x1), Enc( x2), Enc ′(x3)) = (Enc( x1), Enc( x2), Enc ′(x3) + a Enc( x1) ∗ Enc( x2)) = (Enc( x1), Enc( x2), Enc ′(x3) + a Enc ′(x1 ∗ x2, x ′)) = (Enc ⊔ Enc ⊔ Enc ′)(CCX a(x1, x 2, x 3) + (0 k, 0k, 0k, ax ′)) 

∈ (Enc ⊔ Enc ⊔ Enc ′)( ¯OCCX a (x1, x 2, x 3)) .

35 As RCX a has a single timestep consisting of 3-dit gates, any input error on some dit can only propagate to two other dits. Therefore if each of Enc( x1), Enc( x2), Enc ′(x3) has < λ in input errors and there are < λ run fault errors, then there are < λ out = 3 λin + λrun output errors, so the desired fault-tolerance holds. In Lemma 3.14, strictly speaking all of the gadgets except the X a gadget (Item 3) implement transversal gates according to Definition 3.13, as the X a gadget applies different gates X ai to different dits i.Item 5 in Lemma 3.14 can be generalized to allow for two distinct codes C1, C 2 such that 

C1 ∗ C2 ⊆ C′. However, it will be sufficient for our purposes to consider C1 = C = C2 as in Lemma 3.14. The reason that Item 5 needs a set ¯OCCX a of functions, whereas the other gadgets impelement a single function, is that C′ may have larger dimension than C. Hence when multiplying two codewords Enc( x1), Enc( x2) ∈ C, the product is a codeword Enc( x1) ∗ Enc( x2) = Enc ′(x1 ∗

x2, x ′) ∈ C′ with some additional message dits x′ ∈ Fk′−kq , which may have arbitrary value. 

# 4 Fault-Detecting and Fault-Tolerance Schemes 

In this section, we combine our gadgets in Section 3 to present schemes for compiling any logical circuit into a fault-detecting or a fault-tolerant circuit that implements the same underlying function as the logical circuit. Our fault-detecting circuit will be resilient to more errors per timestep than our fault-tolerant circuit, which is not surprising as fault-tolerance is a stronger property than fault-detection. Indeed, the two schemes have the same structure, except that our fault-detecting scheme detects errors using the error-detection gadget in Lemma 3.1, whereas our fault-tolerant scheme corrects errors using the error-correction gadget in Lemma 3.5. Our schemes in this section are based on tensor products of Reed-Solomon codes, which have growing alphabet size. In Section 5 below, we show how to reduce the alphabet size to a constant. Our main schemes are stated in the theorem below. 

Theorem 4.1. For every u, n, k ∈ N with u ≥ 4, k ≤ n/ 32 , every subset K ⊆ [k]u, and every prime power q ≥ n, there exists a [nu, |K|, ≥ (n − 8k + 1) u]q code C(q, u, n, k, K ) with encoding map Enc C(q,u,n,k,K ) : FKq → F[n]u 

> q

satisfying the following properties. The nu codeword dits of 

C(q, u, n, k, K ) are labeled by [n]u, and the |K| = dim( C(q, u, n, k, K )) message dits are labeled by the set K.For every circuit ¯R acting on a set of q-ary dits labeled by K[3] := K1 ⊔ K2 ⊔ K3 with each 

Kb = [ k]u, using time ¯T and gate set G = {Init , Term , X∗, CX ∗, CCX ∗}, with input and output dits 

Kin , K out ⊆ K[3] respectively, letting 

Cα,b =

(

C(q, u, n, k, K α ∩ Kb), Kα ∩ Kb̸ = ∅∅, Kα ∩ Kb = ∅. (19) 

for α ∈ { in , out } and b ∈ [3] , then the following hold: 1. (Mending fault-detecting gadget) For d ≥ 0, let ρ(u, n, d ) be the soundness parameter defined in Lemma 2.5. Let λrun ∈ [0 , ¯λrun ] for 

¯λrun = ¯λrun (u, n ) = ρ(u, n, n − 16 k + 2) · (n − 16 k + 2) u−1

210 · u2n8 , (20) 36 and let 

λin = (n − 8k + 1) u

4

λout = 16 u2n4

ρ(u, n, n − 8k + 1) · λrun 

λdet = ( u − 1) 2n4 · λrun .

(21) 

For α ∈ { in , out }, let 

Dα,b = ( Cα,b , Enc Cα,b , 2[n]u

|≥λα ) ∀b ∈ [3] 

Dα = ( Cα, Enc α, Eα) = G

> b∈[3]

Dα,b .

Then there exists T ∈ N and a set N ′ with subsets Edet ,t ⊆ N ′ for t ∈ [T ] such that there is a mending fault-detecting gadget 

R, Erun = 2 N ′

|⊔T 

> ≥λrun

, D in , D out , Edet = G 

> t∈[T]

(2 Edet ,t |≥λdet )

 (22) 

for ¯O(·) = ¯R(·) using space |N ′| ≤ 3( u + 1) nu, time T ≤ O( ¯T · u4n2(log n)2), and gate set G.2. (Fault-tolerant gadget) There exists an absolute constant ηDec ≥ 1 such that the following holds. Let λrun ∈ [0 , ¯λrun ] for 

¯λrun = ¯λrun (u, n ) = nu−16 

ηDec · log( q) · 2u(u+16) , (23) 

and let 

λin =

 n − 8k + 1 8

u

λout = ηDec · 2u(u+8) · n3 log( q) · λrun .

(24) 

For α ∈ { in , out }, let 

Dα,b = ( Cα,b , Enc Cα,b , 2[n]u

|≥λα ) ∀b ∈ [3] 

Dα = G

> b∈[3]

Dα,b .

Then there exists a fault-tolerant gadget 



R, Erun = 2 N ′

|⊔T 

> ≥λrun

, D in , D out 



(25) 

for ¯O(·) = ¯R(·) using space |N ′| ≤ 3ηDec · log( q) · nu+1 , time T ≤ O( ¯T · u4n2(log n)2(log q)) ,and gate set G.

37 In Theorem 4.1, the choice to allow up to 3 input and output code blocks was somewhat arbitrary, to highlight our ability to perform 3-dit gates such as CCX ∗ on logical dits across different code blocks. It is useful to be able to act on at least 2 code blocks, so that we can perform logical computations on data encoded in different code blocks, and move message dits between different code blocks. By composing any scheme capable of acting on 2 more more code blocks with itself, we can fault-tolerantly accumulate message dits from any number of code blocks into a single code block, within which we can then perform a fault-tolerant computation. 

4.1 Logical Circuit Compilation 

In this section, we show how to compile an arbitrary logical circuit ¯R into one whose structure is amenable to our fault-tolerant gadgets in Section 3, with only small blowups in the space and time usage. In particular, in light of the fault-tolerant gadgets for transversal gates in Lemma 3.14, our compiled circuit will consist of such transversal gates (see Definition 3.13). For this purpose, we introduce the following notation. 

Definition 4.2. For a set K = Q

> i∈[u]

[ki], for every i ∈ [u] and j ∈ [ki] we define a subset 

K|i→j ⊆ K by K|i→j = {κ ∈ K : κi = j}.

Lemma 4.3. For a prime power q and for u, k ∈ N with u ≥ 4, let ¯R be a circuit acting on 3ku

q-ary dits labeled by the set K[3] = K1 ⊔ K2 ⊔ K3 with each Kb = [ k]u, using time ¯T and gate set G = {Init , Term , X∗, CX ∗, CCX ∗}. For subsets Bin , B out ⊆ [3] , we assume ¯R has input dits 

KBin = F 

> b∈Bin

Kb and output dits KBout = F 

> b∈Bout

Kb. Letting k′ = 8 k, then there exists a circuit 

¯R′ acting on 3 · k′u dits labeled by K′ 

> [3]

= K′ 

> 1

⊔ K′ 

> 2

⊔ K′ 

> 3

with each K′ 

> b

= [ k′]u ⊇ [k]u = Kb and with input dits K′ 

> Bin

= F 

> b∈Bin

K′ 

> b

and output dits K′ 

> Bout

= F 

> b∈Bout

K′

> b

, using time ¯T ′ ≤ O( ¯T · (u log k)2)

and gate set G, with associated function ¯R′(·) : FK′ 

> Bin
> q

→ FK′ 

> Bout
> q

that maps every x ∈ FK′ 

> Bin
> q

to 

¯R′(x) = ( ¯R(x|KBin ), 0K′  

> Bout \KBout

) ∈ FK′ 

> Bout
> q

.

Furthermore, for every t ∈ [ ¯T ′] , then either: 1. ¯R′ 

> t

consists entirely of X∗ gates, or 2. For some i ∈ [u] and some G ∈ { Init , Term , CX ∗, CCX ∗}, ¯R′ 

> t

consists entirely of transversal applications of G to sets K′

> b

|i→j for arbitrary values of b ∈ [3] and j ∈ [k′].

To illustrate the structure of ¯R′ in Lemma 4.3, consider some t ∈ [ ¯T ′] and a ∈ Fq such that 

G = CX a in Item 2 in Lemma 4.3. Then there exists i ∈ [u] such that the only non-identity gates applied in timestep t of ¯R′ consist of transversal applications (in the sense of Definition 3.13) of 

G = CX a to pairs of sets K′ 

> b1

|i→j1 , K ′ 

> b2

|i→j2 for ( b1, j 1)̸ = ( b2, j 2) ∈ [3] × [k′]. That is, G = CX a

is applied to every κ1 ∈ K′ 

> b1

|i→j1 , κ 2 ∈ K′ 

> b2

|i→j2 such that κ1 ∈ K′

> b1

∼= [ k′]u and κ2 ∈ K′

> b2

∼= [ k′]u

agree at all coordinates in [ u] \ { i} when viewed as tuples in [ k′]u.To prove Lemma 4.3, we will use the following known result on efficiently implementing permuta-tions in circuits with a hypercubic connectivity structure. Below, we define the gate Swap : F2 

> q

→ F2

> q

in the ordinary way Swap( x1, x 2) = ( x2, x 1). Recall that Swap can for instance be implemented using one ancilla dit along with 6 CX ∗ gates, one Init gate, and one Term gate. 3   

> 3If Fqhas characteristic 2, there is a simpler implementation with 3 CX ∗gates and no ancillas.

38 Lemma 4.4 ([Bat68]) . For s ∈ N, let S = [2] s, and let π : S → S be a permutation, which we also view as a function π : FSq → FSq in the natural way. Then there exists a circuit R acting on dits labeled by S using time T ≤ O(s2) and gate set {Swap }, such that R(·) = π(·). Furthermore, for every time step t ∈ [T ], there exists some i(t) ∈ [s] such that every Swap gate in Rt acts on a pair of dits whose labels differ only in coordinate i(t).

Lemma 4.4 allows us to implement arbitrary logical permutations via transversal controlled-swap gates, which in turn can be implemented using transversal CCX gates. Therefore to prove Lemma 4.3, we will use Lemma 4.4 to move dits into appropriate positions, such that the desired gates can then be implemented transversally. We provide the details below; we will also use the following basic fact. 

Fact 4.5. For every a ∈ Fq, the gate CX a can be implemented using one ancilla dit along with one 

CCX +1 gate, one X∗ gate, one Init gate, and one Term gate. Furthermore, the gate CCX a can be implemented using two ancilla dits along with two CCX +1 

gates, one X∗ gate, two Init gates, and two Term gates. Proof. We implement CX a by performing (x1, x 2) Init 

−−→ (x1, x 2, 0) Xa

−−→ (x1, x 2, a )

> CCX +1

−−−−→ (x1, x 2 + ax 1, a ) Term 

−−−→ (x1, x 2 + ax 1).

We implement CCX a by performing (x1, x 2, x 3) Init ⊔2

−−−−→ (x1, x 2, x 3, 0, 0) Xa

−−→ (x1, x 2, x 3, 0, a ) CCX +1 

−−−−→ (x1, x 2, x 3, x 1x2, a )

> CCX +1

−−−−→ (x1, x 2, x 3 + ax 1x2, x 1x2, a ) Term ⊔2

−−−−−→ (x1, x 2, x 3 + ax 1x2).

Proof of Lemma 4.3. The circuit ¯R′ will consist of the sequential composition of three parts ¯R′ = ¯R′ 

> end

◦ ¯R′ 

> mid

◦ ¯R′

> start

, (26) which we now define. ¯R′ 

> start

initializes all dits in K′ 

> [3]

\ KBin to 0, and then performs transversal Swap gates to move all dits in KBin into K′

> 1

. Specifically, for each b ∈ [3] \ KBin then ¯R′ 

> start

applies Init to all dits in K′

> b

, while for each b ∈ KBin , each i ∈ [u], and each j ∈ [k′] \ [k] then ¯R′

> start

applies Term followed by Init to all dits in K′

> b

|i→j . Next, for each j ∈ [k], ¯R′ 

> start

swaps the dits in 

K′

> 2

|1→j with the dits in K′

> 1

|1→j+k, where the swaps are implemented using CX ∗ gates (see above) with K′

> 2

|1→j+k as ancillas. Similarly, for each j ∈ [k], ¯R′ 

> start

swaps the dits in K′

> 3

|2→j with the dits in K′

> 1

|2→j+k, where the swaps are implemented using CX ∗ gates (see above) with K′

> 3

|2→j+k

as ancillas. This entire procedure takes time 2 u + O(1) = O(u), as in each step we can parallelize across all j.Therefore ¯R′ 

> start

swaps the input dits originally in K1, K 2, K 3 into the respective blocks K1,1, K 1,2, K 1,3 ⊆

K′ 

> 1

defined by K1,1 = [ k]u, K1,2 = {k + 1 , . . . , 2k} × [k]u−1, K1,3 = [ k] × { k + 1 , . . . , 2k} × [k]u−2. We let K1,[3] = K1,1 ⊔ K1,2 ⊔ K1,3 ⊆ K′

> 1

. The circuit ¯R′ 

> end

will invert this operation, by performing the same swaps described above to swap the dits in K1,[3] back to their original positions K[3] . Then for each b ∈ [3] \ Bout , ¯R′ 

> end

applies Term to all dits in K′

> b

, while for each b ∈ Bout , each i ∈ [u], 39 and each j ∈ [k′] \ [k], ¯R′ 

> end

applies Term followed by Init to all dits in K′

> b

|i→j . This procedure for ¯R′ 

> end

takes time O(u), similarly as for ¯R′ 

> start

above. Therefore ¯R′ as defined in Equation (26) with ¯R′

> start

, ¯R′ 

> end

defined above has the desired set of input and output dits, and outputs value 0 on all dits in K′ 

> Bout

\ KBout . For α ∈ { in , out }, let 

K1,B α = F 

> b∈Bα

K1,b ∼= KBα . It only remains to construct ¯R′ 

> mid

such that for every x ∈ FK1,B in  

> q

,then ¯R′

> mid

(x, 0K′ 

> [3] \K1,B in

)|K1,B out = ¯R(x) (27) under the isomorphisms FKBα  

> q

∼= FK1,Bα  

> q

.Let s = ⌈log 2 k⌉ + 2, and fix an isomorphism [2 s] ∼= [2] s, so that [2 s]u ∼= [2] su . Note that 4k ≤ 2s ≤ 8k = k′. We let [2 s]u 

> 1

⊆ K′ 

> 1

denote the dits in K′ 

> 1

labeled by [2 s]u ⊆ [k′]u

We begin by applying Lemma 4.4 to show Claim 4.6 below. In the remainder of this proof below, we say a circuit acting on dits K′ 

> [3]

(or on a subset thereof) has transversal structure if each timestep of the circuit satisfies Item 1 or Item 2 in the statement of Lemma 4.3. 

Claim 4.6. For every permutation π : [2 s]u × [2 s]u, there exists a circuit using space K′ 

> [3]

and gate set G with transversal structure that permutes the dits in [2 s]u 

> 1

⊆ K′ 

> 1

by π in time O(su )2. Dits in 

K′ 

> [3]

\ [2 s]u 

> 1

may be acted upon arbitrarily. Proof. Lemma 4.4 gives a circuit on dits [2 s]u 

> 1

∼= [2] su  

> 1

that implements the permutation π using time 

O(su )2 and gate set {Swap }, where each timestep t applies a subset of the Swap gates performed in transversal applications to sets K′

> 1

|i→j for some i = i(t) ∈ [u]. That is, for various disjoint pairs of sets K′

> 1

|i→j1 , K ′

> 1

|i→j2 ⊆ K′

> 1

, this circuit from Lemma 4.4 applies Swap to some (but maybe not all) pairs of dits κ1 ∈ K′

> 1

|i→j1 , κ 2 ∈ K′

> 1

|i→j2 whose labels in [ k′]u agree on coordinates [ u] \ { i}.For each such j1, j 2, we may instead initialize a block K′

> 2

|i→j3 ⊆ K′ 

> 2

to contain the indicator vector for the Swap gates we want to perform on K′

> 1

|i→j1 , K ′

> 2

|i→j2 ; we can in particular set j3 = j1.This initialization can be done with Term followed by Init gates to set dits in K′

> 2

|i→j3 to value 0, and then by a layer of X ∗ gates to give the desired values in {0, 1}. Then the desired Swap gates can be implemented by permforming transversal controlled-Swap across the three blocks 

K′

> 2

|i→j3 , K ′

> 1

|i→j1 , K ′

> 1

|i→j2 . Just as Swap can be implemented using an ancilla dit initialized to 0 along with 6 CX ∗ gates, controlled-Swap can be implemented using an ancilla dit initializd to 0 along with 6 CCX ∗ gates. Therefore we initialize an additional block K′

> 2

|i→j4 ⊆ K′ 

> 2

for these additional ancilla dits, and then we implement one layer of the desired Swap gates using gates 

{Init , Term , X∗, CCX ∗} ⊆ G in a circuit with transversal structure; we can in particular set j4 = j2.Furthermore, by construction this circuit acts on dits K′ 

> 1

⊔K′ 

> 2

and runs in time O(1). It follows that the entire permutation circuit, which consists of O(su )2 layers of Swap gates, can be implemented in a circuit with transversal structure using gate set G in time O(su )2, as desired. We thus construct ¯R′ 

> mid

from ¯R as follows. First, by increasing the running time by at most a factor of 5, we can assume that every timestep in ¯R consists entirely of gates from one of the sets 

{Init }, {Term }, {X∗}, {CX ∗}, or {CCX ∗}. We now compile each such timestep into a sub-circuit of ¯R′ 

> mid

of the desired transversal structure, using time O(su )2. Note that instead of terminating and then initializing a dit in K[3] that experiences a Term and then later a Init gate in ¯R, we will simply set this dit’s value to 0, but leave it active in ¯R′

> mid

. Specifically, we consider timesteps of ¯R

with each of the five types of gates separately: 1. X ∗: Simply apply the desired X ∗ gates to dits in K1,[3] ∼= K[3] .40 2. Term on dits A ⊆ K1,[3] ∼= K[3] : Run Term followed by Init on K′

> 1

|3→j+k for every j ∈ [k]. (Recall here that K′

> 1

|3→j+k exists because u ≥ 4 ≥ 3.) Use Claim 4.6 to swap some |A| of the 

≥ k(2 s)u−1 dits in [2 s]u 

> 1

∩ F 

> j∈[k]

K′

> 1

|3→j+k with the dits A ⊆ K1,[3] ∼= K[3] , while preserving the positions of the other dits in K1,[3] . (Recall here that k(2 s)u−1 ≥ 3ku = |K1,[3] | because 2s ≥ 4k and u ≥ 4.) Then again run Term followed by Init on K′

> 1

|3→j+k for every j ∈ [k]. 3. Init on dits A ⊆ K[3] : apply the same procedure as for Term above. 4. CX ∗ on various pairs of dits in K1,[3] ∼= K[3] : First, apply Claim 4.6 to permute the dits in [2 s]u 

> 1

∩F 

> j∈[2 k]

K′

> 1

|3→j such that every CX ∗ gate now has control dit in some K′

> 1

|3→j for j ∈ [k], and target dit in the respective position in K′

> 1

|3→j+k. For dits acted on the by identity, we simply assign their associated gate to be CX 0 = I2. We then implement the desired CX ∗ gates using transversal gates in G on blocks K′

> b

|3→j . Specifically, we implement each gate CX a on a dit in K′

> 1

|3→j and a dit in K′

> 1

|3→j+k using the procedure in Fact 4.5, where K′

> 2

|3→j provides the ancilla dits, so that each timestep either performs X ∗ gates, or else performs transversal CCX +1 , Init, or Term gates across different blocks K′

> b

|3→j′ . Finally, we apply Claim 4.6 again to permute the dits originally in K1,[3] back to their starting positions. 5. CCX ∗ on various triples of dits in K[3] : First, apply Claim 4.6 to permute the dits in [2 s]u 

> 1

∩F 

> j∈[3 k]

K′

> 1

|3→j such that every CCX ∗ gate now has first control dit in some K′

> 1

|3→j for j ∈ [k], second control dit in the respective position in K′

> 1

|3→j+k, and target dit in the respective position in K′

> 1

|3→j+2 k. For dits acted on the by identity, we simply assign their associated gate to be CCX 0 = I3. We then implement the desired CCX ∗ gates using transversal gates in G on blocks K′

> b

|3→j . Specifically, we implement each gate CCX a using the procedure in Fact 4.5, where K′

> 2

|3→j , K ′

> 2

|3→j+k provide the ancilla dits, so that each timestep either performs X ∗ gates, or else performs transversal CCX +1 , Init, or Term gates across different blocks K′

> b

|3→j′ . Finally, we apply Claim 4.6 again to permute the dits originally in K1,[3] back to their starting positions. By construction, in each cases above, the resulting circuit acting on dits K′ 

> [3]

has the desired transversal structure, and by Claim 4.6 uses time O(su )2. For each timestep of the original circuit consisting of either X ∗, CX ∗, or CCX ∗ gates, the above construction for ¯R′ 

> mid

by definition induces the same function as the original circuit on dits K1,[3] ∼= K[3] . Meanwhile, for each timestep of the original circuit consisting of either Init or Term gates on some set A ⊆ K1,[3] ∼= K[3] of dits, the above construction simply sets the values of all dits in A to 0 while preserving the values of all other dits; this change does not affect the overall function implemented by the entire circuit, as a dit that is terminated cannot be used until it is reinitialized to 0. Therefore when we combine all timesteps, ¯R′ 

> mid

satisfies the desired condition Equation (27). It follows from the construction above that ¯R′ 

> mid

uses time O( ¯T · (su )2). We showed above that ¯R′

> start

, ¯R′ 

> end

both use time O(u), and all three circuits use space K′

> [3]

. Thus ¯R′ uses time 

O( ¯T · (su )2) = O( ¯T · (u log k)2) and space K′

> [3]

, as desired. 

4.2 Code Family 

In this section, we describe the codes C(q, u, n, k, K ) and associated encoding maps Enc C(q,u,n,k,K )

that we use to prove Theorem 4.1. Specifically, defining k′ = 8 k as in Lemma 4.3 above, will construct C(q, u, n, k, K ) as a subcode of the uth tensor power C⊗u 

> 0

of a [ n, k ′, d = n − k′ + 1] q code 41 C0 that exhibits a multiplication property , meaning that C∗20 = C0 ∗ C0 = span {c ∗ c′ : c, c ′ ∈ C0}

has good distance. Such codes C0 are in turn given by Reed-Solomon codes, as described below (see Definition 2.7). Note that our techniques do not rely on the distance being exactly n − k′ + 1 (i.e. on the Singleton bound), though we assume such a distance for simplicity, as it is achieved by the Reed-Solomon codes we use. 

Fact 4.7. For every k′ ≤ n ∈ N and every prime power q ≥ n, there exists a [n, k ′, d = n − k′ + 1] q

code C0 = C0(q, n, k ′) such that C∗20 has distance n − 2k′ + 2 . Furthermore, there exists a radius-

(n − k′ + 1) /2 decoder for C0, and a radius-(n − 2k′ + 2) /2-decoder for C∗20 , where both decoders use space |N ′| = O(n2 log q), time T = O(n2 log q), and get set G = {Init , Term , X∗, CX ∗, CCX ∗} over 

q-ary dits. Proof. For some subset E ⊆ Fq of size |E| = n, simply take C0 to be the Reed-Solomon code RS( q, k ′, E ), so that by Fact 2.8 the code C∗20 = RS( q, 2k′ − 1, E ) has distance n − 2k′ + 1. The desired decoders exist by Lemma 3.3. For the code C0 in Fact 4.7, fix some set K′ 

> 0

⊆ [n] of size |K′

> 0

| = k′ giving a systematic encoding for C0, as shown to exist in Fact 2.2. That is, under a fixed isomorphism [ k′] ∼= K′

> 0

, which induces an isomorphism Fk′ 

> q

∼= FK′ 

> 0
> q

, we have a well-defined systematic encoding map Enc 0 : Fk′ 

> q

∼= FK′ 

> 0
> q∼

−→

C0 ⊆ Fnq , meaning that each x ∈ Fk′ 

> q

∼= FK′ 

> 0
> q

is mapped to the unique codeword Enc 0(x) ∈ C such that Enc 0(x)|K′ 

> 0

= x.Furthermore, because C∗20 |K′ 

> 0

= ( C0|K′ 

> 0

)∗2 = ( FK′ 

> 0
> q

)∗2 = FK′ 

> 0
> q

, Fact 2.2 implies that there exists a subset K′′  

> 0

⊆ [n]u \ K′ 

> 0

such that K′ 

> 0

⊔ K′′  

> 0

gives a systematic encoding map Enc ′ 

> 0

: FK′ 

> 0⊔K′′
> 0
> q∼

−→

C∗20 ⊆ Fnq for C∗20 . That is, for every ( x, x ′) ∈ FK′ 

> 0
> q

⊕ FK′′  

> 0
> q

, we have Enc ′

> 0

(x, x ′)|K′ 

> 0⊔K′′
> 0

= ( x, x ′). 

Claim 4.8. For every I ⊆ [u] and every x1, x 2 ∈ FK′ 

> 0
> I
> q

, there exists some x′ ∈ F(K′   

> 0⊔K′′
> 0)I\K′
> 0
> I
> q

such that 

Enc ⊗I 

> 0

(x1) ∗ Enc ⊗I 

> 0

(x2) = Enc ′ 

> 0
> ⊗I

(x1 ∗ x2, x ′). (28) 

In other words, the codes (C⊗I 

> 0

, Enc ⊗I 

> 0

), (( C∗20 )⊗I , Enc ′ 

> 0
> ⊗I

) satisfy Equation (18) in Item 5 in Lemma 3.14. Proof. By definition Enc ⊗I 

> 0

(x1) ∗ Enc ⊗I 

> 0

(x2) ∈ (C∗20 )⊗I . Because tensor products of systematic encodings are in turn systematic encodings, we have Enc ⊗I 

> 0

(x1) ∗ Enc ⊗I 

> 0

(x2)|K′ 

> 0
> I

= x1 ∗ x2 ∈ FK′ 

> 0
> I
> q

,so there exists some x′ ∈ F(K′   

> 0⊔K′′
> 0)I\K′
> 0
> I
> q

satisfying Equation (28). Recall that k′ = 8 k. Let K0 ⊆ K′ 

> 0

be the subset of size |K0| = k corresponding to the set [k] ⊆ [k′] under the isomorphism [ k′] ∼= K′

> 0

, that is, K0 ∼= [ k] ⊆ [k′] ∼= K′

> 0

.For K ⊆ [k]u ∼= Ku 

> 0

, we then define Enc C(q,u,n,k,K ) : FKq → F[n]u 

> q

and C(q, u, n, k, K ) ⊆ F[n]u 

> q

by Enc C(q,u,n,k,K ) := Enc ⊗u 

> 0

|K

C(q, u, n, k, K ) := Enc C(q,u,n,k,K )(FKq ) = {c ∈ C⊗u 

> 0

: c|K′ 

> 0
> u\K

= 0 }.

By definition, Enc ⊗u 

> 0

and hence Enc C(q,u,n,k,K ) is also a systematic encoding map. That is, for 

x ∈ FKq we have Enc C(q,u,n,k,K )(x)|K = x.42 4.3 Fault-Detecting and Fault-Tolerant Gadgets 

In this section, we prove Theorem 4.1 using the codes C(q, u, n, k, K ) defined in Section 4.2. The main idea is to compile the logical circuit ¯R to a circuit ¯R′ with the transversal structure given by Lemma 4.3, which we in turn render fault-tolerant using the gadgets in Section 3. Throughout this section, we define q, u, n, k as in Theorem 4.1, we define k′ = 8 k, C0 =

C0(q, n, k ′), Enc 0, Enc ′ 

> 0

as in Section 4.2, and for K ⊆ [k]u we define C(q, u, n, k, K ) as in Section 4.2. We also define K′ 

> [3]

= K′ 

> 1

⊔K′ 

> 2

⊔K′ 

> 3

with each K′ 

> b

= [ k′]u ⊇ [k]u = Kb as in Lemma 4.3. Furthermore, we let N[3] = N1 ⊔ N2 ⊔ N3 with each Nb = [ n]u. For a subset B ⊆ [3], we define KB = F 

> b∈B

Kb,

K′ 

> B

= F 

> b∈B

K′

> b

, and NB = F 

> b∈B

Nb.

Proof of Theorem 4.1. By its definition in Section 4.2, the code C(q, u, n, k, K ) has nu codeword dits labeled by [ n]u, has |K| = dim( C(q, u, n, k, K )) message dits labeled by K, and has distance 

≥ (n − k′ + 1) u = ( n − 8k + 1) u. It therefore remains to prove the mending fault-detecting and fault-tolerant claims in the statement of Theorem 4.1. For this purpose, as in Theorem 4.1, fix a circuit ¯R using space K[3] , time ¯T , and gate set 

G = {Init , Term , X∗, CX ∗, CCX ∗}, with input and output dits Kin , K out ⊆ K[3] . For α ∈ { in , out }

and b ∈ [3], define Cα,b as in Equation (19), and let Bα = {b ∈ [3] : Kα ∩ Kb̸ = ∅} be the set of 

b ∈ [3] for which Cα,b ̸ = ∅.We first define a circuit ¯R0 that is a slight modification of ¯R in order to fit the structure needed to apply Lemma 4.3. Specfically, ¯R0 uses space K[3] and time ¯T + 2, with input dits KBin ⊇ Kin 

and output dits KBout ⊇ Kout . We let the first timestep of ¯R0 apply Term to all dits in KBin \ Kin ,and we let the last timestep of ¯R0 apply Init to all dits in KBout \ Kout ; the remaining ¯T timesteps of ¯R0 simply apply ¯R. Therefore ¯R0(·) implements the same function as ¯R(·), except with extra input dits in KBin ⊇ Kin that are ignored, and extra output dits in KBout \ Kout that will always be set to 0. We can then apply Lemma 4.3 to ¯R0 to obtain a circuit ¯R′ = ( ¯R′

> 1

, . . . , ¯R′¯T ′ ) using space K′

> [3]

and time ¯T ′ ≤ O( ¯T · (u log k)2), with input and output dits K′ 

> Bin

, K ′ 

> Bout

respectively, such that ¯R′

has the transversal structure described in Lemma 4.3, and such that for every x ∈ FK′ 

> Bin
> q

we have ¯R′(x) = ( ¯R0(x|KBin ), 0K′  

> Bout \KBout

) = ( ¯R(x|Kin ), 0K′  

> Bout \Kout

) ∈ FK′ 

> Bout
> q

. (29) We show Items 1 and 2 in Theorem 4.1 separately below. In both cases, we construct the desired circuit R by making each timestep of ¯R′ fault-detecting/fault-tolerant using Lemma 3.14, for which purpose we first switch to an associated code using Lemmas 3.11 and 3.12. The main difference between the two cases is in how we prevent error propagation: to prove Item 1 we use Lemma 3.1 to detect errors, whereas to prove Item 2 we use Lemma 3.5 to correct errors. Recall from Section 4.2 that for b ∈ Bα, then Cα,b = C(q, u, n, k, K α ∩ Kb) is simply the subcode of C⊗u 

> 0

given by setting message dits K′ 

> b

\ Kα to 0. As we will see below, this structure is perfectly aligned with the definition of the function ¯R′(·) (Equation (29) above), which simply applies ¯R

while setting dits in K′ 

> Bα

\ Kα to 0. 1. (Mending fault-detecting gadget) We define the desired circuit R as follows. We let R act on dits N ′ = N[3] ⊔ Nanc , where N[3] = ([ n]u)⊔3 as defined above, and Nanc is a set of ancilla dits that can be used by gadgets that we will invoke in the definition of R. By definition, R has input dits NBin ⊆ N[3] and output dits NBout ⊆ N[3] . We let the first timestep of R apply Init 43 to all dits in N[3] \Bin , and we let the last timestep of R apply Term to all dits in N[3] \Bout . To define the remainder of R, for each timestep ¯ t ∈ [ ¯T ′] of ¯R′, we define an associated subcircuit 

R¯t of R, acting on all dits N ′ across O(u2n2) timesteps, as follows. We then let R be the sequential composition 

R = Term ⊔N[3] \Bout ◦R ¯T ′ ◦ · · · ◦ R 1 ◦ Init ⊔N[3] \Bin . (30) By definition, ¯R′ 

> ¯t

has the transversal structure described in Lemma 4.3, meaning that there exists some i ∈ [u] such that either ¯R′ 

> ¯t

consists entirely of X ∗ gates, or else entirely of transversal applications of some gate G ∈ { Init , Term , CX ∗, CCX ∗} to sets K′

> b

|i→j for arbitrary values of b ∈ [3] and j ∈ [k]. Therefore we let R¯t perform the following: (1) For every b ∈ [3], run the error-detecting gadget in Lemma 3.1 for the code C⊗u 

> 0

on dits 

Nb with 

λ(1) in = (n − k′ + 1) u

2

λ(1) out = 8u2n4

ρ(u, n, n − k′ + 1) · λrun 

λ(1) det = ρ(u, n, n − k′ + 1) 8 · λout = u2n4 · λrun .

(2) For every b ∈ [3], run the downwards switching gadget in Item 1 in Lemma 3.11 to unencode from ( C0, Enc 0) in direction i on dits Nb, with 

λ(2) in = λ(1) out 

λ(2) out = n · λ(2) in + 3 n3 · λrun ≤ 11 u2n5

ρ(u, n, n − k′ + 1) · λrun .

Specifically, each application of this gadget takes as input a codeblock of C⊗[u]0 in dits 

Nb, and outputs a codeblock of ( C⊗[u]\{ i} 

> 0

)⊔k′

in dits F 

> j∈[k′]

Nb|i→j .(3) By construction ¯R′ 

> ¯t

consists entirely of gates from one of the five sets {Init }, {Term },

{X∗}, {CX ∗}, or {CCX ∗}, and these gates act transversally on blocks K′

> b

|i→j in all cases except for {X∗}. We treat each case separately: Init : For every b ∈ [3] , j ∈ [k′] for which ¯R′ 

> ¯t

applies transversal Init to dits K′

> b

|i→j , we apply transversal Term followed by Init to dits Nb|i→j .Term : Perform the same procedure as for the Init case above. X∗: For every b ∈ [3] , j ∈ [k′] for which ¯R′ 

> ¯t

applies X a to K′

> b

|i→j for some a ∈ F[k′][u]\{ i} 

> q

,we apply the gadget RXa in Item 3 in Lemma 3.14 to ( C⊗[u]\{ i} 

> 0

, Enc ⊗[u]\{ i} 

> 0

) on dits 

Nb|i→j .CX ∗: For every b1, b 2 ∈ [3] , j 1, j 2 ∈ [k′] for which ¯R′ 

> ¯t

applies CX a to ( C⊗[u]\{ i} 

> 0

, Enc ⊗[u]\{ i} 

> 0

)on dits K′ 

> b1

|i→j1 , K ′ 

> b2

|i→j2 for some a ∈ Fq, we apply the gadget RCX a in Item 4 in Lemma 3.14 to Nb1 |i→j1 , N b2 |i→j2 .44 For each of the four cases Init , Term , X∗, CX ∗ above, we have run a gadget consisting of ≤ 2 timesteps, which is by definition fault-tolerant with input and output error thresholds on each block of the code C⊗[u]\{ i} 

> 0

given by 

λ(3) in = λ(2) out 

λ(3) out = 2 λ(3) in + 2 λrun ≤ 25 u2n5

ρ(u, n, n − k′ + 1) · λrun .

CCX ∗ For every b1, b 2, b 3 ∈ [3] , j 1, j 2, j 3 ∈ [k′] for which ¯R′ 

> ¯t

applies CCX a to K′ 

> b1

|i→j1 ,

K′ 

> b2

|i→j2 , K′ 

> b3

|i→j3 for some a ∈ Fq, we apply: 

• The gadget in Item 1 in Lemma 3.12 with ( Cin , Enc in ) = ( C⊗[u]\{ i} 

> 0

, Enc ⊗[u]\{ i} 

> 0

)and ( Cout , Enc out ) = (( C∗20 )⊗[u]\{ i}, Enc ′

> 0
> ⊗[u]\{ i}

) on dits Nb3 |i→j3 . (Note that here we replace u in the statement of Lemma 3.12 with u − 1.) For symmetry across codeblocks, in parallel we also apply the gadget in Lemma 3.12 with 

Cin = Cout = C⊗[u]\{ i} 

> 0

on dits Nb1 |i→j1 and Nb2 |i→j2 , though this step could be removed. 

• The gadget RCCX a in Item 5 in Lemma 3.14 to ( C⊗[u]\{ i} 

> 0

, Enc ⊗[u]\{ i} 

> 0

) on dits 

Nb1 |i→j1 , N b2 |i→j2 and (( C∗20 )⊗[u]\{ i}, Enc ′

> 0
> ⊗[u]\{ i}

) on dits Nb3 |i→j3 . These codes satisfy the necessary condition in Equation (18) by Claim 4.8. 

• The gadget in Item 1 in Lemma 3.12 with (Cin , Enc in ) = (( C∗20 )⊗[u]\{ i}, Enc ′

> 0
> ⊗[u]\{ i}

)(Cout , Enc out ) = ( C⊗[u]\{ i} 

> 0

, Enc ⊗[u]\{ i} 

> 0

)on dits Nb3 |i→j3 . (Again we replace u in the statement of Lemma 3.12 with 

u − 1.) Again for symmetry, in parallel we also apply the gadget in Lemma 3.12 with Cin = Cout = C⊗[u]\{ i} 

> 0

on dits Nb1 |i→j1 and Nb2 |i→j2 .By Lemmas 3.12 and 3.14 along with Lemmas 2.22 and 2.24, the composition of the gadgets above is a fault-detecting gadget for transversal CCX a on blocks 

Kb1 |i→j1 , K b2 |i→j2 , K b3 |i→j3 , where within each block of the code C⊗[u]\{ i} 

> 0

we can take the input, output, and error detection thresholds to be 

λ(3) in = λ(2) out 

λ(3) out = 8( u − 1) 2n4

ρ(u − 1, n, n − 2k′ + 2) · λrun 

λ(3) det = ( u − 1) 2n4 · λrun 

The upper bounds on the parameters λin , λ run in Equation (13) needed to apply Lemma 3.12 hold by the definition of λrun ≤ ¯λrun in Equation (20). (4) For every b ∈ [3], run the upwards switching gadget in Item 2 in Lemma 3.11 to encode into ( C0, Enc 0) in direction i on dits Nb, with 

λ(4) in = λ(3) out ≤ 25 u2n5

ρ(u, n, n − 2k′ + 2) · λrun 

λ(4) out = n2 · λ(4) in + 3 n3 · λrun ≤ 28 u2n7

ρ(u, n, n − 2k′ + 2) · λrun .

45 Specifically, each application of this gadget takes as input a codeblock of ( C⊗[u]\{ i} 

> 0

)⊔k′

in dits F 

> j∈[k]

Nb|i→j , and outputs a codeblock of C⊗[u]0 in dits Nb.(5) For every b ∈ [3], run the error-detecting gadget in Lemma 3.1 for the code C⊗u 

> 0

on dits 

Nb with 

λ(5) in = λ(4) out 

λ(5) out = 8u2n4

ρ(u, n, n − k′ + 1) · λrun 

λ(5) det = ρ(u, n, n − k′ + 1) 8 · λout = u2n4 · λrun .

The necessary condition λ(5) in ≤ (n−k′+1) u/2 in Equation (3) needed to apply Lemma 3.1 holds by the definition of λrun ≤ ¯λrun in Equation (20). By Equations (20) and (21), it holds that 

λ(1) in ≥ λin + λrun 

λ(1) in ≥ λ(5) out 

λout ≤ λ(5) out + λrun 

λdet ≤ min {λ(1) det , λ (3) det , λ (5) det }.

Therefore we may apply Lemma 2.22 to each sequential composition in the definition of the circuit R in Equation (30), where the above bounds imply that the output error threshold from each gadget in the sequence does not exceed the input error threshold of the subsequent gadget. Thus for α ∈ { in , out } we define the decorated code 

D′ 

> α

= ( C′

> α

, Enc ′

> α

, E′ 

> α

= Eα) = G

> b∈Bα

(C⊗u 

> 0

, Enc ⊗u 

> 0

, 2[n]u

|≥λα ),

so that R, Erun = 2 N ′

|⊔T 

> ≥λrun

, D ′

> in

, D ′

> out

, Edet = G 

> t∈[T]

(2 Edet ,t |≥λdet )

 (31) provides a fault-detecting gadget for the function ¯O′ given by the composition of the functions implemented by the sequence of gadgets in Equation (30), where the sets Edet ,t ⊆ N ′ for 

t ∈ [T ] are simply given by taking the union over b ∈ [3] of the corresponding sets given by the applications of Lemmas 3.1 and 3.12 above. Furthermore, because R1 begins by applying the mending gadget in Lemma 3.1, Lemma 2.22 implies that the gadget in Equation (31) is mending. Here note that the single layer of Init gates in Equation (30) preceding the application of R1 simply adds an additional < λ run errors to the input (accounted for by the bound λ(1) in ≥ λin + λrun above), but otherwise does not affect the mending property. Meanwhile, by construction the function ¯O′ : FK′ 

> Bin
> q

→ FK′ 

> Bout
> q

simply applies the circuit ¯R′

defined previously to its input, except that for every dit that is terminated at some point (and possibly later initialized again), we instead simply set that dit’s value to 0 but leave it 46 as an active dit. This change has no effect on the output, so it follows that ¯O′(·) = ¯R′(·). Therefore by Equation (29), ¯O′(x) = ( ¯R(x|Kin ), 0K′  

> Bout \Kout

) ∈ FK′ 

> Bout
> q

. (32) We will now complete the proof of Item 1 in Theorem 4.1 by showing that the gadget in Equation (22) is mending fault-detecting for ¯R(·), with the desired space and time usage; by definition R uses gate set G. First, we have shown that the gadget in Equation (31) is fault-detecting for ¯O′. Meanwhile, by definition the decorated code Dα is given by restricting D′

> α

to codewords that encode messages whose dits in K′ 

> Bα

\ Kα are set to 0, that is for x ∈ FKα

> q

then Enc α(x) = Enc ′

> α

(x, 0K′  

> Bα \Kα

). (33) Then because Item 1 in Definition 2.18 holds for the gadget in Equation (31), it also holds for the gadget in Equation (22). That is, for every x ∈ FKin  

> q

and every E ∈ E det , we have 

R ◦ Enc in (x) = R ◦ Enc ′

> in

(x, 0K′  

> Bin \Kin

) = Enc ′

> out

( ¯R(x), 0K′  

> Bout \Kout

) = Enc out ( ¯R(x)) ,

where the second equality above holds by Equation (32), and also 

E ∩ supp(tran( R; Enc in (x))) = E ∩ supp(tran( R; Enc ′

> in

(x, 0K′  

> Bin \Kin

))) = ∅.

Similarly, because Item 2 in Definition 2.18 holds for the gadget in Equation (31), it also holds for the gadget in Equation (22). That is, for every x ∈ FKin  

> q

, every Ein = E′

> in

-deviation 

y of Enc in (x) = Enc ′

> in

(x, 0K′  

> Bin \Kin

), and every Erun -avoiding fault F for R, then either 

R[F]( y) is a Eout = E′

> out

-deviation of Enc ′ 

> out

◦ ¯O′(x, 0K′  

> Bin \Kin

) = Enc out ◦ ¯R(x) (where the equality holds by Equations (32) and (33)), or else there exists a set E ∈ E det such that 

E ⊆ supp(tran( R[F]; y)). Therefore the gadget in Equation (22) is fault-detecting for ¯R(·). It similarly follows that because the gadget in Equation (31) is mending, the gadget in Equation (22) is also mending. That is, for every y ∈ FNBin  

> q

and every Erun -avoiding fault F for R, then either there exists 

x′ ∈ FK′ 

> Bin
> q

such that R[F]( y) is a Eout = E′

> out

-deviation of Enc ′ 

> out

◦ ¯O′(x′) = Enc out ◦ ¯R(x′|Kin )(where the equality holds by Equations (32) and (33)), or else there exists a set E ∈ E det such that E ⊆ supp(tran( R[F]; y)). It remains to analyze the space and time usage of R. By by the definition of R in Equation (30) along with Lemmas 3.1, 3.11, 3.12 and 3.14 and Lemmas 2.22 and 2.24, R uses space 

|N ′| ≤ 3 · max {(u + 1) nu, 2nu} = 3( u + 1) nu

and time 

T ≤ 2 + ¯T ′ · (2( un 2 + 2) + 2( n2 + 2) + 2 · 16 u2n2 + 2) 

≤ 2 + O( ¯T · (u log k)2) · (2( un 2 + 2) + 2( n2 + 2) + 2 · 16 u2n2 + 2) 

≤ O( ¯T · u4n2(log n)2),

as desired. 47 2. (Fault-tolerant gadget) The construction of the circuit R and the analysis will be similar as for the mending fault-detecting case above: the main difference is that we use the error-correction gadget Lemma 3.5 in place of the error-detection gadget Lemma 3.1, which in turn chanes the error thresholds throughout. For this purpose, we will apply the decoders for C0

and C∗20 in Fact 4.7. We let ηDec ≥ 1 be a sufficiently large constant such that these decoders have space and time usage 

|NDec |, T Dec ≤ ηDec · n2 log q, (34) i.e. ηDec is an upper bound on the hidden constants in the big-Os in Fact 4.7. Specifically, we define the desired circuit R as follows. We again let R act on dits N ′ =

N[3] ⊔ Nanc , where N[3] = ([ n]u)⊔3 as defined above, and Nanc is a set of ancilla dits that can be used by gadgets that we will invoke in the definition of R. By definition, R has input dits 

NBin ⊆ N[3] and output dits NBout ⊆ N[3] . We again let the first timestep of R apply Init to all dits in N[3] \Bin , and we let the last timestep of R apply Term to all dits in N[3] \Bout . To define the remainder of R, for each timestep ¯ t ∈ [ ¯T ′] of ¯R′, we define an associated subcircuit 

R¯t of R, acting on all dits N ′ across O(u2n2) timesteps, as follows. We then let R be the sequential composition 

R = Term ⊔N[3] \Bout ◦R ¯T ′ ◦ · · · ◦ R 1 ◦ Init ⊔N[3] \Bin . (35) By definition, ¯R′ 

> ¯t

has the transversal structure described in Lemma 4.3, meaning that there exists some i ∈ [u] such that either ¯R′ 

> ¯t

consists entirely of X ∗ gates, or else entirely of transversal applications of some gate G ∈ { Init , Term , CX ∗, CCX ∗} to sets K′

> b

|i→j for arbitrary values of b ∈ [3] and j ∈ [k]. Therefore we let R¯t perform the following: (1) For every b ∈ [3], run the error-correcting gadget in Lemma 3.5 for the code C⊗u 

> 0

on dits 

Nb with 

λ(1) in =

 n − k′ + 1 4

u

λ(1) out =

 2u + 1 (n − k′ + 1) /4n

u

· u2n · (ηDec · n2 log q) · λrun 

≤ ηDec · 2u(u+6) · n3 log( q) · λrun ,

where we use the radius-( n − k′ + 1) /2 decoder for C0 in Fact 4.7. The inequality above holds because k ≤ n/ 32 so ( n − k′ + 1) /4n ≥ 1/8, and because u2 ≤ 22u.(2) For every b ∈ [3], run the downwards switching gadget in Item 1 in Lemma 3.11 to unencode from ( C0, Enc 0) in direction i on dits Nb, with 

λ(2) in = λ(1) out 

λ(2) out = n · λ(2) in + 3 n3 · λrun ≤ ηDec · 2u(u+6)+1 · n3 log( q) · λrun .

Specifically, each application of this gadget takes as input a codeblock of C⊗[u]0 in dits 

Nb, and outputs a codeblock of ( C⊗[u]\{ i} 

> 0

)⊔k′

in dits F 

> j∈[k′]

Nb|i→j .48 (3) By construction ¯R′ 

> ¯t

consists entirely of gates from one of the five sets {Init }, {Term },

{X∗}, {CX ∗}, or {CCX ∗}, and these gates act transversally on blocks K′

> b

|i→j in all cases except for {X∗}. We treat each case separately: Init : For every b ∈ [3] , j ∈ [k′] for which ¯R′ 

> ¯t

applies transversal Init to dits K′

> b

|i→j , we apply transversal Term followed by Init to dits Nb|i→j .Term : Perform the same procedure as for the Init case above. X∗: For every b ∈ [3] , j ∈ [k′] for which ¯R′ 

> ¯t

applies X a to K′

> b

|i→j for some a ∈ F[k′][u]\{ i} 

> q

,we apply the gadget RXa in Item 3 in Lemma 3.14 to ( C⊗[u]\{ i} 

> 0

, Enc ⊗[u]\{ i} 

> 0

) on dits 

Nb|i→j .CX ∗: For every b1, b 2 ∈ [3] , j 1, j 2 ∈ [k′] for which ¯R′ 

> ¯t

applies CX a to ( C⊗[u]\{ i} 

> 0

, Enc ⊗[u]\{ i} 

> 0

)on dits K′ 

> b1

|i→j1 , K ′ 

> b2

|i→j2 for some a ∈ Fq, we apply the gadget RCX a in Item 4 in Lemma 3.14 to Nb1 |i→j1 , N b2 |i→j2 .For each of the four cases Init , Term , X∗, CX ∗ above, we have run a gadget consisting of ≤ 2 timesteps, which is by definition fault-tolerant with input and output error thresholds on each block of the code C⊗[u]\{ i} 

> 0

given by 

λ(3) in = λ(2) out 

λ(3) out = 2 λ(3) in + 2 λrun ≤ ηDec · 2u(u+6)+3 · n3 log( q) · λrun .

CCX ∗ For every b1, b 2, b 3 ∈ [3] , j 1, j 2, j 3 ∈ [k′] for which ¯R′ 

> ¯t

applies CCX a to K′ 

> b1

|i→j1 ,

K′ 

> b2

|i→j2 , K′ 

> b3

|i→j3 for some a ∈ Fq, we apply: 

• The gadget in Item 2 in Lemma 3.12 with ( Cin , Enc in ) = ( C⊗[u]\{ i} 

> 0

, Enc ⊗[u]\{ i} 

> 0

)and ( Cout , Enc out ) = (( C∗20 )⊗[u]\{ i}, Enc ′

> 0
> ⊗[u]\{ i}

) on dits Nb3 |i→j3 . (Note that here we replace u in the statement of Lemma 3.12 with u − 1.) For symmetry across codeblocks, in parallel we also apply the gadget in Lemma 3.12 with 

Cin = Cout = C⊗[u]\{ i} 

> 0

on dits Nb1 |i→j1 and Nb2 |i→j2 , though this step could be removed. 

• The gadget RCCX a in Item 5 in Lemma 3.14 to ( C⊗[u]\{ i} 

> 0

, Enc ⊗[u]\{ i} 

> 0

) on dits 

Nb1 |i→j1 , N b2 |i→j2 and (( C∗20 )⊗[u]\{ i}, Enc ′

> 0
> ⊗[u]\{ i}

) on dits Nb3 |i→j3 . These codes satisfy the necessary condition in Equation (18) by Claim 4.8. 

• The gadget in Item 2 in Lemma 3.12 with ( Cin , Enc in ) = (( C∗20 )⊗[u]\{ i}, Enc ′

> 0
> ⊗[u]\{ i}

)and ( Cout , Enc out ) = ( C⊗[u]\{ i} 

> 0

, Enc ⊗[u]\{ i} 

> 0

) on dits Nb3 |i→j3 . (Again we replace 

u in the statement of Lemma 3.12 with u − 1.) Again for symmetry, in parallel we also apply the gadget in Lemma 3.12 with Cin = Cout = C⊗[u]\{ i} 

> 0

on dits 

Nb1 |i→j1 and Nb2 |i→j2 .In the applications of Lemma 3.12 above, we use the radius ≥ (n − 2k′ + 2) /2decoders for C0 and C∗20 in Fact 4.7, which have space and time usage given by Equation (34). By Lemmas 3.12 and 3.14 along with Lemmas 2.22 and 2.24, the composition of the gadgets above is a fault-tolerant gadget for transversal CCX a on blocks Kb1 |i→j1 , K b2 |i→j2 , K b3 |i→j3 , where within each block of the code C⊗[u]\{ i} 

> 0

we 49 can take the input, output, and error detection thresholds to be 

λ(3) in = λ(2) out 

λ(3) out = ηDec · 2u(u+5) · n3 log( q) · λrun .

The upper bounds on the parameters λin , λ run in Equation (16) needed to apply Lemma 3.12 hold by the definition of λrun ≤ ¯λrun in Equation (23). (4) For every b ∈ [3], run the upwards switching gadget in Item 2 in Lemma 3.11 to encode into ( C0, Enc 0) in direction i on dits Nb, with 

λ(4) in = λ(3) out ≤ ηDec · 2u(u+6)+3 · n3 log( q) · λrun 

λ(4) out = n2 · λ(4) in + 3 n3 · λrun ≤ ηDec · 2u(u+6)+4 · n5 log( q) · λrun .

Specifically, each application of this gadget takes as input a codeblock of ( C⊗[u]\{ i} 

> 0

)⊔k′

in dits F 

> j∈[k]

Nb|i→j , and outputs a codeblock of C⊗[u]0 in dits Nb.(5) For every b ∈ [3], run the error-correcting gadget in Lemma 3.5 for the code C⊗u 

> 0

on dits 

Nb with 

λ(5) in = λ(4) out 

λ(5) out =

 2u + 1 (n − k′ + 1) /4n

u

· u2n · (ηDec · n2 log q) · λrun 

≤ ηDec · 2u(u+6) · n3 log( q) · λrun ,

where we use the radius-( n − k′ + 1) /2 decoder for C0 in Fact 4.7. The necessary condition λ(5) in ≤ (( n − k′ + 1) /4) u in Equation (9) needed to apply Lemma 3.5 holds by the definition of λrun ≤ ¯λrun in Equation (23). By Equations (23) and (24), it holds that 

λ(1) in ≥ λin + λrun 

λ(1) in ≥ λ(5) out 

λout ≤ λ(5) out + λrun .

Therefore we may apply Lemma 2.22 to each sequential composition in the definition of the circuit R in Equation (35), where the above bounds imply that the output error threshold from each gadget in the sequence does not exceed the input error threshold of the subsequent gadget. Thus for α ∈ { in , out } we define the decorated code 

D′ 

> α

= ( C′

> α

, Enc ′

> α

, E′ 

> α

= Eα) = G

> b∈Bα

(C⊗u 

> 0

, Enc ⊗u 

> 0

, 2[n]u

|≥λα ),

so that R, Erun = 2 N ′

|⊔T 

> ≥λrun

, D ′

> in

, D ′

> out

, Edet = G 

> t∈[T]

(2 Edet ,t |≥λdet )

 (36) 50 provides a fault-tolerant gadget for the function ¯O′ given by the composition of the functions implemented by the sequence of gadgets in Equation (35). Note that the single layer of Init gates in Equation (35) preceding the application of R1 simply adds an additional < λ run 

errors to the input, which is accounted for by the bound λ(1) in ≥ λin + λrun above. Meanwhile, by construction the function ¯O′ : FK′ 

> Bin
> q

→ FK′ 

> Bout
> q

simply applies the circuit ¯R′

defined previously to its input, except that for every dit that is terminated at some point (and possibly later initialized again), we instead simply set that dit’s value to 0 but leave it as an active dit. This change has no effect on the output, so it follows that ¯O′(·) = ¯R′(·). Therefore by Equation (29), ¯O′(x) = ( ¯R(x|Kin ), 0K′  

> Bout \Kout

) ∈ FK′ 

> Bout
> q

. (37) We will now complete the proof of Item 2 in Theorem 4.1 by showing that the gadget in Equation (25) is fault-tolerant for ¯R(·), with the desired space and time usage; by definition 

R uses gate set G. First, we have shown that the gadget in Equation (36) is fault-tolerant for ¯O′. Meanwhile, by definition the decorated code Dα is given by restricting D′ 

> α

to codewords that encode messages whose dits in K′ 

> Bα

\ Kα are set to 0, that is for x ∈ FKα 

> q

then Enc α(x) = Enc ′

> α

(x, 0K′  

> Bα \Kα

). (38) Then because Item 1 in Definition 2.17 holds for the gadget in Equation (36), it also holds for the gadget in Equation (25). That is, for every x ∈ FKin  

> q

, we have 

R ◦ Enc in (x) = R ◦ Enc ′

> in

(x, 0K′  

> Bin \Kin

) = Enc ′

> out

( ¯R(x), 0K′  

> Bout \Kout

) = Enc out ( ¯R(x)) ,

where the second equality above holds by Equation (37). Similarly, because Item 2 in Defini-tion 2.17 holds for the gadget in Equation (36), it also holds for the gadget in Equation (25). That is, for every x ∈ FKin  

> q

, every Ein = E′

> in

-deviation y of Enc in (x) = Enc ′

> in

(x, 0K′  

> Bin \Kin

), and every Erun -avoiding fault F for R, then R[F]( y) is a Eout = E′

> out

-deviation of Enc ′ 

> out

◦ ¯O′(x, 0K′  

> Bin \Kin

) = Enc out ◦ ¯R(x),

where the equality holds by Equations (37) and (38). Therefore the gadget in Equation (22) is fault-tolerant for ¯R(·). It remains to analyze the space and time usage of R. By by the definition of R in Equation (35) along with Lemmas 3.5, 3.11, 3.12 and 3.14 and Eq. (34) and Lemmas 2.22 and 2.24, R uses space 

|N ′| ≤ 3 · max {nu−1 · ηDec · n2 log( q), 2nu} = 3 ηDec · log( q) · nu+1 

and time 

T ≤ 2 + ¯T ′ · (2 · u · ηDec · n2 log( q) + 2( n2 + 2) + 2(2 u2 · ηDec · n2 log( q) + 8 un 2) + 2) 

≤ 2 + O( ¯T · (u log k)2) · O(u2n2 log( q)) 

≤ O( ¯T · u4n2(log n)2(log q)) .

as desired. 51 5 Instantiation and Alphabet Reduction 

In this section, we instantiate our schemes in Theorem 4.1 with specific parameters, and show how to reduce the alphabet size down to a constant. By doing so, we obtain schemes for compiling an arbitrary logical circuit with constant alphabet size into a mending fault-detecting or a fault-tolerant physical circuit. If the logical circuit uses space | ¯N |, the physical circuit uses space |N | ≤ | ¯N |1+ ϵ

for an arbitrarily small constant ϵ > 0. The mending fault-detecting circuit can withstand faults with |N |/ poly log |N | errors per timestep, while the fault-tolerant circuit can withstand |N |1−o(1) 

errors per timestep. The formal statements are given below. 

Theorem 5.1 (Main result on fault-detection and fault-tolerance) . The following two statements hold: 1. (Mending fault-detecting gadget) For every prime power r and every 0 < ϵ < 1/8, there exists an ¯N0(r, ϵ ) ≥ 0 such that the following holds. For every set ¯N of size | ¯N | ≥ ¯N0(r, ϵ ) and every 

K ⊆ ¯N , there is an integer M = M (ϵ, ¯N ) ∈ [| ¯N |1+ ϵ, | ¯N |1+4 ϵ] and an [n(r, ϵ, ¯N ), |K|, d (r, ϵ, ¯N )] r

code C(r, ϵ, K, ¯N ) with associated encoding map Enc C(r,ϵ,K, ¯N ), where the |K| message dits are labeled by the set K and 

n(r, ϵ, ¯N ) ≤ log log | ¯N |

ϵ · Md(r, ϵ, ¯N ) ≥ M

232 ,

such that the following statement holds. Let η > 1 be the absolute constant in Lemma 2.5. For every circuit ¯R acting on a set of r-ary dits labeled by ¯N using time ¯T and gate set G = {Init , Term , X∗, CX ∗, CCX ∗} with input and output dits Kin , K out ⊆ ¯N respectively, letting 

Din = ( Cin = C(r, ϵ, K in , ¯N ), Enc in = Enc Cin , Ein = 2 [n(r,ϵ, ¯N )] |≥λin )

Dout = ( Cout = C(r, ϵ, K out , ¯N ), Enc out = Enc Cout , Eout = 2 [n(r,ϵ, ¯N )] |≥λout ),

then for every λrun ∈ [0 , ¯λrun ] with 

¯λrun = ¯λrun (r, ϵ, ¯N ) = M

(log | ¯N |)16 η/ϵ , (39) 

there exists T ∈ N and a set N with subsets Edet ,t ⊆ N for t ∈ [T ] such that there is a mending fault-detecting gadget 

R, Erun = 2 N |≥λrun , D in , D out , Edet = G 

> t∈[T]

(2 Edet ,t |≥λdet )



for ¯R(·), where R is a circuit using space |N | ≤ (log | ¯N |)2 · M , time T ≤ (log | ¯N |)8/ϵ · ¯T , and gate set G, and where 

λin = M

232 

λout = (log | ¯N |)16 η/ϵ · λrun 

λdet = λrun .

(40) 52 2. (Fault-tolerant gadget) For every prime power r, there exists ¯N0(r) ≥ 0 such that the following holds. For every set ¯N of size | ¯N | ≥ ¯N0(r) and every K ⊆ ¯N , there is an integer M =

M ( ¯N ) ∈ [| ¯N |, 212(log | ¯N |)2/3

·| ¯N |] and a [n(r, ¯N ), |K|, d (r, ¯N )] r code C(r, K, ¯N ) with associated encoding map Enc C(r,K, ¯N ), where the |K| message dits are labeled by the set K and 

n(r, ¯N ) ≤ (log | ¯N |) · Md(r, ¯N ) ≥ M

22(log | ¯N |)1/3 ,

such that the following statement holds: For every circuit ¯R acting on a set of r-ary dits labeled by ¯N using time ¯T and gate set 

G = {Init , Term , X∗, CX ∗, CCX ∗} with input and output dits Kin , K out ⊆ ¯N respectively, letting 

Din = ( Cin = C(r, K in , ¯N ), Enc in = Enc Cin , Ein = 2 [n(r, ¯N )] |≥λin )

Dout = ( Cout = C(r, K out , ¯N ), Enc out = Enc Cout , Eout = 2 [n(r, ¯N )] |≥λout ),

then for every λrun ∈ [0 , ¯λrun ] with 

¯λrun = ¯λrun (r, ϵ, ¯N ) = M

232(log | ¯N |)2/3 , (41) 

there exists a fault-tolerant gadget 

 R, Erun = 2 N |≥λrun , D in , D out 



for ¯R(·), where R is a circuit using space |N | ≤ 22(log | ¯N |)2/3

· M , time T ≤ 24(log | ¯N |)2/3

· ¯T ,and gate set G, and where 

λin = M

28(log | ¯N |)1/3

λout = 2 8(log | ¯N |)2/3

· λrun .

(42) 

Furthermore, in both statements above, if r, ϵ are fixed constants, then R can be constructed in 

poly( | ¯N |) time from ¯R.

The remainder of this section is dedicated to proving Theorem 5.1. Our construction of R from ¯R in Theorem 5.1 described below is inherently explicit, as it consists of performing a sequence of explicit operations on tensor products of explicit Reed-Solomon codes. Therefore the claim in Theorem 5.1 that R can be constructed in polynomial time is immediate. In Section 5.1 below, we show how to simulate circuits over a given alphabet size using dits of a different alphabet size. We will use such changes in alphabet size to prove Theorem 5.1 using Theorem 4.1, as the latter provides fault-detecting and fault-tolerance schemes only over alphabets that grow with the circuit’s space usage. Specifically, for both Item 1 and Section 5.3 of Theorem 5.1, given a logical circuit ¯R over r-ary dits, we first simulate ¯R using a circuit over larger 

q-ary dits, where Fq ⊇ Fr is an extension field. We are able to apply Theorem 4.1 to this logical 

q-ary circuit to obtain a physical fault-detecting/fault-tolerant q-ary circuit. We then simulate this physical q-ary circuit using r-ary dits to obtain our desired gadget over r-ary dits. 53 There is one additional complication in proving Item 1 in Theorem 5.1: when simulating the fault-detecting q-ary circuit using an r-ary circuit, to ensure the mending property holds, we must check that the circuit’s input dits lie in the subfield Fr ⊆ Fq. For this purpose, for each logical input x ∈ Fq, we compute xr − x, and check that the result is 0; if this check is violated, then we have detected that x ∈ Fq \ Fr.We formalize these ideas to prove Item 1 and Section 5.3 of Theorem 5.1 in Section 5.2 and Section 5.3 below, respectively. 

5.1 Increasing and Reducing Alphabet Size 

In this section, we show how to simulate circuits over a small alphabet using a larger alphabet, and vice versa. Throughout this section, for an r-ary gate G = Gr ∈ { Init , Term , X∗, CX ∗, CCX ∗}

and a field extension Fq = Frκ ⊇ Fr, we let Gq denote the q-ary version of G. For instance, if 

Gr = CX a : F2 

> r

→ F2 

> r

for a ∈ Fr, then Gq = CX a : F2 

> q

→ F2 

> q

with the same a ∈ Fr ⊆ Fq.Lemma 5.2 below shows that we can replace every r-ary gate with the analogous q-ary gate, while preserving the gate’s functionality on inputs in the subfield Fr ⊆ Fq.

Lemma 5.2 (Increase alphabet size) . Let ι : Fr ,→ Fq be a field inclusion. For every a ∈ Fr and every gate G ∈ { Init , Term , Xa, CX a, CCX a} with min input dits and mout output dits, then 

Gq ◦ ι⊔min = ι⊔mout ◦ Gr.

Proof. The result follows immediately by the definition of the gates Init , Term , Xa, CX a, CCX a, as restricting the q-ary version of each of these gates to the subfield Fr ⊆ Fq simply yields the r-ary version of the gate. Lemma 5.3 below shows that we can every q-ary gate with a small r-ary circuit, while preserving the circuit’s functionality up to an isomorphism Fκr ∼= Fq.

Lemma 5.3 (Reduce alphabet size) . Let r be a prime power, and κ ∈ N. Let q = rκ, and let ϕ : Fκr

> ∼

−→ Fq be an arbitrary fixed Fr-linear isomorphism. Then for every gate G ∈ G := 

{Init , Term , X∗, CX ∗, CCX ∗} over q-ary dits, there exists a circuit R = RG using gate set G over 

r-ary dits, using space |N | = κ · max {min , m out } and time T = κ3, such that 

ϕ⊔mout ◦ R = G ◦ ϕ⊔min , (43) 

where min and mout denote the number of input and output dits in G, respectively, so that R has 

κ · min input dits and κ · mout output dits. Proof. If G = Init or G = Term, we simply let R apply G⊔κ. If G = X a for a ∈ Fq, we simply let 

R apply X ϕ−1(a).If G = CX a for a ∈ Fq, then (ϕ−1)⊔2 ◦ CX a ◦ϕ⊔2(x1, x 2) = ( x1, x 2 + ϕ−1(aϕ (x1))) where x1 7 → ϕ−1(aϕ (x1)) is a linear map on Fκr , so that there exists a matrix A ∈ F[κ]×[κ] 

> r

such that 

Ax 1 = ϕ−1(aϕ (x1)). Then for every ( i, j ) ∈ [κ]2, we let R apply a CX Ai,j gate from dit j of x1 to dit i of x2; these κ2 gates together implement the function ( x1, x 2) 7 → (x1, x 2 + Ax 1). 54 If G = CCX a for a ∈ Fq, then (ϕ−1)⊔3 ◦ CCX a ◦ϕ⊔3(x1, x 2, x 3) = ( x1, x 2, x 3 + ϕ−1(aϕ (x1)ϕ(x2))) where ( x1, x 2) 7 → ϕ−1(aϕ (x1)ϕ(x2)) is a bilinear map on Fκr × Fκr . Because every such bilinear map factors through the tensor product Fκr ⊗ Fκr = F[κ]2 

> q

, there exists a matrix B ∈ F[κ]×[κ]2 

> r

such that 

B(x1 ⊗ x2) = ϕ−1(aϕ (x1)ϕ(x2)). Then for every ( i, j, k ) ∈ [κ]3, we let R apply CCX Bi, (j,k ) to dit 

j of x1, dit k of x2, and dit i of x3; these κ3 gates together implement the function ( x1, x 2, x 3) 7 →

(a1, x 2, x 3 + B(x1 ⊗ x2)). For every gate G ∈ G acting on m = max {min , m out } q-ary dits, the associated circuit R = RG

defined above by construction satisfies Equation (43), acts on |N | = κ · m r -ary dits, and uses time 

≤ κ3. We may add additional idling timesteps to use time exactly T = κ3 for all G, as desired. 

5.2 Mending Fault-Detecting Gadget 

In this section, we prove Item 1 in Theorem 5.1. Here, all logarithms should be assumed to be base 2 unless explicitly stated otherwise. In this section, we will also need the following extended version of Definition 2.18, which describes gadgets over an alphabet Fq that implement functions ¯O acting on a smaller alphabet Fr ⊆ Fq:

Definition 5.4. Let Fr ⊆ Fq be a subfield. Let R be a circuit over q-ary dits with input dits 

Nin and output dits Nout . For α ∈ { in , out }, let Dα be a [|Nα|, k α]q decorated code. We say 

(R, Erun , D in , D out , Edet ) forms a fault-detecting gadget for a set ¯O of functions ¯O : Fkin  

> r

→ Fkout  

> r

if it satisfies Item 1 and Item 2 in Definition 2.18, but where we only consider every x ∈ Fkin  

> r

instead of x ∈ Fkin  

> q

. Similarly, we say the gadget is furthermore mending if either Item (c) or Item (d) in Definition 2.18 holds, but where in Item (c) we require x′ ∈ Fkin  

> r

⊆ Fkin  

> q

instead of x ∈ Fkin  

> q

.Proof of Item 1 in Theorem 5.1. At various points throughout the proof below, we will require that ¯N0(r, ϵ ) be sufficiently large compared to r, 1/ϵ so that certain bounds on | ¯N | that hold asymptotically in fact hold for every | ¯N | ≥ ¯N0(r, ϵ ). The final value of ¯N0(r, ϵ ) will then implicitly be given by the maximum of the values required in these various instances. Given the set ¯N , we first let ¯N ′ = ¯N × [4] ∼= ¯N ⊔4. We then define 4

n =

$ ϵ

1 − 2ϵ · log | ¯N ′|

log log | ¯N ′|

1/ϵ %

k = ⌊n1−ϵ⌋

u = ⌊nϵ⌋

κ = ⌈log( n)/ log( r)⌉

q = rκ.

(44) We let 

M = M (ϵ, ¯N ) = nu,     

> 4Note that the variable nhere is distinct from n(r, ϵ, ¯N) in the statement of Theorem 5.1.

55 so that if | ¯N | ≥ ¯N0(r, ϵ ) is sufficiently large, then 

M ≥ 2(nϵ−1) log n

≥ 2 ϵ              

> 1−ϵ·log |¯N′|
> log log |¯N′|·1
> ϵlog log |¯N′|

≥ | ¯N ′|1+ ϵ

≥ | ¯N |1+ ϵ

and 

M ≤ 2nϵ log n

≤ 2 ϵ              

> 1−2ϵ·log |¯N′|
> log log |¯N′|·1
> ϵlog log |¯N′|

≤ 2 log | ¯N |

> 1−1.9ϵ

≤ | ¯N |1+4 ϵ,

where the final inequality above uses the assumption that 0 < ϵ < 1/8. Thus M ∈ [| ¯N |1+ ϵ, | ¯N |1+4 ϵ], as desired. Furthermore, assuming | ¯N | ≥ ¯N0(r, ϵ ) is sufficiently large, then 

ku ≥ 2(nϵ−1) log (n1−ϵ−1)

≥ 2 ϵ              

> 1−1.9ϵ·log |¯N′|
> log log |¯N′|·(1 −ϵ)·1
> ϵlog log |¯N′|

≥ | ¯N ′|

Therefore we may fix an arbitrary set inclusion ¯N ′ ,→ [k]u, so that we may view ¯N ′ as a subset of [k]u. Also if | ¯N | ≥ ¯N0(r, ϵ ) is sufficiently large, then we have u ≥ 4, k ≤ n/ 32, and q ≥ n. Thus for K ⊆ ¯N ′ ⊆ [k]u, the inequalities above ensure that the code C(q, u, n, k, K ) with encoding map Enc C(q,u,n,k,K ) in Theorem 4.1 is well-defined. By Theorem 4.1, C(q, u, n, k, K ) has parameters [nu, |K|, ≥ (n − 8k + 1) u]q.To prove Item 1 in Theorem 5.1, we will first construct fault-detecting gadgets over the alphabet 

Fq, before reducing the alphabet to Fr using Lemma 5.3. For this purpose, we first construct a circuit ¯R1 acting on q-ary dits labeled by ¯N ′ = ¯N × [4] ∼=¯N ⊔4 as follows. ¯R1 has input dits Kin × { 1} and output dits Kin × { 1, 2}. The first timestep of ¯R1 applies Init gates to all dits in Kin × [4], and the last timestep of ¯R1 applies Term gates to all dits in Kin × { 3, 4}. On input x ∈ FKin  

> q

, for each j ∈ Kin , the remaining timesteps of ¯R1 use dits 

{j} × [4] to perform repeated squaring in order to compute xrj − xj , and output this value in dit (j, 2); the output in dit ( j, 1) is simply the input value xj . Note that this value xrj − xj equals 0 iff 

xj lies in the subfield Fr ⊆ Fq; below we will apply Theorem 4.1 to ¯R1, and use these values xrj − xj

to check that the logical inputs xj lie in the subfield Fr, in order to ultimately obtain a mending gadget over r-ary dits. Specifically, for each j ∈ Kin , ¯R1 initializes dits {j} × { 2, 3, 4} to value 0; the value xj of dit (j, 1) is never changed. ¯R1 then performs CX 1 to copy value xj into dit ( j, 2). ¯R1 then loops through the bits in the binary representation of r from least to most significant; for each bit equal to 0, then ¯R1 squares the value in dit ( j, 2), while for each bit equal to 1, then ¯R1 squares the value in dit ( j, 2) and subsequently multiplies xj (from dit ( j, 1)) into the value in dit ( j, 2). Upon 56 completion of this procedure, by definition dit ( j, 2) will contain xrj . ¯R1 then performs CX −1 on dits ( j, 1) , (j, 2) to compute xrj − xj in dit ( j, 2). Each squaring and multiplication operation can be performed using a sequence of ≤ 10 gates in G acting on the dits {j} × [4], where dits {j} × { 3, 4}

are used as ancillas into which values can be copied using CX ∗ gates (after resetting the ancillas to 0 using Term and Init), and multiplication (and therefore also squaring) is performed using CCX ∗

gates. Thus ¯R1 uses space ¯N ′ and time ¯T 1 ≤ 16 log r.Let ˜λrun = 3 κ3 · λrun 

˜λin = (n − 8k + 1) u

4˜λout = 16 u2n4

ρ(u, n, n − 8k + 1) · ˜λrun 

˜λdet = ( u − 1) 2n4 · ˜λrun 

(45) and for α ∈ { in , out } let ˜Dα = ( C(q, u, n, k, K α), Enc C(q,u,n,k,K α), 2[n]u

|≥˜λα )˜D′ 

> α

= ( C(q, u, n, k, K α), Enc C(q,u,n,k,K α), 2[n]u

|≥˜λα−2˜λrun ).

Assuming | ¯N | ≥ ¯N0(r, ϵ ) is sufficiently large, we have (n − 16 k + 2) u ≥ M ·



1 − 16 

nϵ

nϵ

≥ M

264 ,

so the expression ¯λrun (u, n ) in Equation (20) satisfies ¯λrun (u, n ) ≥

 (n − 16 k + 2) u

un · nu

η

· (n − 16 k + 2) u−1

210 · u2n8

≥ M

(un · 264 )η · 274 · u2n9

≥ M

(log | ¯N |)15 η/ϵ 

≥ 3κ3 · ¯λrun (r, ϵ, ¯N ),

(46) where the final equality above holds by Equations (39) and (44). Then because ˜λrun = 3 κ3 · λrun ≤

3κ3 · ¯λrun (r, ϵ, ¯N ), we have ˜λrun ≤ ¯λrun (u, n ). By Equations (20), (45) and (46), we also have ˜λout + ˜λrun ≤ 32 u2n4

ρ(u, n, n − 8k + 1) · ¯λrun (u, n )= 32 u2n4

ρ(u, n, n − 8k + 1) · ρ(u, n, n − 16 k + 2) · (n − 16 k + 2) u−1

210 · u2n8

≤ (n − 16 k + 2) u−1

25n4

≤ ˜λin .

57 Therefore by Item 1 in Theorem 4.1, there exists a circuit ˜R1 using space | ˜N 1| ≤ 3( u + 1) nu,time ˜T 1 ≤ O( ¯T 1 · u4n2(log n)2) ≤ O((log r) · u4n2(log n)2), and gate set G, and there exist subsets ˜E1det ,˜t ⊆ ˜N 1 for ˜ t ∈ ˜T 1, such that 

 ˜R1, ˜E1run = 2 ˜N 1

|⊔ ˜T 1 

> ≥˜λrun

, ˜Din , ( ˜D′

> in

)⊔2, ˜E1det = G 

> ˜t∈[ ˜T1]

(2 ˜E1det ,˜t |≥˜λdet )

 (47) is a mending fault-detecting gadget for ˜¯O1(·) = ¯R1(·). Specifically, the two output blocks of ˜D′

> in

above encode output dits Kin × { 1} and Kin × { 2} of ¯R1, respectively. Here we may use ˜Din for the output blocks because ˜λout ≤ ˜λin − 2˜λrun as shown above. Let ( ˜R1)′ be the circuit that first runs ˜R1, and then has two additional timesteps: timestep ˜T 1 + 1 applies identity gates, and timestep ˜T 1 + 2 applies Term gates to all dits in Kin × { 2}. Let ˜E1det , ˜T 1+1 = [ n]u × { 2} and ˜E1det , ˜T 1+2 = ∅, where [ n]u × { 2} ⊆ ˜N 1 denotes the second of the two output blocks of ˜R1.

Claim 5.5. The data 

( ˜R1)′, ( ˜E1)run = 2 ˜N 1

|⊔ ˜T 1+2 

> ≥˜λrun

, ˜Din , ˜Din , ( ˜E1)′ 

> det

= G 

> ˜t∈[ ˜T1+2]

(2 ˜E1det ,˜t |≥˜λdet )

 (48) 

forms a mending fault-detecting gadget (in the sense of Definition 5.4) for the identity map ( ¯O1)′ =

Ir : FKin  

> r

→ FKin  

> r

on r-ary dits labeled by Kin .Proof. In the absence of errors, the second output block of ˜R1(x) on input x ∈ FKin  

> q

contains an encoding of the values xrj − xj for every j ∈ Kin . These values equal 0 iff xj ∈ Fr. Thus the gadget in Equation (48) is fault-detecting because the gadget in Equation (47) is fault-detecting. Note that the output error threshold of ˜D′ 

> in

is ˜λin − 2˜λrun , and hence the two additional timesteps at the end of ( ˜R1)′ bring the error threshold up to the error threshold ˜λin of Din .To see that the gadget in Equation (48) is mending, consider an arbitrary input y ∈ Fnu 

> q

and a ˜E1run -avoiding fault F for ( ˜R1)′. By the mending property of the gadget in Equation (47), either there exists x′ ∈ FKin  

> q

such that ( ˜R1)′[F]( y) is a ˜Ein -deviation of Enc C(q,u,n,k,K )(x′), or else there exists a set E ∈ ˜E1det such that E ⊆ supp(tran( ˜R1[F]; y)). If the first case holds with x′ ∈ FKin  

> r

⊆ FKin  

> q

,or if the second case holds, then the desired mending property in the sense of Definition 5.4 holds. The only remaining possibility is if the first case holds, but for some x′ ∈ FKin  

> q

\ FKin  

> r

.To analyze this possiblity, define x′′ ∈ FKin  

> q

by x′′  

> j

= ( x′ 

> j

)r − x′ 

> j

. Then x′′ ̸ = 0, and the second output block of ˜R1(x′) differs from Enc C(q,u,n,k,K )(x′′ ) by an error of weight < ˜λin − 2˜λrun .As Enc C(q,u,n,k,K )(x′′ ) is a nonzero codeword of C(q, u, n, k, K ), its weight is at least the code’s distance ( n − 8k + 1) u. Hence the support E of the restriction of tran(( ˜R1)′[F]; y) to dits ˜E1det , ˜T 1+1 

at timestep ˜T 1 + 1 has weight at least 

|E| ≥ (n − 8k + 1) u − ˜λin ≥ (n − 8k + 1) u

2 ≥ ˜λdet ,

where the first inequality above holds by Equation (45), and the second inequality holds because by Equations (20) and (46), ˜λdet ≤ (u − 1) 2n4 · ¯λrun (u, n ) ≤ (n − 8k + 1) u

2 .

58 Thus E ∈ ( ˜E1)′

> det

, completing the proof of the mending property (in the sense of Definition 5.4). Now we let ¯R2 denote the circuit obtained from ¯R by replacing every r-ary gate with the analogous q-ary gate. Therefore ¯R2 acts on q-ary dits, but otherwise has the same space usage ¯N ∼= ¯N × { 1} ⊆ ¯N ′ and time usage ¯T as ¯R. Therefore by Item 1 in Theorem 4.1, there exists a circuit ˜R2 using space | ˜N 2| ≤ 3( u + 1) nu, time ˜T 2 ≤ O( ¯T · u4n2(log n)2), and gate set G, and there exist subsets ˜E2det ,˜t ⊆ ˜N 2 for ˜ t ∈ ˜T 2, such that 

 ˜R2, ˜E2run = 2 ˜N 2

|⊔ ˜T 2 

> ≥˜λrun

, ˜Din , ˜Dout , ˜E2det = G 

> ˜t∈[ ˜T2]

(2 ˜E2det ,˜t |≥˜λdet )

 (49) is a mending fault-detecting gadget for ˜¯O2(·) = ¯R2(·) : FKin  

> q

→ FKout  

> q

. Letting ι : Fr ,→ Fq denote the inclusion, then by Lemma 5.2 we have ¯R2 ◦ ι⊔Kin = ι⊔Kout ◦ ¯R, so the gadget above is therefore also a (non-mending) fault-detecting gadget for ¯R(·) : FKin  

> r

→ FKout  

> r

in the sense of Definition 5.4. 

Claim 5.6. The sequential composition 

 ˜R = ˜R2 ◦ ( ˜R1)′, ˜Erun = ˜E2run ⊔ ( ˜E1)run , ˜Din , ˜Dout , ˜Edet = ˜E2det ⊔ ( ˜E1)′

> det



(50) 

of the gadgets in Equations (48) and (49) forms a mending fault-detecting gadget for ˜¯O(·) = ¯R(·) : 

FKin  

> r

→ FKout  

> r

, using space | ˜N | = max {| ˜N 1|, | ˜N 2|} ≤ 3( u + 1) nu, time ˜T = ˜T 1 + 2 + ˜T 2 ≤

O(( ¯T + log r) · u4n2(log n)2), and gate set G over q-ary dits. Proof. The result holds by the same reasoning used to prove Lemma 2.22, but applied to gadgets in the form of Definition 5.4 in which the physical circuit over q-ary dits implements a function over smaller r-ary dits. The proof in this case is essentially the same as that of Lemma 2.22, so we omit the details to avoid redundancy. To complete the proof of Item 1 in Theorem 5.1, we will apply Lemma 5.3 to reduce the alphabet size of the physical dits of ˜R in the gadget in Equation (50) from q to r; the desired mending fault-detecting property over r-ary dits will follow from Claim 5.6. Specifically, fix an Fr-linear isomorphism ϕ : Fκr → Fq such that that first input component maps to the subfield Fr ⊆ Fq,that is, ϕ|Fr ×{ 0}κ−1 = ι. For K ⊆ ¯N = ¯N × { 1} ⊆ ¯N ′ ⊆ [k]u, we obtain the code C(r, ϵ, K, ¯N ) with encoding map Enc C(r,ϵ,K, ¯N ) in Item 1 in Theorem 5.1 by restricting C(q, u, n, k, K ) to messages in 

FKr ⊆ FKq and applying ϕ−1 to all codeword dits, that is, Enc C(r,ϵ,K, ¯N ) = ( ϕ−1)⊔[n]u

◦ Enc C(q,u,n,k,K ) ◦ι⊔K

C(r, ϵ, K, ¯N ) = Enc C(r,ϵ,K, ¯N )(FKr ).

Then C(r, ϵ, K, ¯N ) is a code over r-ary dits with length 

n(r, ϵ, ¯N ) = κ · nu ≤ log log | ¯N |

ϵ · M

dimension |K|, and distance d(r, ϵ, ¯N ) at least the distance of C(q, u, n, k, K ), so that 

d(r, ϵ, ¯N ) ≥ (n − 8k + 1) u ≥ M ·



1 − 8

nϵ

nϵ

≥ M

232 .

59 Now we define a circuit R on r-ary dits using space N = [ κ] × ˜N and time T = κ3 · ˜T as follows: for each q-ary gate G ∈ G acting on some set of dits B ⊆ ˜N in some timestep ˜ t ∈ [ ˜T ] of ˜R, we let R apply the corresponding r-ary circuit RG from Lemma 5.3 to dits [ κ] × B in timesteps 

{κ3(˜ t − 1) + 1 , . . . , κ 3˜t} ⊆ [T ]. By construction, ˜Edet is of the form ˜Edet = F˜t∈[t](2 ˜Edet ,˜t |≥˜λdet ) for some subsets ˜Edet ,˜t ⊆ ˜N for ˜t ∈ [ ˜T ]. Then for t ∈ [T ], we define 

Edet ,t =

(

[κ] × ˜Edet ,t/κ 3 , t ∈ κ3 · [ ˜T ]

∅, t / ∈ κ3 · [ ˜T ]. (51) Assuming | ¯N | ≥ ¯N0(r, ϵ ) is sufficiently large, then by the bounds on | ˜N |, ˜T in Claim 5.6, the space and time usages of R satisfy 

|N | = κ · | ˜N |≤ κ · 3( u + 1) nu

≤ 1

ϵ (log log | ¯N |) · log | ¯N | · M

≤ (log | ¯N |)2 · MT = κ3 · ˜T

≤ O(κ3 · ( ¯T + log r) · u4n2(log n)2)

≤ ¯T · (log | ¯N |)8/ϵ ,

where the above inequalities apply the bounds in Equation (44). We will now complete the proof of Item 1 in Theorem 5.1 by showing the following. 

Claim 5.7. The data 

R, Erun = 2 N |≥λrun , D in , D out , Edet = G 

> t∈[T]

(2 Edet ,t |≥λdet )

 (52) 

forms a mending fault-detecting gadget for ¯R(·), where Din , D out and λrun , λ in , λ out , λ det are defined as in Item 1 in Theorem 5.1. 

To prove Claim 5.7, we will use the following bounds relating the variables defined in Equa-tion (45) to those defined in Equation (40), where below we assume | ¯N | ≥ ¯N0(r, ϵ ) is sufficiently 60 large: ˜λin ≥ M

4 ·



1 − 8

nϵ

nϵ

≥ M

232 = λin 

˜λout = 16 u2n4 · (un )η

 nn − 8k + 1 

u·η

· ˜λrun 

≤ 16 n10 η ·



1 + 10 

nϵ

nϵ·η

· ˜λrun 

≤ n12 η · (3 κ3 · λrun )

≤ (log | ¯N |)16 η/ϵ · λrun 

κ

= λout 

κ

˜λdet = ( u − 1) un4 · (3 κ3 · λrun ) ≥ λrun = λdet .

(53) 

Proof of Claim 5.7. We begin with the proof of the (non-mending) fault-detecting property. First, Item 1 in Definition 2.18 holds for the gadget in Equation (52) by Claim 5.6 and Lemma 5.3. Specifically, in the absence of errors, then R simply runs ˜R, but expresses each q-ary dit using 

κ r -ary dits according to the isomorphism ϕ, and spends κ3 timesteps simulating each q-ary gate 

G using a circuit RG of r-ary gates. At every timestep t that is a multiple of κ3, so that a layer of q-ary gates has just finished being simulated, the detector set Edet ,t simply contains all κ of the r-ary dits associated to every q-ary dit in the corresponding detector set ˜Edet ,t/κ 3 . Therefore when we run R on input y = Enc C(r,ϵ,K in , ¯N )(x) for x ∈ FKin  

> r

, every dit in every detector set Edet ,t 

has value 0 (because when running ˜R on input Enc C(q,u,n,k,K in )(x), every dit in every detector set ˜Edet ,t/κ 3 must have value 0), and we must have 

R(y) = ( ϕ−1)⊔[n]u

◦ ˜R ◦ Enc C(q,u,n,k,K in )(x)= ( ϕ−1)⊔[n]u

◦ Enc C(q,u,n,k,K out ) ◦ ¯Rq(x)= Enc C(r,ϵ,K out , ¯N ) ◦ ¯Rr(x).

We now similarly show that Item 2 in Definition 2.18 holds for the gadget in Equation (52) by using the fact that this property holds for the gadget in Equation (50). Let x ∈ FKin  

> r

, let y

be an input to R that differs from Enc C(r,ϵ,K in , ¯N )(x) by an error of weight < λ in , and let F be a fault with weight < λ run in each timestep. We want to show that either R[F]( y) differs from Enc C(r,ϵ,K out , ¯N ) ◦ ¯R(x) by an error of weight < λ out , or else that there exists some t ∈ [T ] such that 

| tran( R[F]; y)|Edet ,t ×{ t}| ≥ λdet .By definition, ˜ y := ϕ⊔[n]u

(y) differs from Enc C(q,u,n,k,K in )(x) = ϕ⊔[n]u

◦Enc C(r,ϵ,K in , ¯N )(x) at < λ in 

dits, and λin ≤ ˜λin by Equation (53). Furthermore, R[F]( y) simulates the execution of ˜R[ ˜F](˜ y) for an appropriate fault ˜F. Specifically, for ˜ t ∈ [ ˜T ], we have tran( R[F]; y)|N ×{ κ3·˜t} = ϕ⊔ ˜N (tran( ˜R[ ˜F]; ˜ y)| ˜N ×{ ˜t}), (54) where ˜F is an appropriate fault on ˜R that simulates the effect of the fault F on R. For every gate G acting on some dits B ⊆ ˜N in some timestep ˜ t ∈ [ ˜T ] of ˜R, if F has nontrivial support on 61 the restriction to the associated subcircuit RG of R, then at timestep ˜ t we let ˜F set the value of dits B to equal ( ϕ−1)⊔3(tran( R[F]; y)|[κ]×B×{ κ3·˜t}). If instead F has no support on the restriction to this subcircuit RG of R, then ˜F has no support on dits B in timestep ˜ t. Because every gate 

G ∈ G in our gate set has |B| ≤ 3, for each ˜ t ∈ [ ˜T ], each of the κ3 timesteps {κ3(˜ t − 1) + 1 , . . . , κ 3˜t}

of R[F] can induce errors on up to 3 λrun dits in ˜R[ ˜F]. Therefore ˜F has weight < ˜λrun = 3 κ3 · λrun 

in timestep ˜ t (see Equation (45)). Thus by Claim 5.6, either ˜R[ ˜F](˜ y) differs from Enc C(q,u,n,k,K out ) ◦ ¯R(x) = ϕ⊔[n]u

◦ Enc C(r,ϵ,K out , ¯N ) ◦ ¯R(x)at < ˜λout dits, or else there exists ˜ t ∈ [ ˜T ] such that | tran( ˜R[ ˜F]; ˜ y)| ˜Edet ,˜t×{ ˜t}| ≥ ˜λdet . In the first case, by Equations (53) and (54), we conclude that R[F]( y) differs from Enc C(r,ϵ,K out , ¯N ) ◦ ¯R(x) at 

< κ · ˜λout ≤ λout dits. In the latter case, by Equations (53) and (54), then there exists t = κ3 ·˜t ∈ [T ]such that | tran( R[F]; y)|Kdet ,t ×{ t}| ≥ ˜λdet ≥ λdet . Thus Item 2 in Definition 2.18 holds for the gadget in Equation (52). We have now shown that the gadget in Equation (52) is fault-detecting; we will next show that it is mending. The proof is similar as the proof of fault-detecting above. For an arbitrary input 

y ∈ F[κ]×[n]u 

> r

and a fault F with weight < λ run in each timestep, we want to show that either there exists x′ ∈ FKin  

> r

such that R[F]( y) differs from Enc C(r,ϵ,K out , ¯N ) ◦ ¯R(x′) at < λ out dits, or else that there exists some t ∈ [T ] such that | tran( R[F]; y)|Edet ,t ×{ t}| ≥ λdet . As in the non-mending case above, we again let ˜ y := ϕ⊔[n]u

(y), and we construct a fault ˜F with weight < ˜λrun in each timestep, such that Equation (54) holds. By Claim 5.6, the gadget in Equation (50) is mending (in the sense of Definition 5.4), so either there exists x′ ∈ FKin  

> r

such that ˜R[ ˜F](˜ y) differs from Enc C(q,u,n,k,K out ) ◦ ¯R(x′) = ϕ⊔[n]u

◦ Enc C(r,ϵ,K out , ¯N ) ◦ ¯R(x′) at < ˜λout dits, or else there exists ˜ t ∈ [ ˜T ]such that | tran( ˜R[ ˜F]; ˜ y)| ˜Edet ,˜t×{ ˜t}| ≥ ˜λdet . In the first case, by Equations (53) and (54), we conclude that R[F]( y) differs from Enc C(r,ϵ,K out , ¯N ) ◦ ¯R(x′) at < κ · ˜λout ≤ λout dits. In the latter case, by Equations (53) and (54), then there exists t = κ3 · ˜t ∈ [T ] such that | tran( R[F]; y)|Kdet ,t ×{ t}| ≥ 

˜λdet ≥ λdet . Thus the gadget in Equation (52) is mending, as desired. 

5.3 Fault-Tolerant Gadget 

In this section, we prove Item 2 of Theorem 5.1. The proof is similar as the proof of Item 1 of Theorem 5.1 in Section 5.2 above, except we now will apply Item 2 (instead of Item 1) of Theorem 4.1. The main differences in the proof are therefore that the values of parameters change (due to the differences in parameters between Item 1 and Item 2 of Theorem 4.1), and that we do not prove the mending property here (so some steps are simplified). In the proof below, we will use the following basic fact: 

Fact 5.8. For every a ≥ 0 and every 0 ≤ ∆a ≤ a, then 

a3/2 + 32 a1/2∆a ≤ (a + ∆ a)3/2 ≤ a3/2 + 3 a1/2∆a. 

Proof. Letting f (a) = a3/2, then f ′(a) = (3 /2) a1/2 ≥ 0 and f ′′ (a) = (3 /4) a−1/2 ≥ 0. Therefore 

f (a + ∆ a) ≥ f (a) + f ′(a)∆ a and f (a + ∆ a) ≤ f (a) + f ′(a + ∆ a)∆ a ≤ f (a) + f ′(2 a)∆ a, from which the desired inequalities follow. 62 Proof of Item 2 in Theorem 5.1. At various points throughout the proof below, we will require that ¯N0(r) be sufficiently large compared to r so that certain bounds on | ¯N | that hold asymptotically in fact hold for every | ¯N | ≥ ¯N0(r). The final value of ¯N0(r) will then implicitly be given by the maximum of the values required in these various instances. Given the set ¯N , we define 5

n = 2 ⌊(log | ¯N |)2/3+4(log | ¯N |)1/3⌋

k = ⌊n/ 32 ⌋

u = ⌊plog n⌋

κ = ⌈log( n)/ log( r)⌉

q = rκ.

(55) We let 

M = M ( ¯N ) = nu,

so that if | ¯N | ≥ ¯N0(r) is sufficiently large, then 

M ≥ ku ≥ 2((log | ¯N |)2/3+2(log | ¯N |)1/3)·(√(log | ¯N |)2/3+2(log | ¯N |)1/3−1) 

= 2 ((log | ¯N |)2/3+2(log | ¯N |)1/3)3/2−((log | ¯N |)2/3+2(log | ¯N |)1/3)

≥ 2(log | ¯N |+3(log | ¯N |)2/3)−3(log | ¯N |)2/3

= | ¯N |,

(56) where the second inequality above holds by Fact 5.8. Similarly, again assuming | ¯N | ≥ ¯N0(r) is sufficiently large, 

M ≤ 2((log | ¯N |)2/3+4(log | ¯N |)1/3)·√(log | ¯N |)2/3+4(log | ¯N |)1/3

= 2 ((log | ¯N |)2/3+4(log | ¯N |)1/3)3/2

≤ 2log | ¯N |+12(log | ¯N |)2/3

= | ¯N | · 212(log | ¯N |)2/3

,

where the third inequality above holds by Fact 5.8. Thus M ∈ [| ¯N |, 212(log | ¯N |)2/3

· | ¯N |], as desired. Meanwhile because ku ≥ | ¯N | by Equation (56), we may fix an arbitrary set inclusion ¯N , → [k]u,so that we may view ¯N as a subset of [ k]u. Also if | ¯N | ≥ ¯N0(r) is sufficiently large, then we have 

u ≥ 4, k ≤ n/ 32, and q ≥ n. Thus for K ⊆ ¯N ′ ⊆ [k]u, the inequalities above ensure that the code 

C(q, u, n, k, K ) with encoding map Enc C(q,u,n,k,K ) in Theorem 4.1 is well-defined. By Theorem 4.1, 

C(q, u, n, k, K ) has parameters [ nu, |K|, ≥ (n − 8k + 1) u]q.To prove Item 2 in Theorem 5.1, we will first construct fault-tolerant gadgets over the alpha-bet Fq, before reducing the alphabet to Fr using Lemma 5.3. Specifically, let ηDec be the absolute 

> 5

Note that the variable n here is distinct from n(r, ¯N ) in the statement of Theorem 5.1. 

63 constant defined in Item 2 in Theorem 4.1, and let ˜λrun = 3 κ3 · λrun 

˜λin =

 n − 8k + 1 8

u

˜λout = ηDec · 2u(u+8) · n3 log( q) · ˜λrun .

(57) For α ∈ { in , out } let ˜Dα = ( C(q, u, n, k, K α), Enc C(q,u,n,k,K α), 2[n]u

|≥˜λα ).

Assuming | ¯N | ≥ ¯N0(r) is sufficiently large, the expression ¯λrun (u, n ) in Equation (23) satisfies ¯λrun (u, n ) = 3 κ3 · MηDec · 3κ3 · log( q) · 2u(u+16) · n16 

≥ 3κ3 · Mn20 

≥ 3κ3 · M

232(log | ¯N |)2/3

= 3 κ3 · ¯λrun (r, ¯N ),

where the inequalities above hold by Equation (55), and the final equality holds by Equation (41). Then because ˜λrun = 3 κ3 · λrun ≤ 3κ3 · ¯λrun (r, ¯N ), we have ˜λrun ≤ ¯λrun (u, n ). Let ¯Rr = ¯R, and let ¯Rq denote the circuit obtained from ¯Rr by replacing every r-ary gate in G

with the analogous q-ary gate. Therefore ¯Rq acts on q-ary dits, but otherwise has the same space usage ¯N and time usage ¯T as ¯Rr. Letting ι : Fr ,→ Fq denote the inclusion, then by Lemma 5.2 we have ¯Rq ◦ ι⊔Kin = ι⊔Kout ◦ ¯Rr. That is, when restricting inputs to the subfield Fr ⊆ Fq, then ¯Rq

has the same output as ¯Rr.Now by Item 2 in Theorem 4.1, there exists a circuit ˜R using space | ˜N | ≤ 3ηDec · log( q) · nu+1 ,time ˜T ≤ O( ¯T · u4n2(log n)2(log q)), and gate set G such that 

 ˜R, ˜Erun = 2 ˜N |⊔ ˜T 

> ≥˜λrun

, ˜Din , ˜Dout 



(58) is a fault-tolerant gadget for ˜¯O(·) = ¯Rq(·). We now apply Lemma 5.3 to reduce the alphabet size of the physical dits of ˜R in the gadget in Equation (50) from q to r. Specifically, fix an Fr-linear isomorphism ϕ : Fκr → Fq such that that first input component maps to the subfield Fr ⊆ Fq, that is, ϕ|Fr ×{ 0}κ−1 = ι. For K ⊆ ¯N ⊆ [k]u, we obtain the code C(r, K, ¯N ) with encoding map Enc C(r,K, ¯N ) in Item 2 in Theorem 5.1 by restricting 

C(q, u, n, k, K ) to messages in FKr ⊆ FKq and applying ϕ−1 to all codeword dits, that is, Enc C(r,K, ¯N ) = ( ϕ−1)⊔[n]u

◦ Enc C(q,u,n,k,K ) ◦ι⊔K

C(r, K, ¯N ) = Enc C(r,K, ¯N )(FKr ).

Then C(r, K, ¯N ) is a code over r-ary dits with length 

n(r, ¯N ) = κ · nu ≤ (log | ¯N |) · M

64 dimension |K|, and distance d(r, ¯N ) at least the distance of C(q, u, n, k, K ), so that 

d(r, ¯N ) ≥ (n − 8k + 1) u

≥ M · (1 /2) u

≥ M

22(log | ¯N |)1/3 .

The above inequalities hold by Equation (55) assuming that | ¯N | ≥ ¯N0(r) is sufficiently large. Now we define a circuit R on r-ary dits using space N = [ κ] × ˜N and time T = κ3 · ˜T as follows: for each q-ary gate G ∈ G acting on some set of dits B ⊆ ˜N in some timestep ˜ t ∈ [ ˜T ] of ˜R, we let R apply the corresponding r-ary circuit RG from Lemma 5.3 to dits [ κ] × B in timesteps 

{κ3(˜ t − 1) + 1 , . . . , κ 3˜t} ⊆ [T ]. Assuming | ¯N | ≥ ¯N0(r) is sufficiently large, the space and time usages of R satisfy 

|N | = κ · | ˜N |≤ 3ηDec · log( q) · nu+1 

≤ 22(log | ¯N |)2/3

· MT = κ3 · ˜T

≤ O(κ3 · ¯T · u4n2(log n)2(log q)) 

≤ ¯T · 24(log | ¯N |)2/3

,

where above we apply the bounds in Equation (55). We will now complete the proof of Item 2 in Theorem 5.1 by showing the following. 

Claim 5.9. The data  R, Erun = 2 N |≥λrun , D in , D out 

 (59) 

forms a fault-tolerant gadget for ¯R(·) = ¯Rr(·), where Din , D out and λrun , λ in , λ out are defined as in Item 2 in Theorem 5.1. 

To prove Claim 5.9, we will use the following bounds relating the variables defined in Equa-tion (57) to those defined in Equation (42), where below we assume | ¯N | ≥ ¯N0(r) is sufficiently large: ˜λin ≥

 n

16 

u

= M

24u ≥ M

28(log | ¯N |)1/3 = λin 

˜λout = ηDec · 2u(u+8) · n3 log( q) · 3κ4 · λrun 

κ

≤ n5 · λrun 

κ

≤ 28(log | ¯N |)2/3

· λrun 

κ

= λout 

κ .

(60) 

Proof of Claim 5.9. The proof is similar as the proof of Claim 5.7 above, except here we show fault-tolerance instead of fault-detection. 65 First, Item 1 in Definition 2.17 holds for the gadget in Equation (59) by the fault-tolerance of the gadget in Equation (58) along with Lemma 5.3. Specifically, in the absence of errors, then R

simply runs ˜R, but expresses each q-ary dit using κ r -ary dits according to the isomorphism ϕ,and spends κ3 timesteps simulating each q-ary gate G using a circuit RG of r-ary gates. Therefore when we run R on input y = Enc C(r,K in , ¯N )(x) for x ∈ FKin  

> r

, we must have 

R(y) = ( ϕ−1)⊔[n]u

◦ ˜R ◦ Enc C(q,u,n,k,K in )(x)= ( ϕ−1)⊔[n]u

◦ Enc C(q,u,n,k,K out ) ◦ ¯Rq(x)= Enc C(r,K out , ¯N ) ◦ ¯Rr(x).

We now similarly show that Item 2 in Definition 2.17 holds for the gadget in Equation (59) by using the fact that this property holds for the gadget in Equation (58). Let x ∈ FKin  

> r

, let y be an input to R that differs from Enc C(r,K in , ¯N )(x) by an error of weight < λ in , and let F be a fault with weight < λ run in each timestep. We want to show that R[F]( y) differs from Enc C(r,K out , ¯N ) ◦ ¯R(x)by an error of weight < λ out .By definition, ˜ y := ϕ⊔[n]u

(y) differs from Enc C(q,u,n,k,K in )(x) = ϕ⊔[n]u

◦ Enc C(r,K in , ¯N )(x) at < λ in 

dits, and λin ≤ ˜λin by Equation (60). Furthermore, R[F]( y) simulates the execution of ˜R[ ˜F](˜ y) for an appropriate fault ˜F. Specifically, for ˜ t ∈ [ ˜T ], we have tran( R[F]; y)|N ×{ κ3·˜t} = ϕ⊔ ˜N (tran( ˜R[ ˜F]; ˜ y)| ˜N ×{ ˜t}), (61) where ˜F is an appropriate fault on ˜R that simulates the effect of the fault F on R. For every gate 

G acting on some dits B ⊆ ˜N in some timestep ˜ t ∈ [ ˜T ] of ˜R, if F has nontrivial support on the restriction to the associated subcircuit RG of R, then at timestep ˜ t we let ˜F set the value of dits 

B to equal ( ϕ−1)⊔3(tran( R[F]; y)|[κ]×B×{ κ3·˜t}). If instead F has no support on the restriction to this subcircuit RG of R, then ˜F has no support on dits B in timestep ˜ t. Because every gate G ∈ G 

in our gate set has |B| ≤ 3, for each ˜ t ∈ [ ˜T ], each of the κ3 timesteps {κ3(˜ t − 1) + 1 , . . . , κ 3˜t} of 

R[F] can induce errors on up to 3 λrun dits in ˜R[ ˜F]. Therefore ˜F has weight < ˜λrun = 3 κ3 · λrun in timestep ˜ t (see Equation (57)). Thus by the fault-tolerance of the gadget in Equation (58), ˜R[ ˜F](˜ y) differs from Enc C(q,u,n,k,K out ) ◦ ¯R(x) = ϕ⊔[n]u

◦ Enc C(r,K out , ¯N ) ◦ ¯R(x)at < ˜λout dits. Then by Equations (60) and (61), we conclude that R[F]( y) differs from Enc C(r,K out , ¯N ) ◦ ¯R(x)at < κ · ˜λout ≤ λout dits. Thus Item 2 in Definition 2.17 holds for the gadget in Equation (59). 

# 6 Probabilistically Checkable Proofs 

In Theorem 6.2 below, we apply Theorem 5.1 to obtain probabilistically checkable proofs (PCPs) for circuit-satisfiability (and therefore for NP ) with almost-linear size and inverse-polylogarithmic soundness. In this section, we work over the binary alphabet F2 for simplicity in notation, but our techniques and results also apply to larger alphabets. We do still use the term “dit” instead of “bit” for consistency with the rest of the paper. We begin by defining CSPs 6.

> 6Our definition of CSPs is sufficient for our purposes, though more general definitions are useful in other settings.

66 Definition 6.1. A constraint satisfaction problem (CSP) instance P with n variables and m con-straints consists of a (multi)set P = {P 1, . . . , Pm} of m functions called constraints , where for each 

j ∈ [m], the constraint Pj : FBj 

> 2

→ F2 is a function for some Bj ⊆ [n]. The instance P has locality 

ℓ if every |Bj | ≤ ℓ, and if every i ∈ [n] is contained in at most ℓ of the sets Bj .For an assignment x ∈ F[n] 

> q

of elements of [n] to values in F2, a constraint Pj is satisfied if 

Pj (x|Bj ) = 0 . For δ > 0, if ≥ (1 − δ)m of the constraints are satisfied, we say x is a δ-approximate solution to P. A 0-approximate solution is simply called a solution or a satisfying assignment ; a CSP instance P with a satisfying assignment is called satisfiable .

Below, recall that the size of a circuit R using a gate set G is the number of input dits plus the total number of non-identity gates in the circuit. 

Theorem 6.2 (PCPs for circuit-satisfiability with inverse-polylogarithmic soundness) . Fix an ar-bitrary ϵ > 0, and fix an arbitrary set G of gates, each of which acts on O(1) bits (i.e. 2-ary dits). Then for some s0(ϵ) > 0, there exists a polynomial-time algorithm that takes as input a circuit 

R using gate set G of size s ≥ s0(ϵ) that has a single output dit, and outputs a CSP instance P

with the following properties. P has n ≤ s1+ ϵ variables, m = Θ( n) constraints, and locality O(1) .Furthermore, we have: 1. (Completeness) If there exists an input x such that R(x) = 0 , then P is satisfiable. 2. (Soundness) If every input x has R(x) = 1 , then P has no 1/(log m)O(1 /ϵ )-approximate solution. Proof. We will first construct a constant-depth circuit ¯R that takes as input a transcript of the execution of R, and outputs a vector of all-0s iff the transcript describes a fault-free execution of R on some input x with output R(x) = 0. We will then apply Item 1 in Theorem 5.1 to construct a mending fault-detecting gadget ˜R for ¯R(·). Our final CSP instance P will have variables corresponding to dits in the transcript of ˜R on some input, and constraints verifying that this transcript describes a fault-free execution of ˜R with all-0s output. The mending fault-detecting property implies that if few constraints of this CSP instance are violated, then the transcript indeed describes an execution of ˜R with a low-weight fault, and with some input x that has all-0s output. Thus soundness follows; completeness holds by construction. We now present the details for how we construct the CSP instance P. Each stage of our construction will be explicit, and hence we will obtain a polynomial-time algorithm to construct P.We begin by showing how to construct the circuit ¯R.

Claim 6.3. There exists a circuit ¯R using space | ¯N | = Θ( s), time ¯T = O(1) , and gate set G′ =

{Init , Term , X∗, CX ∗, CCX ∗} such that ¯R has an input yielding all-0s output iff R has an input yielding output 0. Furthermore, ¯R can be constructed from R in poly( s) time. Proof. Let R use space N and time T . Let S ⊆ N × { 0, . . . , T } be the set of all pairs ( j, t ) ∈

N × { 0, . . . , T } such that either t = 0 and dit j is an input dit of R, or t = T and j = jout is the unique output dit of R, or else t ∈ { 1, . . . , T − 1} and dit j is an active dit that is acted upon by a non-identity gate in timestep t. In words, S contains the active dits across all timesteps of R, excluding locations in which a dit idles, i.e. experiences an identity gate. Furthermore, each non-identity gate G ∈ G acts on O(1) dits, and therefore contributes O(1) elements to S for every appearance of G in R. Thus |S| = Θ( s). 67 We define ¯R to take as input a set of dits labeled by S. Let there be g non-identity gates in R,labeled by the set [ g] (in an arbitrary order); we also include the gate acting on the output dit jout 

in timestep T as a “non-identity gate,” even if it acts as the identity. For i ∈ [g], let the ith gate 

Gi ∈ G act on dits Bi ⊆ N in timestep ti ∈ [T ]. Let S1 

> i

⊆ Bi × { ti} be the set of output dits of Gi.Let S0 

> i

⊆ N × [T ] contain the input dits of Gi in their most recent non-idling timesteps; that is, for each input dit j ∈ Bi, then S0 

> i

contains the pair ( j, t ) that lies in S for the greatest possible value of t < t i. We then let Si = S0 

> i

⊔ S1 

> i

. In words, Si ⊆ S contains the input and output dits of the ith gate in the transcript of R, where we exclude idling locations. We then let ¯R have g + 1 output dits labeled by {0, . . . , g }. On input z ∈ FS 

> 2

, the 0th output ¯R(z)0 equals the input dit z(jout ,T ), which corresponds to R’s output dit when viewing z as a (partial) transcript for R. For i ∈ [g], the ith output ¯R(z)i equals 1 if z|S1

> i

̸ = Gi(z|S0 

> i

), and 0 otherwise. That is, ¯R(z)i indicates if there was an error in the execution of Gi, again viewing z as a (partial) transcript for R. Because each output dit of ¯R only depends on max i |Si| ≤ max i 2|Bi| =

O(1) input dits, and each input dit affects at most 2 output dits, we can implement ¯R with gate set G′ (or any universal gate set) using space O(|S|) = O(s) and time O(1). Specifically, we color the g + 1 output dits of ¯R with a constant number of colors, such that no two output dits that both depend on some shared input dit have the same color. We then loop through the colors sequentially, and for each color compute all outputs with that color in parallel. There is a polynomial-time algorithm to color the vertices of a graph with maximum degree ∆ using ∆ + 1 colors such that no two adjacent vertices share a color; simply sequentially assign each vertex a distinct color from its previously assigned neighbors. Thus ¯R can be constructed from R

in poly( s) time. By construction, if R has some input x with R(x) = 0, then ¯R(tran( R; x)|S ) = 0 g+1 . Con-versely, if every input x for R has output R(x) = 1, then every z ∈ FS 

> 2

has ¯R(z)̸ = 0 g+1 . For if instead some z ∈ FS 

> 2

had ¯R(z) = 0 g+1 , then z would provide a transcript for the execution for R

on input x = z|N ×{ 0} with output R(x) = z(jout ,T ) = ¯R(z)0 = 0, a contradiction. Here the fact that R(x) = z(jout ,T ) follows from the assumption that ¯R(z) = 0 g+1 , which implies that z pro-vides a fault-free transcript for the exeuction of R, where idling locations have been “shortcutted” (i.e. removed from the transcript and from the checks that gates executed without fault). Thus ¯R

exhibits the desired properties, completing the proof of the claim. We now apply Item 1 in Theorem 5.1 to the circuit ¯R using space ¯N and time ¯T in Claim 6.3 with alphabet size r = 2. We set the ϵ parameter in Theorem 5.1 to equal the value ϵ′ = ϵ/ 16 for ϵ

defined in Theorem 6.2, so that in Theorem 5.1 we have 

M = M (ϵ′, ¯N ) ∈ [| ¯N |1+ ϵ′

, | ¯N |1+4 ϵ′

]. (62) Then assuming | ¯N | = Θ( s) is sufficiently large (i.e. larger than ¯N0(2 , ϵ ′) in Theorem 5.1, which we can ensure by setting s0(ϵ) sufficiently large), Theorem 5.1 provides a polynomial-time constructable mending fault-detecting gadget 

R′, Erun = 2 N ′

|≥λrun , D in , D out , Edet = G 

> t′∈[T′]

(2 Edet ,t ′ |≥λdet )



68 for ¯R(·), using space |N ′| ≤ (log | ¯N |)2 · M , time T ′ ≤ ¯T · (log | ¯N |)8/ϵ ′

, and gate set G′, where 

Din = ( Cin , Enc in , Ein = 2 [n(2 ,ϵ ′, ¯N )] |≥λin )

Dout = ( Cout , Enc out , Eout = 2 [n(2 ,ϵ ′, ¯N )] |≥λout )are decorated codes of length n(2 , ϵ ′, ¯N ) and distance d(2 , ϵ ′, ¯N ), such that 

d(2 , ϵ ′, ¯N ) ≥ M

232 

λrun = M

264 · (log | ¯N |)16 η/ϵ ′

λout = (log | ¯N |)16 η/ϵ ′

· λrun = M

264 

λdet = λrun = M

264 · (log | ¯N |)16 η/ϵ ′ .

Recall above that η > 1 is the absolute constant in Lemma 2.5. Let S′ ⊆ N ′ × { 0, . . . , T ′} denote the set of all active dits of R′ across all timesteps, so that transcripts of R′ lie in FS′ 

> 2

. We define the CSP instance P to act on a collection y ∈ FS′ 

> 2

of |S′|

variables labeled by elements of S′, with the following three collections of constraints: 1. (Fault constraints) For every gate G ∈ G ′ that R′ applies to dits B ⊆ N ′ in some timestep 

t′ ∈ [T ′] with input dits S0 ⊆ B × { t′ − 1} and output dits S1 ⊆ B × { t′}, we impose a constraint that has value 0 if y|S1 = G(y|S0 ), and value 1 if y|S1̸ = G(y|S0 ). 2. (Detector constraints) For every t′ ∈ [T ′] and every detector dit j′ ∈ Edet ,t ′ , we impose a constraint that has value y(j′,t ′).3. (Output constraints) For every output dit ( j′, T ′) of R′, we impose a constraint that has value 

y(j′,T ′).Assuming s0(ϵ) and therefore s and | ¯N | are sufficiently large, the number |S′| of variables in P

satisfies 

|S′| ≤ | N ′| · T ′ ≤ M · (log | ¯N |)O(1 /ϵ )

≤ | ¯N |1+ ϵ/ 4 · (log | ¯N |)O(1 /ϵ )

≤ s1+ ϵ

|S′| ≥ n(2 , ϵ ′, ¯N ) ≥ d(2 , ϵ ′, ¯N ) ≥ M

32 .

By definition, each variable participates in at most two fault constraints, one detector constraint, and one output constraint. Similarly, as each gate in G′ acts on ≤ 3 dits, each constraint depends on ≤ 6 variables. Thus P has Ω( M ) ≤ m = Θ( |S′|) ≤ M · (log | ¯N |)O(1 /ϵ ) (63) constraints, and locality O(1), as desired. Also by definition, we can construct P in polynomial time given the circuit R′ and the detector sets Edet ,t ′ .In words, P takes as input a transcript y for R′, and checks that each gate was executed without fault, that all detector dits have value 0, and that all output dits have value 0. This definition immediately implies completeness: 69 Claim 6.4. The completeness claim in Theorem 6.2 holds. Proof. If there exists an input x for R with output R(x) = 0, then by Claim 6.3, there exists an input z to ¯R with output ¯R(z) = 0 g+1 . Then by the definition of a fault-detecting gadget, letting 

z′ = Enc in (z), we have 

R′(z′) = Enc out ◦ ¯R(z) = Enc out (0 g+1 ) = 0 n(2 ,ϵ ′, ¯N ),

where the final equality above holds because every linear code maps the all-0s input to the all-0s output. The fault-detecting property also implies that the transcript tran( R′; z′) has value 0 on the restriction to every detector set Edet ,t ′ × { t′}. Thus all constraints have value 0 for the assignment 

z′ = Enc in (z), so P is satisfiable. We now conclude the proof of Theorem 6.2 by proving soundness: 

Claim 6.5. The soundness claim in Theorem 6.2 holds. Proof. Assume that every input x for R has output R(x) = 1. Let y ∈ FS′ 

> 2

be an arbitrary assignment of the variables of P. We will show that P has at least m/ (log m)O(1 /ϵ ) unsatisfied constraints on assignment y.Let z′ ∈ Fn(2 ,ϵ ′, ¯N )2 equal the restriction of y to the input dits of R′ in N ′ × { 0}. Then we can express y as the transcript y = tran( R′[F′]; z′) of a noisy execution of R′, where we define the fault F′ as follows. For every gate G ∈ G ′ that R′ applies to dits B ⊆ N ′ in some timestep t′ ∈ [T ′]with input dits S0 ⊆ B × { t′ − 1} and output dits S1 ⊆ B × { t′}, if y|S1̸ = G(y|S0 ), we let F′ set the value of dits S1 to be G(y|S0 ) in timestep t′. If instead y|S1 = G(y|S0 ), then F′ has no support inside (i.e. does not corrupt) S1 in timestep t′.Then y = tran( R′[F′]; z′) by induction on t′ = 0 , . . . , T ′. Indeed, for the base case, by definition 

y|N ′×{ 0} = z′ = tran( R′[F′]; z′)|N ′×{ 0}. For the inductive step, for t′ ∈ [T ′], assuming y|N ′×{ t′−1} =tran( R′[F′]; z′)|N ′×{ t′−1}, then for every dit j′ in which the gates at time t′ do not set the dit’s value to y(j′,t ′), the fault F′ sets the value of dit j′ to be y(j′,t ′).Furthermore, by definition for every set S1 ⊆ B × { t′} ⊆ N ′ × [T ′] described above on which F′

has nontrivial support, then by definition the fault constraint associated to this set of dits is not satisfied on assignment y. Because |S1 ≤ | B| ≤ 3 as every gate G ∈ G ′ acts on ≤ 3 dits, it follows that at least |F ′|/3 fault constraints are not satisfied. Thus if |F ′| ≥ λrun , then the number of unsatisfied constraints is at least 

λrun 

3 ≥ M

(log | ¯N |)O(1 /ϵ ) ≥ m

(log m)O(1 /ϵ ) ,

as desired, where the second inequality above holds by Equations (62) and (63). Therefore it only remains to consider the case where |F ′| < λ run . In this case, the mending fault-detecting property ensures that either there exists z ∈ FS 

> 2

such that R′[F′]( z′) is a Eout -deviation of Enc out ◦ ¯R(z), or else there exists t′ ∈ [T ′] such that | tran( R′[F′]; z′)|Edet ,t ′ ×{ t′}| ≥ λdet .In the first case, by the assumption that every input x to R has R(x) = 1, then Claim 6.3 implies that ¯R(z)̸ = 0 g+1 , and hence Enc out ◦ ¯R(z) is a nonzero codeword of Cout , which must have weight 

≥ d(2 , ϵ ′, ¯N ). Therefore 

|R ′[F′]( z′)| ≥ d(2 , ϵ ′, ¯N ) − λout ≥ M

264 ≥ m

(log | ¯N |)O(1 /ϵ ) .

70 Because R′[F′]( z′) equals the restriction of y = tran( R′[F′]; z′) to the output dits in N ′ × { T ′},all of which have output constraints checking that their values are 0, we must have at least 

m/ (log | ¯N |)O(1 /ϵ ) unsatisfied constraints, as desired. The only remaining case is if there exists t′ ∈ [T ′] such that | tran( R′[F′]; z′)|Edet ,t ′ ×{ t′}| ≥ 

λdet . Because y = tran( R′[F′]; z′), in this case at least λdet = λrun ≥ m/ (log | ¯N |)O(1 /ϵ ) detector constraints are unsatisfied. Thus we have shown that if every input x for R has output R(x) = 1, then every assignment 

y ∈ FS′ 

> 2

for P has ≥ m/ (log m)O(1 /ϵ ) unsatisfied constraints, as desired. 

# Acknowledgments 

We thank Venkatesan Guruswami for helpful discussions. A.A. acknowledges support through the NSF Award Nos. 2238836, 2430375 and QCIS-FF: Quantum Computing & Information Science Faculty Fellow at Harvard University (NSF 2013303). N.P.B. acknowledges support through the Quantum Research Pod, CIQC and the Simons Institute for the Theory of Computing. L.G. acknowledges support from ONR grant N00014-24-1-2491, a UC Noyce initiative award, a Google PhD Fellowship, and a National Science Foundation Graduate Research Fellowship under Grant No. DGE 2146752. This work was done in part while the authors were attending the Simons Institute for the Theory of Computing, with support from NSF QLCI Grant No. 2016245. 

AI use disclosure: All proofs and writing are the work of the human authors. ChatGPT was used to check the final draft for typos and minor presentation issues. 

# References 

[AAV13] Dorit Aharonov, Itai Arad, and Thomas Vidick. The Quantum PCP Conjecture. 

arXiv:1309.7495 [quant-ph] , September 2013. arXiv: 1309.7495. [ABN24] Anurag Anshu, Nikolas P. Breuckmann, and Quynh T. Nguyen. Circuit-to-Hamiltonian from Tensor Networks and Fault Tolerance. In Proceedings of the 56th Annual ACM Symposium on Theory of Computing , STOC 2024, pages 585–595, New York, NY, USA, June 2024. Association for Computing Machinery. [ALM +98] Sanjeev Arora, Carsten Lund, Rajeev Motwani, Madhu Sudan, and Mario Szegedy. Proof verification and the hardness of approximation problems. Journal of the ACM ,45(3):501–555, May 1998. [AN02] Dorit Aharonov and Tomer Naveh. Quantum np - a survey. arXiv preprint quant-ph/0210077, 2002. [AS98] Sanjeev Arora and Shmuel Safra. Probabilistic checking of proofs: a new characteri-zation of NP. J. ACM , 45(1):70–122, January 1998. 71 [Bat68] K. E. Batcher. Sorting networks and their applications. In Proceedings of the April 30–May 2, 1968, spring joint computer conference , AFIPS ’68 (Spring), pages 307–314, New York, NY, USA, April 1968. Association for Computing Machinery. [BEGY19] Mark Braverman, Klim Efremenko, Ran Gelles, and Michael A. Yitayew. Optimal short-circuit resilient formulas. In 34th Computational Complexity Conference, CCC 2019 . Schloss Dagstuhl- Leibniz-Zentrum fur Informatik GmbH, Dagstuhl Publishing, July 2019. [BGH +06] Eli Ben-Sasson, Oded Goldreich, Prahladh Harsha, Madhu Sudan, and Salil Vadhan. Robust PCPs of Proximity, Shorter PCPs, and Applications to Coding. SIAM Journal on Computing , 36(4):889–974, January 2006. [BGV26] Nikolas Breuckmann, Louis Golowich, and Umesh Vazirani. Fault-Tolerant Quantum Computation with Adversarial Errors. In 2026 IEEE 67th Annual Symposium on Foundations of Computer Science (FOCS) , 2026. To appear. [BISW18] Dan Boneh, Yuval Ishai, Amit Sahai, and David J. Wu. Quasi-Optimal SNARGs via Linear Multi-Prover Interactive Proofs. In Jesper Buus Nielsen and Vincent Rijmen, editors, Advances in Cryptology – EUROCRYPT 2018 , pages 222–255, Cham, 2018. Springer International Publishing. [BMVY25] Mitali Bafna, Dor Minzer, Nikhil Vyas, and Zhiwei Yun. Quasi-linear size pcps with small soundness from hdx. In Proceedings of the 57th Annual ACM Symposium on Theory of Computing , pages 45–53, 2025. [BOGW88] Michael Ben-Or, Shafi Goldwasser, and Avi Wigderson. Completeness theorems for non-cryptographic fault-tolerant distributed computation. In Proceedings of the twen-tieth annual ACM symposium on Theory of computing , STOC ’88, pages 1–10, New York, NY, USA, January 1988. Association for Computing Machinery. [Bom16] H. Bombin. Dimensional Jump in Quantum Error Correction, May 2016. arXiv:1412.5079 [quant-ph]. [BSKK +16] Eli Ben-Sasson, Yohay Kaplan, Swastik Kopparty, Or Meir, and Henning Stichtenoth. Constant Rate PCPs for Circuit-SAT with Sublinear Query Complexity. J. ACM ,63(4):32:1–32:57, November 2016. [BSS04] Eli Ben-Sasson and Madhu Sudan. Robust Locally Testable Codes and Products of Codes. In Klaus Jansen, Sanjeev Khanna, Jos´ e D. P. Rolim, and Dana Ron, ed-itors, Approximation, Randomization, and Combinatorial Optimization. Algorithms and Techniques , Lecture Notes in Computer Science, pages 286–297, Berlin, Heidel-berg, 2004. Springer. [BSS08] Eli Ben-Sasson and Madhu Sudan. Short PCPs with Polylog Query Complexity. SIAM Journal on Computing , 38(2):551–607, January 2008. [DIK10] Ivan Damg˚ ard, Yuval Ishai, and Mikkel Krøigaard. Perfectly Secure Multiparty Com-putation and the Computational Overhead of Cryptography. In Henri Gilbert, editor, 72 Advances in Cryptology – EUROCRYPT 2010 , pages 445–465, Berlin, Heidelberg, 2010. Springer. [Din07] Irit Dinur. The PCP theorem by gap amplification. Journal of the ACM , 54(3):12–es, June 2007. [DO77] Roland L’vovich Dobrushin and S. I. Ortyukov. Upper bound on the redundancy of self-correcting arrangements of unreliable functional elements. Problemy Peredachi Informatsii , 13(3):56–76, 1977. [EHK +22] Klim Efremenko, Bernhard Haeupler, Yael Tauman Kalai, Pritish Kamath, Gillat Kol, Nicolas Resch, and Raghuvansh R. Saxena. Circuits resilient to short-circuit errors. In Proceedings of the 54th Annual ACM SIGACT Symposium on Theory of Computing , STOC 2022, pages 582–594, New York, NY, USA, June 2022. Association for Computing Machinery. [GCZ25] Louis Golowich, Kathleen Chang, and Guanyu Zhu. Constant-Overhead Addressable Gates via Single-Shot Code Switching, October 2025. arXiv:2510.06760 [quant-ph]. [GOS24] Tom Gur, Jack O’Connor, and Nicholas Spooner. Perfect Zero-Knowledge PCPs for #P. In Proceedings of the 56th Annual ACM Symposium on Theory of Computing ,STOC 2024, pages 1724–1730, New York, NY, USA, June 2024. Association for Com-puting Machinery. [GOS25] Tom Gur, Jack O’Connor, and Nicholas Spooner. A Zero-Knowledge PCP Theorem. In 

Proceedings of the 57th Annual ACM Symposium on Theory of Computing , STOC ’25, pages 986–994, New York, NY, USA, June 2025. Association for Computing Machinery. [GRS22] Venkatesan Guruswami, Atri Rudra, and Madhu Sudan. Essential coding theory. Draft available at http://www. cse. buffalo. edu/ atri/courses/coding-theory/book , 2022. [GS95] A. Gal and M. Szegedy. Fault tolerant circuits and probabilistically checkable proofs. In Proceedings of Structure in Complexity Theory. Tenth Annual IEEE Conference ,pages 65–73, Minneapolis, MN, USA, 1995. IEEE Comput. Soc. Press. [HNP25] Zhiyang He, Quynh T. Nguyen, and Christopher A. Pattison. Composable Quantum Fault-Tolerance, August 2025. arXiv:2508.08246 [quant-ph]. [JO19] Tomas Jochym-O’Connor. Fault-tolerant gates via homological product codes. Quan-tum , 3:120, February 2019. [KLM97] Dan Kleitman, Tom Leighton, and Yuan Ma. On the Design of Reliable Boolean Circuits That Contain Partially Unreliable Gates. Journal of Computer and System Sciences , 55(3):385–401, December 1997. [KLR12] Yael Tauman Kalai, Allison Lewko, and Anup Rao. Formulas Resilient to Short-Circuit Errors. In 2012 IEEE 53rd Annual Symposium on Foundations of Computer Science ,pages 490–499, New Brunswick, NJ, USA, October 2012. IEEE. 73 [KP25] Gleb Kalachev and Pavel Panteleev. Maximally Extendable Product Codes are Good Coboundary Expanders. In 2025 IEEE 66th Annual Symposium on Foundations of Computer Science (FOCS) , pages 1512–1524, December 2025. ISSN: 2575-8454. [LFKN92] Carsten Lund, Lance Fortnow, Howard Karloff, and Noam Nisan. Algebraic methods for interactive proof systems. J. ACM , 39(4):859–868, October 1992. [Mei12] Or Meir. Combinatorial PCPs with Short Proofs. In 2012 IEEE 27th Conference on Computational Complexity , pages 345–355, June 2012. ISSN: 1093-0159. [Mei13] Or Meir. IP = PSPACE Using Error-Correcting Codes. SIAM J. Comput. , 42(1):380– 403, January 2013. [Neu56] J. von Neumann. Probabilistic Logics and the Synthesis of Reliable Organisms From Unreliable Components. In Automata Studies , pages 43–98. Princeton University Press, 1956. Section: Automata Studies. [NP25] Quynh T. Nguyen and Christopher A. Pattison. Quantum Fault Tolerance with Constant-Space and Logarithmic-Time Overheads. In Proceedings of the 57th Annual ACM Symposium on Theory of Computing , STOC ’25, pages 730–737, New York, NY, USA, June 2025. Association for Computing Machinery. [Pip85] Nicholas Pippenger. On networks of noisy gates. In 26th Annual Symposium on Foundations of Computer Science (sfcs 1985) , pages 30–38, October 1985. ISSN: 0272-5428. [Spi96] D. A. Spielman. Highly fault-tolerant parallel computation. In Proceedings of the 37th Annual Symposium on Foundations of Computer Science , FOCS ’96, page 154, USA, October 1996. IEEE Computer Society. [THL +25] Shi Jie Samuel Tan, Yifan Hong, Ting-Chun Lin, Michael J. Gullans, and Min-Hsiu Hsieh. Single-Shot Universality in Quantum LDPC Codes via Code-Switching, October 2025. arXiv:2510.08552 [quant-ph]. [Vid15] Michael Viderman. A combination of testability and decodability by tensor products. Random Structures & Algorithms , 46(3):572–598, 2015. eprint: https://onlinelibrary.wiley.com/doi/pdf/10.1002/rsa.20498. [XZB +25] Qian Xu, Hengyun Zhou, Dolev Bluvstein, Madelyn Cain, Marcin Kalinowski, John Preskill, Mikhail D. Lukin, and Nishad Maskara. Batched high-rate logical operations for quantum LDPC codes, October 2025. arXiv:2510.06159 [quant-ph]. 74